# Documentation Index

Documentation for the Nirogidhara AI Command Center.

> **Phase 16N — Director AI Daily Briefing Real Data Wiring + Safe Recommendation Pack is implemented and pushed to origin/main; VPS production verification pending.** Read-only / internal-only decision layer over the existing Phase 16I–16M workboard data — extends `apps.ai_copilot` with **NO new model and NO migration**: a new `apps.ai_copilot.briefing` module + 3 read-only endpoints `GET /api/v1/ai-copilot/director-briefing/` + `.../summary/` + `.../recommendations/` (`AuthenticatedReadAdminWrite`, GET-only; POST/PATCH/DELETE → 405). Composes the Phase 16M analytics + Phase 16K director-attention queue + pending suggestion/action counts into a deterministic AI briefing (executive summary, attention items, department/member focus, safe internal-only recommendations with `permittedAction` ∈ {internal_review, assign_internal, create_internal_action, review_blocker, no_external_action}, blocked-live-actions, safety snapshot). New **Director AI Briefing** section on `/operations/ai-copilot` (read-only, no live-action button). Never calls a provider, mutates a business/workboard row, enqueues a Celery job, or touches `RuntimeKillSwitch` / `SandboxState`. Verification: backend 17 Phase 16N + 97 regression = 114 passed targeted, frontend 423/423, lint 0, build green; `makemigrations --check` → "No changes detected" (NO migration) + `manage.py check` clean. **Phase 16N is implemented and pushed, VPS production verification pending — do NOT mark production verified until a VPS deploy + Director browser validation pass. Phase 16O is NOT started.** Phase 16M (`a992207`) is the previous verified baseline.
>
> **Previous verified baseline: Phase 16M — Workboard Analytics + SLA Throughput Dashboard, PRODUCTION VERIFIED on the VPS and CLOSED at commit `a992207`.** A **read-only / internal-only** analytics layer over the existing Phase 16J/16K/16L workboard; extends the internal-only `apps.ai_copilot` app with **NO new model and NO migration** — 1 read-only service `get_workboard_analytics(window_days=14)` + 1 read-only endpoint `GET /api/v1/ai-copilot/workboard/analytics/` (`AuthenticatedReadAdminWrite`, GET-only; POST/PATCH/DELETE → 405). Derives, purely from existing fields, summary cards + per-department analytics + per-member workload + SLA analytics + blocker analytics + a daily throughput trend (safe empty state); SLA reuses Phase 16K `compute_sla_status` (read-time). Every response carries `readonly:true` / `internalOnly:true` / `providerActionAttempted:false` / `providerActionTaken:false` / `externalActionAllowed:false` / `externalActionTaken:false`. New **Workboard Analytics + SLA Throughput** section on `/operations/ai-copilot` (safety banner + summary cards + department/member tables + SLA/blocker panel + trend table) — **no mutation / provider / send / call / payment / dispatch / auto-execute button**. Read-only/internal-only: never calls a provider, mutates a business row, enqueues a business Celery job, or touches `RuntimeKillSwitch` / `SandboxState` / operations-user roles (asserted by a defensive test). Verification: backend 15 Phase 16M + 82 regression = 97 passed (targeted), frontend 410/410 (+12), lint 0 errors, build green; `makemigrations --check` → "No changes detected" (NO migration) + `manage.py check` clean. **Phase 16N is implemented and pushed to origin/main (VPS verification pending); Phase 16O is NOT started and requires a separate written Director directive. Phase 16M is PRODUCTION VERIFIED on the VPS and CLOSED at commit `a992207`.** Phase 16L (`9d144f5`, docs `05f048e`) is the previous verified baseline. Current safety state remains AI Paused / Sandbox OFF / Sync Live; the Phase 15 safety shell remains frozen at `eefd8b3`.
>
> **Phase 16L — Scoped Team Member Work Permissions + My Work Queue, PRODUCTION VERIFIED on the VPS and CLOSED at commit `9d144f5`.** Lets internal team members safely work their own assigned internal actions (+ claim eligible department work) without broad Director/Admin power. Extends the internal-only `apps.ai_copilot` app with 1 additive model `AiWorkboardDepartmentMember` (`user` / `department` [the 9 Phase 16K teams] / `is_active` / `can_claim` / `can_work` / `can_complete` / `created_by` / timestamps; partial-unique active membership per user+dept); migration `ai_copilot.0004_phase16l_workboard_department_member` (pure additive). Scoped rules via `services.can_user_work_action` / `can_user_claim_action`: Director/Admin/Superuser keep full control; a non-admin claims an unclaimed non-terminal action only with an active dept membership (`can_claim=true`); the assigned user can start/block/unblock/complete-internal/add-note their own action (per-dept `can_work` / `can_complete=false` further restricts); assign/reassign + membership-management stay Director/Admin-only; viewers read-only; non-assignee/non-member → `403`. 7 new endpoints (`workboard/my/`, `workboard/my/summary/`, `workboard/my-permissions/`, `workboard/department-members/` GET+POST, `.../<id>/activate/` + `.../deactivate/`); the 6 Phase 16K scoped transitions now run `IsAuthenticated` + service scoped checks; workboard responses carry per-action `permissions` booleans; new **My work queue** section on `/operations/ai-copilot` with permission-gated buttons. Every path is internal/DB-only and never calls a provider, never broadens operations users to admin; `provider_action_*` / `external_action_*` stay false (asserted by a defensive test). Verification: backend 24 Phase 16L + 58 regression = 82 passed (targeted), frontend 398/398 (+13), lint 0 errors, build green; `makemigrations --check` + `manage.py check` clean. **VPS production verification PASSED (2026-06-05):** HEAD `9d144f5`; pre-deploy backup `phase16l_pre_deploy_2026-06-05_072806.sql`; migration `ai_copilot.0004_phase16l_workboard_department_member` applied OK; `makemigrations --check` / `manage.py check` clean; 16L + 16K/16J/16I tests `[100%]`; healthz 200 OK locally + public; Director browser validation of `/operations/ai-copilot` (My work queue + workboard) + `/operations/pilot-workbench` passed, safety shell unchanged, provider/external flags false (scoped-member/viewer login not visually exercised — backend tests cover that matrix). **Phase 16L is CLOSED. Next planned work is Phase 16O (NOT started; requires a separate written Director directive); Phase 16N is implemented and pushed to origin/main (VPS verification pending).** Phase 16K (`efea751`) is the previous verified baseline.
>
> **Phase 16K — Department Action Workboard + Ownership / SLA Execution Layer, PRODUCTION VERIFIED on the VPS and CLOSED at commit `efea751`.** Makes the Phase 16J AI-approved internal actions operational for real internal departments. Extends the internal-only `apps.ai_copilot` app (no second action queue): `AiApprovedAction` gains 8 additive workboard fields (`department` [9 internal teams], `assignee_user`, `work_status` [`unassigned → assigned → in_progress → blocked → completed_internal`, + `rejected` / `cancelled`], `due_at`, `blocker_reason`, `completed_by`, `completed_at`, `last_activity_at`) + a new model `AiActionWorkEvent`; migration `ai_copilot.0003_phase16k_action_workboard` (pure additive); `sla_status` computed at read time. 12 new endpoints (`workboard/`, `workboard/summary/`, `workboard/director-attention/`, `actions/<id>/{assign,claim,start,block,unblock,complete-internal,reassign,notes}/`) + a Department action workboard section on `/operations/ai-copilot`. Every transition is internal/DB-only — completing a workboard item never calls a provider; `provider_action_*` / `external_action_*` stay false (asserted by a defensive test). All workboard mutations require director/admin/superuser; reads = auth. Verification: backend 23 Phase 16K + 102 regression = 125 passed (targeted), frontend 385/385 (+11), lint 0 errors, build green; `makemigrations --check` + `manage.py check` clean. **VPS production verification PASSED:** browser validation of `/operations/ai-copilot` (Department action workboard + summary cards) — Assign / Claim / Block (Director attention updated) / Unblock / Complete internal / Add note all PASSED; external action taken=false / provider action taken=false; safety shell unchanged; no live provider / customer-facing action; `pytest tests/test_phase16k_action_workboard.py` → 23 passed; healthz OK. Phase 16K ADDS migration `ai_copilot.0003_phase16k_action_workboard` → a fresh VPS deploy requires `migrate` (already applied on the VPS). **Phase 16K is CLOSED. (Historical close-state: Phase 16L was next at 16K close — it has since shipped and is PRODUCTION VERIFIED + CLOSED at `9d144f5`; the current next-planned work is Phase 16O.)** Phase 16J (`aa8cf13`) is the previous verified baseline.
>
> **Phase 16J — AI-Approved Internal Action Queue + Work Execution Bridge, PRODUCTION VERIFIED on the VPS and CLOSED at commit `aa8cf13`.** Extends the internal-only `apps.ai_copilot` app (2 additive models `AiApprovedAction` / `AiApprovedActionEvent`, migration `ai_copilot.0002_phase16j_ai_action_queue`, 7 endpoints under `/api/v1/ai-copilot/actions...`, Approved action queue section on `/operations/ai-copilot`): converts an **approved** Phase 16I AI suggestion into an internal-only work item (calling follow-up / QA review / pilot task / customer-or-order note / callback / RTO review / payment follow-up / dispatch review / Director review item) that a human applies. Only an approved suggestion can become an action (else HTTP 409); applying is DB-only (may create an internal `PilotTask` via the Phase 16H safe service, or record a `result_payload`) and never calls a provider — every action keeps `provider_action_attempted` / `provider_action_taken` / `external_action_allowed` / `external_action_taken` = false. Verification: backend 102 passed targeted (16 new), frontend 374/374 (+7 new), lint 0 errors, build green; `makemigrations --check` + `manage.py check` clean. **VPS production verification PASSED:** browser validation of the Approved action queue on `/operations/ai-copilot` — an approved suggestion created an internal action (PENDING INTERNAL ACTION → Apply → APPLIED INTERNAL; Reject → REJECTED); safety flags stayed false; safety shell unchanged; no live provider / customer-facing action; `migrate --noinput` → "No migrations to apply"; `pytest tests/test_phase16j_ai_action_queue.py` → 16 passed; `GET /api/healthz/` OK. Phase 16J ADDS migration `ai_copilot.0002_phase16j_ai_action_queue` → a fresh VPS deploy requires `migrate` (already applied on the VPS). **Phase 16J is CLOSED. (Historical close-state: Phase 16K was next at 16J close — it has since shipped and is PRODUCTION VERIFIED + CLOSED at `efea751`; the current next-planned work is Phase 16O.)** Phase 16I (`0f91f6b`) is the previous verified baseline.
>
> **Phase 16I — AI Copilot Enablement + Human Approval Workflow, PRODUCTION VERIFIED on the VPS and CLOSED at commit `0f91f6b`.** New internal-only app `apps.ai_copilot` (2 models `AiCopilotSuggestion` / `AiCopilotReviewEvent`, migration `ai_copilot.0001_phase16i_ai_copilot`, 5 endpoints under `/api/v1/ai-copilot/`, page `/operations/ai-copilot`): an internal AI copilot / decision-support layer that analyzes / drafts / recommends / scores / summarises (lead summary, call priority, call script, objection handling, compliance risk, pilot recommendation, director briefing, WhatsApp/payment/RTO draft) — a human approves before any business action; AI is NOT a live autonomous operator. AI mode is deterministic ("mock"/"sandbox") by default; a live LLM/AI provider is NEVER called this phase. Every suggestion is sanitized and stored with `provider_call_made=false` / `external_action_allowed=false` / `external_action_taken=false`; review is internal-only. **VPS production verification PASSED:** `/operations/ai-copilot` opened with the AI Copilot Center + sidebar AI Copilot + safety shell AI Paused / Sandbox OFF / Sync Live + AI Mode mock / Live Autonomous Locked / Live Provider unavailable / Provider disabled / Human Approval Required / Provider Call None; a suggestion was generated internally → pending review → approved internally, another rejected internally; external action allowed/taken + provider call stayed false; no live side effect; `pytest tests/test_phase16i_ai_copilot.py` → 19 passed `[100%]`; regression (16H/16G/16F/16E) → 67 passed `[100%]`; `makemigrations --check` + `manage.py check` clean; `GET /api/healthz/` → `{"status": "ok", "service": "nirogidhara-backend"}`. Verification: backend 155 passed (19 new), frontend 367/367 (+10 new), lint 0 errors, build green. **(Historical close-state: Phase 16J was next at 16I close — it has since shipped and is PRODUCTION VERIFIED + CLOSED at `aa8cf13`; the current next-planned work is Phase 16O.)** Phase 16H (`d733cf0`) is the previous verified baseline.
>
> **Phase 16H — Internal Pilot Execution Workbench + Role-Based Task Queues, PRODUCTION VERIFIED on the VPS and CLOSED at commit `d733cf0`.** Extends the internal-only `apps.pilot` app (2 additive models `PilotTask` / `PilotTaskEvent`, migration `pilot.0003_phase16h_pilot_execution`, 7 endpoints under `/api/v1/pilot/`, page `/operations/pilot-workbench`): converts an approved internal pilot plan into role-based internal task queues (Calling / Confirmation / Dispatch-Warehouse / Delivery-RTO / QA-Compliance / Finance), tracks task status (`todo → in_progress → done`, plus `blocked`/`unblock`/`skip`/`cancel`), and reports per-team execution progress. A transition changes ONLY the internal task record — `provider_actions_allowed` stays false and `provider_actions_blocked` stays true at every task status including `in_progress`/`done`. No live provider call, no business Celery enqueue, no safety-state change. **VPS production verification PASSED:** `/operations/pilot-workbench` opened with the Internal Pilot Execution Workbench title + sidebar item Pilot Workbench + safety shell AI Paused / Sandbox OFF / Sync Live / Live Provider Actions Locked + internal-control-only safety copy; pilot plan "Phase 16H workbench smoke pilot" created + approved; 14 internal tasks generated across the six team queues (0/14 done + per-team progress); task lifecycle Start→IN PROGRESS / Complete→DONE / Block→BLOCKED / Unblock→IN PROGRESS / Skip→SKIPPED; no live side effect; `makemigrations --check` → "No changes detected"; `manage.py check` → "0 issues"; `pytest tests/test_phase16h_pilot_execution.py --tb=no -q` → 19 passed `[100%]`; `GET /api/healthz/` → `{"status": "ok", "service": "nirogidhara-backend"}`. Verification: backend 136 passed (19 new), frontend 357/357 (+10 new), lint 0 errors, build green. **(Historical close-state: Phase 16I was next at 16H close — it has since shipped and is PRODUCTION VERIFIED + CLOSED at `0f91f6b`; the current next-planned work is Phase 16J.)** Phase 16G (`38e8dc8`) is the previous verified baseline.
>
> **Phase 16G — Internal Pilot Control Center / Pilot Execution Dashboard, PRODUCTION VERIFIED on the VPS and CLOSED at commit `38e8dc8`.** Extends the existing internal-only `apps.pilot` app (3 additive models `PilotPlan` / `PilotPlanEvent` / `PilotPlanReview`, migration `pilot.0002_phase16g_pilot_control`, 6 endpoints under `/api/v1/pilot/`, page `/operations/pilot-control`): a Director/Admin pilot-management + observability layer where the Director creates / configures / internally approves / starts / pauses / resumes / completes / cancels / reviews a controlled pilot plan via the internal status flow `draft → ready_for_review → approved_internal → running_internal → paused → completed | cancelled`. A transition changes ONLY the internal record — `provider_actions_allowed` stays false and `provider_actions_blocked` stays true at every state including `running_internal`. Reuses the Phase 16F readiness gate matrix + Phase 16E payment/logistics readiness for the per-plan gate checklist; no live provider call, no business Celery enqueue, no safety-state change. **VPS production verification PASSED:** `/operations/pilot-control` opened with the Internal Pilot Control Center title + sidebar item Pilot Control + safety shell AI Paused / Sandbox OFF / Sync Live / Live Provider Actions Locked; the status counters (Draft / Ready for Review / Approved Internal / Running Internal / Paused / Completed / Cancelled), Create pilot plan form, and Pilot plans panel all rendered; safety copy confirms internal control only / no live provider automation; no live side effect; `makemigrations --check` → "No changes detected"; `manage.py check` → "0 issues"; `pytest tests/test_phase16g_pilot_control.py --tb=no -q` → 19 passed `[100%]`; `GET /api/healthz/` → `{"status": "ok", "service": "nirogidhara-backend"}`. Verification: backend 117 passed (19 new), frontend 347/347 (+11 new), lint 0 errors, build green. **Next planned work is Phase 16H (NOT started; requires a separate written Director directive).** Phase 16F (`967ed3d`) is the previous verified baseline.
>
> **Phase 16F — Controlled Internal Pilot Readiness + End-to-End Dry Run, PRODUCTION VERIFIED on the VPS and CLOSED at commit `967ed3d`.** New internal-only app `apps.pilot` (2 models, migration `pilot.0001_initial`, 4 endpoints under `/api/v1/pilot/`, page `/operations/pilot-readiness`): an internal pilot/dry-run command center that reuses the Phase 16E `integration_hardening` readiness + reads settings/kill-switch/sandbox/Claim-Vault/team-roles to build a 12-gate readiness matrix; the Director runs an internal dry-run (records gate results + verdict, always live provider actions blocked) and a sign-off review (live-provider gate force-locked NOT approved). No live provider call, no business Celery enqueue, no safety-state change. **VPS production verification PASSED:** `/operations/pilot-readiness` opened with the Controlled Internal Pilot Readiness title + safety shell AI Paused / Sandbox OFF / Sync Live / Live Provider Actions Locked; the gate matrix showed Lead/Customer data PASS, Calling outcome flow PASS, Order creation PASS, Confirmation flow PASS, Payment readiness BLOCKED, Shipment readiness BLOCKED, Vapi/AI calling BLOCKED, Safety state PASS; an internal dry-run ran (toast "Internal dry-run recorded: blocked.") and the recent list showed 1 BLOCKED run (correct — live actions locked); the blocked-live-actions panel + sign-off checklist were visible; no live side effect; `migrate --noinput` → "No migrations to apply."; regression suite + targeted `tests/test_phase16f_pilot_readiness.py` → `[100%]`; `GET /api/healthz/` → `{"status": "ok", "service": "nirogidhara-backend"}`. An observed gate-matrix warning ("WhatsApp live automation blocked — WARNING" / "WhatsApp automation appears enabled — review before pilot.") is a risk to review before any future pilot, NOT a blocker. Phase 16E (Payment / Logistics Integration Hardening, `36395f6`), Phase 16D (Uploaded Data Campaigns, `c0be74a`), Phase 16C (Director Daily Briefing + Team Roles, `687ef41`), Phase 16B (Customer Lifecycle UI Backbone, `00c3295`) are earlier verified baselines. The Phase 15 safety shell remains frozen at code commit `eefd8b3` and unchanged through Phase 16A / 16B / 16C / 16D / 16E / 16F; current safety state AI Paused / Sandbox OFF / Sync Live. The canonical operational source of truth is [`../nd.md`](../nd.md) head-of-file (with [`PHASE_15M_DIRECTOR_SIGNOFF_PACK.md`](PHASE_15M_DIRECTOR_SIGNOFF_PACK.md) for the safety-shell freeze details). Phase 16A audit → 16B → 16C (`687ef41`) → 16D (`c0be74a`) → 16E (`36395f6`) → 16F (pilot readiness, `967ed3d`) → 16G (pilot control, `38e8dc8`) → 16H (pilot workbench, `d733cf0`) → 16I (AI Copilot, `0f91f6b`) → 16J (AI action queue, `aa8cf13`) → 16K (department workboard, `efea751`) → 16L (scoped work permissions, `9d144f5`). **The current operational baseline is Phase 16L (the top note above, PRODUCTION VERIFIED + CLOSED at `9d144f5`) on top of Phase 16K (PRODUCTION VERIFIED + CLOSED at `efea751`); the next planned work is Phase 16O (NOT started; requires a separate written Director directive); Phase 16N is implemented and pushed to origin/main (VPS verification pending).** Historical docs below remain historical; `nd.md` wins for current truth.

| File | Purpose |
| --- | --- |
| [`PHASE_15M_DIRECTOR_SIGNOFF_PACK.md`](PHASE_15M_DIRECTOR_SIGNOFF_PACK.md) | **Current release-freeze / Director sign-off pack** (Phase 15M). Verified baseline, freeze rule, route-wise smoke checklist, sign-off checklist, accepted risks, production safety posture, Phase 16A handoff, rollback plan. |
| [`MASTER_BLUEPRINT_V2.md`](MASTER_BLUEPRINT_V2.md) | **Historical / strategic blueprint mirror** (Master Blueprint v2.0). Reflects strategic framing as of its original revision; contains Phase 12D-era wording. Strategic / historical reference only unless updated later. **Current truth is [`../nd.md`](../nd.md).** Supersedes the v1.0 PDF (also historical). |
| [`RUNBOOK.md`](RUNBOOK.md) | **Operational runbook**, updated through Phase 16L (My Work queue + scoped permissions — production verified at `9d144f5`) + Phase 16K (Department action workboard — production verified at `efea751`) + Phase 16J (AI action queue — production verified at `aa8cf13`) + Phase 16I (AI Copilot section) + Phase 16H (Pilot Workbench) + Phase 16G (Pilot Control) + Phase 16F (Pilot Readiness) + Phase 16E (Payment & Logistics hardening) + Phase 16D (Data Imports + Imported Campaigns) + Phase 16C (Director Briefing + Team Roles) on top of Phase 16B production verification (`00c3295`). Local dev steps, Director playbooks, state semantics, Foundation Release Freeze policy, Phase 16B/16C/16D/16E/16F/16G/16H/16I/16J/16K/16L verification checklists. |
| [`DEPLOYMENT_VPS.md`](DEPLOYMENT_VPS.md) | Production deployment runbook for `ai.nirogidhara.com` (`/opt/nirogidhara-command`). |
| [`BACKEND_API.md`](BACKEND_API.md) | API endpoint reference (Phase 12D-era surfaces + Phase 15B/15C read-only additions + **Phase 16B semantics** + **Phase 16C** `/api/v1/director-ops/` + **Phase 16D** `/api/v1/imports/` + **Phase 16E** `/api/v1/integrations/payment-logistics/` + **Phase 16F** `/api/v1/pilot/` readiness + dry-run + **Phase 16G** `/api/v1/pilot/` control-plan + **Phase 16H** `/api/v1/pilot/` task-queue + **Phase 16I** `/api/v1/ai-copilot/` suggestion + review + **Phase 16J** `/api/v1/ai-copilot/actions...` internal-action-queue + **Phase 16K** `/api/v1/ai-copilot/workboard...` + `actions/<id>/{assign,claim,start,block,unblock,complete-internal,reassign,notes}/` department-workboard + **Phase 16L** `/api/v1/ai-copilot/workboard/my...` + `workboard/department-members...` scoped-permission + My Work endpoints). |
| [`FRONTEND_AUDIT.md`](FRONTEND_AUDIT.md) | Historical frontend audit snapshot (Phase 12D-era). Current UI baseline is **Phase 16L — Scoped Team Member Work Permissions + My Work Queue (PRODUCTION VERIFIED + CLOSED at `9d144f5`)** on top of **Phase 16K — Department Action Workboard (PRODUCTION VERIFIED + CLOSED at `efea751`)**. |
| [`FUTURE_BACKEND_PLAN.md`](FUTURE_BACKEND_PLAN.md) | Historical phased roadmap (Phase 12D-era SaaS runtime gate status preserved). **Current baseline is Phase 16L — Scoped Team Member Work Permissions + My Work Queue (PRODUCTION VERIFIED + CLOSED at `9d144f5`); next planned is Phase 16M** (separate Director directive required). |
| [`PHASE_16A_BUSINESS_MVP_GAP_AUDIT.md`](PHASE_16A_BUSINESS_MVP_GAP_AUDIT.md) | **Phase 16A — Business MVP Gap Audit** (docs-only / read-only). Carries a Phase 16B production-verified follow-up note at its head (`00c3295`); the audit body is a point-in-time snapshot. `nd.md` wins for current truth. |
| [`WHATSAPP_INTEGRATION_PLAN.md`](WHATSAPP_INTEGRATION_PLAN.md) | **Historical WhatsApp + AI Chat Sales Agent design plan** (Phase 5A-0 → 5C + Phase 7E-Live-B + Phase 12C and beyond). Live WhatsApp / broadcast / campaign / lifecycle / handoff / rescue / Day-20 flows all remain locked OFF unless the Director explicitly approves. |

The repo-level [`nd.md`](../nd.md) is the **canonical operational source of truth**. If `nd.md` and any document here disagree on a detail, `nd.md` wins and the document must be updated to match.

The repo-level [`CLAUDE.md`](../CLAUDE.md) / [`AGENTS.md`](../AGENTS.md) are AI-agent guardrails — read on every session.
