# CLAUDE.md — Nirogidhara AI Command Center

> Auto-loaded by Claude Code on every session in this repo.
> If you are an AI agent working here, **read this file first**, then `AGENTS.md` for guardrails and `nd.md` for the full project handoff.

---

## 0. Project context (60-second read)

**Phase 8F Live Execute + Rollback status update:** On 2026-05-14, Phase 8F live execute was run on the VPS for the first time and then rolled back as Reading 1 mechanism proof. Execute used `attempt_id=1`, `gate_id=1`, `source_phase8e_gate_id=1`, target Order `NRG-20435`, target Payment `PAY-30125`, operator `Prarit Sidana`, and Director signoff window `BEGIN_UTC=2026-05-14T09:32:29Z` to `END_UTC=2026-05-14T09:45:29Z`. Execute returned `ok=True`, `status=executed`; Order `NRG-20435.payment_status` changed to `Paid`, and Payment `PAY-30125.status` changed to `Paid`. All locked-False flags (`customer_notification_sent`, `whatsapp_sent`, `courier_called`, `provider_call_attempted`, `shipment_created`) remained False; no Razorpay / Meta Cloud / Delhivery / Vapi call, no WhatsApp send, no customer notification, no shipment/AWB, and no `Order.state` mutation occurred. Rollback Reading 1 returned `ok=True`, `status=rollback_recorded`, `rollbackId=1`; psql confirmed Order `NRG-20435.payment_status` restored to `Partial` and Payment `PAY-30125.status` restored to `Pending`; health endpoint returned `{"status": "ok"}`. Multiple placeholder-signoff execute attempts before the final run were correctly refused by the guard (`phase8f_director_signoff_missing_structured_utc_window`, `phase8f_now_outside_director_signoff_utc_window_before_start`), and Hotfix-3 recovery restored `attempt.status` between refused runs. The final successful execute used shell variable expansion (`BEGIN=$(date -u -d "-1 minute")`, `END=$(date -u -d "+12 minutes")`) to compute and embed the UTC window. The three `PHASE8F_*` flags were passed via runtime env prefix only; `.env.production` was NOT edited. **Phase 8F is complete as executed + rolled back; system state is back to Partial/Pending. Phase 7E-Live-B and Phase 7G-Live remain NOT approved.** Test baseline remains **2200 backend tests + 82 frontend tests**; this docs record has no code change.

**Phase 8F-Hotfix-3 status update:** Phase 8F-Hotfix-3 - Attempt-level recovery command for blocked Phase 8F attempt - is **shipped as a governance/admin command only**. Root cause: a prior Phase 8F execute run used placeholder Director signoff (`<FILL>`), failed the structured UTC window precheck before the atomic mutation block, and set attempt id=1 to `blocked`; because execute refuses any attempt not in `approved_for_one_shot_real_mutation`, future correct signoffs would still hit `phase8f_attempt_status_blocked_not_executable`. The fix adds `python manage.py recover_phase8f_attempt_to_approved --attempt-id <ID> --director-signoff "phase8f_attempt_id_<ID> phase8f_gate_id_<ID> phase8fHotfix3AttemptRecovery" --operator-name "..." --confirm-phase8f-attempt-recovery`, which requires `PHASE8F_REAL_CUSTOMER_CONTROLLED_MUTATION_GATE_ENABLED=true`, runtime kill switch enabled, a blocked attempt, an approved Phase 8F gate, no executed attempt on the same gate, non-empty operator name, explicit confirmation, and all three signoff markers. On success it only transitions `attempt.status` from `blocked` back to `approved_for_one_shot_real_mutation` and appends `phase8fHotfix3Recovery_recovered_from_blocked` to `attempt.blockers`, then writes a `phase8f.real_mutation.approved` audit event. It does **not** require a UTC window because it is not a mutation execute command. Hotfix-3 was used during the Phase 8F pre-execute journey to restore `attempt.status` between correctly refused placeholder-signoff runs; no mutation occurred on any refused attempt.

**Test Hygiene Hotfix-1 status update:** Test Hygiene Hotfix-1 — Pin integration modes in `backend/tests/conftest.py` for VPS-safe full-suite runs — is **shipped (test-only fix; no production code, model, migration, service, view, env flag, .env file, or frontend touched).** When the VPS first ran the full `pytest -q` the suite landed at ~43 failures (reproduced locally as **49 failures** by exporting `RAZORPAY_MODE=test WHATSAPP_PROVIDER=meta_cloud WHATSAPP_LIVE_META_LIMITED_TEST_MODE=true` and running `pytest -q`). Root cause: tests that don't carry their own `override_settings` inherit `.env.production`'s non-mock integration values — real Razorpay TEST URLs (`rzp.io`) instead of the mock placeholder, real Meta Cloud signature verification rejecting the test fixtures' app-secret HMAC, and the Phase 5F-Gate final-send limited-test-mode allow-list guard refusing every outbound to phones not on `WHATSAPP_LIVE_META_ALLOWED_TEST_NUMBERS` (empty on the VPS at suite time). None of these were product defects — purely test-isolation leaks from real `.env.production` values flowing through. The fix extends the existing `_force_eager_celery` session-autouse fixture in `backend/tests/conftest.py` to also pin `RAZORPAY_MODE="mock"`, `WHATSAPP_PROVIDER="mock"`, `WHATSAPP_LIVE_META_LIMITED_TEST_MODE=False`, `DELHIVERY_MODE="mock"`, `VAPI_MODE="mock"`, `META_MODE="mock"` for the whole test session via `django.test.utils.override_settings`. Tests that intentionally need a non-mock value already carry their own `override_settings`, which wins over the session pin inside that test — none of them were touched. After the fix: the same faked-`.env.production` command (`RAZORPAY_MODE=test WHATSAPP_PROVIDER=meta_cloud WHATSAPP_LIVE_META_LIMITED_TEST_MODE=true python -m pytest -q`) is **all green at 2188 passed**, and the normal `python -m pytest -q` stays at 2188 passed. **Test Hygiene Hotfix-1 did NOT modify any production code, model, migration, service, view, env flag, `.env*` file, or frontend; did NOT execute or roll back anything; did NOT call Razorpay / Meta Cloud / Delhivery / Vapi; did NOT send or queue WhatsApp; did NOT send a customer notification; did NOT mutate any business row.** **`.env.production` deviation flagged for Director reconciliation** (nd.md §17 still lists "stays mock-mode in production" for several providers, but the VPS `.env.production` actually has `RAZORPAY_MODE=test`, `WHATSAPP_PROVIDER=meta_cloud`, `WHATSAPP_LIVE_META_LIMITED_TEST_MODE=true`; the WhatsApp pair appears intentional for the limited live test cohort, but `RAZORPAY_MODE=test` must be flipped to `live` before any real customer payment collection goes live). **Do NOT edit `.env.production` without Director sign-off.** Verification baseline unchanged at **2188 backend tests + 82 frontend tests** — the suite is now deterministic on BOTH SQLite (local) and Postgres (VPS) after the hotfix. Phase 8F-Hotfix-1 recovery approval was run on the VPS on 2026-05-14 (Phase 8F gate id=1 now `approved_for_one_shot_real_customer_mutation`, attempt id=1 minted with every locked-False flag still False + `executed_at=NULL`); At the Test Hygiene Hotfix-1 checkpoint, Phase 8F EXECUTE had not yet run; Order `NRG-20435` was still `Partial`, Payment `PAY-30125` was still `Pending`. Phase 8F-Hotfix-2 made the Phase 8F tests Postgres-safe (40/40 on VPS).

**Phase 8F-Hotfix-2 status update:** Phase 8F-Hotfix-2 — Make Phase 8F execute/rollback tests Postgres-safe — is **shipped (test-only fix; no production code touched).** On the VPS (Postgres) the full-file run of `tests/test_phase8f_real_customer_controlled_mutation.py` saw `test_phase8f_execute_happy_path_mutates_only_target_statuses` and `test_phase8f_rollback_restores_old_statuses_no_side_effect` return `ok:False` because the helper `_prep_approve(...)` created a Phase 8E gate but did not return its `pk`, and the test then hardcoded `phase8e_gate_id=1` (and the literal `phase8e_gate_id_1` in inline signoff strings). Postgres `ROLLBACK` does **not** reset `AUTOINCREMENT` sequences between tests in the same file run, so the real Phase 8E gate id is rarely 1 — the execute path's literal-ref check (`phase8e_gate_id_<ID>` must appear in the Director sign-off body) then refused with `phase8f_director_signoff_must_reference_phase8e_gate_id`. SQLite effectively resets the sequence so the bug was invisible locally. The fix patches only `backend/tests/test_phase8f_real_customer_controlled_mutation.py` — `_prep_approve` now returns a 5-tuple `(attempt_id, gate_id, phase8e_gate_pk, order, payment)`; every caller unpacks the new `phase8e_gate_id` value; the two happy-path tests pass the dynamic `phase8e_gate_id` into `_structured_signoff`; every refusal test replaces inline `phase8e_gate_id_1` fragments with `f"phase8e_gate_id_{phase8e_gate_id}"` using the dynamic value. Test intent, counts, and assertion logic are unchanged. **Phase 8F-Hotfix-2 did NOT modify any production code, model, migration, service, env flag, or API; did NOT execute or roll back Phase 8F; did NOT call Razorpay / Meta Cloud / Delhivery / Vapi; did NOT send or queue WhatsApp; did NOT send a customer notification; did NOT create a `Shipment` / AWB / payment link; did NOT mutate any `Order` / `Payment` / `Order.state` / `Customer` / `Lead` / `Shipment` / `DiscountOfferLog` / `WhatsAppMessage` row; did NOT edit any `.env*` file.** Phase 8F test count unchanged at 40 passed; verification baseline unchanged at **2188 backend tests + 82 frontend tests**, with `makemigrations --check --dry-run` clean, `manage.py check` clean, frontend lint/tests/build green. **Phase 8F-Hotfix-1 correction:** the Phase 8F-Hotfix-1 recovery-approval CLI WAS run on the VPS on 2026-05-14 — Phase 8F gate id=1 is now `status="approved_for_one_shot_real_customer_mutation"`, attempt id=1 was minted (`status="approved_for_one_shot_real_mutation"`, every locked-False flag still False, `executed_at=NULL`), `gate.evidence_json` carries the sealed `phase8fHotfix1Recovery` block. **At the Phase 8F-Hotfix-2 checkpoint, Phase 8F EXECUTE had not yet run on the VPS** — `Order.NRG-20435` is still `payment_status="Partial"`; `Payment.PAY-30125` is still `status="Pending"`. Phase 8F live execute on the VPS still requires a separate Director directive AND all three Phase 8F env flags flipped true AND a 15-min structured UTC window referencing the actual Phase 8F gate id 1 / attempt id 1 / Phase 8E gate id 1 / target Order NRG-20435 / target Payment PAY-30125. Phase 7E-Live-B (real customer WhatsApp send) and Phase 7G-Live (real customer courier execution) remain **NOT approved**.

**Phase 8F-Hotfix-1 status update:** Phase 8F-Hotfix-1 — Recover blocked approval gate + sync migration drift — is **shipped (approval-only recovery; no execute; never run on the VPS).** Phase 8F on the VPS landed in `blocked` after a first approve attempt (`gate.id=1`, status `blocked`, `blockers=["PHASE8F_REAL_CUSTOMER_CONTROLLED_MUTATION_GATE_ENABLED_must_be_true"]`, candidate Order `NRG-20435` Partial + Payment `PAY-30125` Pending unchanged); a retry after flipping the env flag failed with `phase8f_gate_status_blocked_not_transitionable_to_approved`, and `makemigrations --check --dry-run` reported a pending Phase 8F index-rename migration. The hotfix has two parts: (a) new migration `payments.0025_phase8f_hotfix_rename_indexes` — pure `RenameIndex` ×8 mapping the 0024 auto-generated `payments_ra_*` suffix names to stable, explicit, ≤30-char canonical names (`p8f_gate_status_created_idx`, `p8f_gate_src8e_created_idx`, `p8f_gate_target_pair_idx`, `p8f_atmpt_gate_created_idx`, `p8f_atmpt_status_created_idx`, `p8f_atmpt_target_pair_idx`, `p8f_rb_attempt_created_idx`, `p8f_rb_status_created_idx`); the three Phase 8F models' `Meta.indexes` now carry explicit `name=` arguments so the names are version-stable across Django patch releases. (b) `apps.payments.phase8f_real_customer_controlled_mutation.approve_phase8f_real_customer_controlled_mutation` is patched with a **narrowly-scoped recovery path** — a Phase 8F gate may now be promoted from `blocked` to `approved_for_one_shot_real_customer_mutation` ONLY when **all** of the following hold simultaneously: gate `status="blocked"` AND `gate.blockers` set is EXACTLY `{"PHASE8F_REAL_CUSTOMER_CONTROLLED_MUTATION_GATE_ENABLED_must_be_true"}` (nothing else) AND `PHASE8F_REAL_CUSTOMER_CONTROLLED_MUTATION_GATE_ENABLED=true` at runtime AND no attempt exists on the gate with `executed_at` set or any `order_mutation_was_made` / `payment_mutation_was_made` / `business_mutation_was_made` / `customer_notification_sent` / `whatsapp_sent` / `courier_called` / `provider_call_attempted` / `shipment_created` flag True AND current `Order.payment_status ∈ {Pending, Partial}` AND `Payment.status=Pending` AND `Payment.order_id==Order.id` AND Phase 8E source gate still `approved_for_future_phase8f_real_customer_controlled_mutation` AND the runtime kill switch is enabled AND Phase 7E-Live-B / 7G-Live / broad customer automation remain not-approved (the existing `_eligibility_blockers` check still runs end-to-end at approve time). When the recovery path fires, `gate.evidence_json` is stamped with a sealed `phase8fHotfix1Recovery` block — `recoveredFromMissingEnvApprovalBlock=true`, `recoveredBlocker="PHASE8F_REAL_CUSTOMER_CONTROLLED_MUTATION_GATE_ENABLED_must_be_true"`, `executionStillNotRun=true`, `recoveredAtUtc=<ISO-Z>`, `phase8fHotfix1=true` — `gate.blockers` is cleared, `gate.warnings` gains `"phase8f_hotfix1_recovered_from_missing_env_flag"`, and the `phase8f.real_mutation.approved` audit row carries `phase8f_hotfix1_recovered_from_missing_env_flag=true` + `phase8f_hotfix1_execution_still_not_run=true`. The canonical refusal (`phase8f_gate_status_blocked_not_transitionable_to_approved`) is preserved for every gate that does NOT satisfy the narrow recovery contract — additional blockers, drifted Order/Payment status, any executed/mutation/provider-flag attempt on the gate, or env flag still off all keep the gate `blocked`. **Phase 8F-Hotfix-1 did NOT execute Phase 8F, did NOT roll back Phase 8F, did NOT call Razorpay / Meta Cloud / Delhivery / Vapi (asserted with the existing static-file import scan; module imports unchanged), did NOT send or queue WhatsApp, did NOT send a customer notification, did NOT create a `Shipment` / AWB / payment link, did NOT capture or refund, did NOT mutate `Order.payment_status` / `Order.state` / `Payment.status` / `Customer` / `Lead` / `Shipment` / `DiscountOfferLog` / `WhatsAppMessage` (asserted with `_row_counts()` before/after parity across the recovery approval), did NOT edit any `.env*` file.** No new audit kind, no new model, no new CLI command, no new API endpoint — surgical service-layer patch + migration only. 8 net new backend tests (recovery succeeds from blocked with only env-flag blocker + env flag on; refuses when blocker set carries any other entry; refuses when Order status drifted to Paid; refuses when any attempt has `executed_at` set; refuses when any attempt has `*_mutation_was_made=True` even without `executed_at`; refuses when any attempt has `provider_call_attempted=True`; refuses when env flag is still off; recovery path keeps every locked-False flag on the new attempt False and leaves Order/Payment statuses untouched). Full Phase 8F test file remains green at 40 passed. Verification baseline now **2188 backend tests + 82 frontend tests**, with `makemigrations --check --dry-run` clean (`No changes detected`), `manage.py check` clean, frontend lint, frontend tests, and frontend build green. **Before Reading 1, Phase 8F live execute on the VPS still required a separate Director directive AND all three Phase 8F env flags flipped true AND a 15-min structured UTC window referencing the actual Phase 8F gate id / attempt id / Phase 8E gate id 1 / target Order NRG-20435 / target Payment PAY-30125 — and a ready rollback command. Phase 7E-Live-B (real customer WhatsApp send) and Phase 7G-Live (real customer courier execution) remain NOT approved.**

**Phase 8F status update:** Phase 8F — Controlled Real Customer Payment → Order Mutation Gate framework — was implemented; the later 2026-05-14 Reading 1 live execute + rollback is recorded in the current status block above. New `payments.RazorpayRealCustomerPaymentOrderControlledMutationGate` + `payments.RazorpayRealCustomerPaymentOrderControlledMutationAttempt` + `payments.RazorpayRealCustomerPaymentOrderControlledMutationRollback` models + migration `payments.0024_phase8f_real_customer_controlled_mutation` (pure CreateModel; no `RunPython`). New service `apps.payments.phase8f_real_customer_controlled_mutation` ships preview / prepare / approve / execute / rollback / reject / archive lifecycle helpers + the defensive `assert_phase8f_no_unauthorized_side_effect` guard (8 gate-level locked-False booleans — `real_customer_mutation_allowed`, `customer_notification_allowed`, `whatsapp_allowed`, `courier_allowed`, `provider_call_allowed`, `shipment_creation_allowed`, `payment_capture_allowed`, `refund_allowed`; 5 attempt-level locked-False booleans — `customer_notification_sent`, `whatsapp_sent`, `courier_called`, `provider_call_attempted`, `shipment_created`; 4 rollback-level locked-False booleans — `customer_notification_sent`, `whatsapp_sent`, `courier_called`, `provider_call_attempted`; full business-row count delta over `Order` / `Payment` / `Shipment` / `DiscountOfferLog` / `Customer` / `Lead` / `WhatsAppMessage` / `WhatsAppLifecycleEvent` / `WhatsAppHandoffToCall`). Eligibility refuses unless source Phase 8E gate is `status=approved_for_future_phase8f_real_customer_controlled_mutation` AND `dry_run_passed=True` AND candidate `Order.id` / `Payment.id` snapshots are present AND the current `Order.payment_status ∈ {Pending, Partial}` AND `Payment.status=Pending` AND `Payment.order_id==Order.id` AND `PHASE8F_REAL_CUSTOMER_CONTROLLED_MUTATION_GATE_ENABLED=true` AND the runtime kill switch is enabled AND Phase 7E-Live-B / 7G-Live / broad customer automation remain not-approved. Execute additionally requires **three** Phase 8F env flags ALL true (`PHASE8F_REAL_CUSTOMER_CONTROLLED_MUTATION_GATE_ENABLED`, `PHASE8F_DIRECTOR_APPROVED_ONE_SHOT_REAL_MUTATION`, `PHASE8F_ALLOW_REAL_CUSTOMER_ORDER_PAYMENT_MUTATION`) + a structured 15-min Director sign-off UTC window (via `apps.saas.utc_window.validate_within_director_window`) + `--confirm-one-shot-real-mutation` + non-empty `--operator-name` + Director sign-off body must literally reference `phase8f_attempt_id_<ID>` AND `phase8f_gate_id_<ID>` AND `phase8e_gate_id_<ID>` AND `target_order_<ORDER_ID>` AND `target_payment_<PAYMENT_ID>` + the current `Order.payment_status` / `Payment.status` must still match the approved snapshot + no prior `executed` attempt on the gate. Execute mutates ONLY `Order.payment_status="Paid"` and `Payment.status="Paid"` on the named target rows; `Order.state` is NEVER mutated. 8 strictly-CLI management commands (`inspect_phase8f_real_customer_controlled_mutation`, `preview_…_mutation --phase8e-gate-id`, `prepare_…_mutation --phase8e-gate-id`, `approve_…_mutation --gate-id --reason` (require_reason=True; mints a matching attempt in `approved_for_one_shot_real_mutation` status), `execute_…_mutation --attempt-id --director-signoff --operator-name --confirm-one-shot-real-mutation` (CLI-only one-shot; refuses on missing flags / non-structured signoff / stale or out-of-window / drifted current statuses / second-run on same gate), `rollback_…_mutation --attempt-id --reason` (record-only restore of `old_*` snapshots; refuses unless attempt is `executed`), `reject_…_mutation --gate-id --reason` (require_reason=True), `archive_…_mutation --gate-id --reason` (require_reason=True)). 6 read-only auth-protected GET DRF endpoints under `/api/v1/saas/phase8/real-customer-controlled-mutation-{readiness,gates,gates/<pk>,preview,attempts/<gate_id>,rollbacks/<attempt_id>}/`; POST/PATCH/DELETE return 405; **no POST endpoint dispatches state changes or execute** (review + execute are CLI-only). 10 audit kinds (each ≤ 64 chars): `phase8f.real_mutation.{readiness_inspected,previewed,prepared,approved,executed,rollback_recorded,rejected,archived,blocked,failed}`. Audit payloads NEVER carry tokens / phones / addresses / raw secrets / raw provider response / full `gateway_reference_id` / full `customer_name` / Director sign-off text (only a 64-char SHA-256 hash of the sign-off is persisted on the attempt row; scrubbed via `_safe_audit_payload` against a 30-entry `PHASE_8F_FORBIDDEN_PAYLOAD_KEYS` tuple). Frontend `/saas-admin` gains a read-only "Phase 8F Controlled Real Customer Payment → Order Mutation" section (`data-testid="phase8f-real-customer-controlled-mutation-section"`). **Phase 8F never calls Razorpay / Meta Cloud / Delhivery / Vapi (asserted with static-file scan that checks actual import lines, not docstring mentions), never sends or queues WhatsApp, never creates a `Shipment` / `WorkflowStep` / `RescueAttempt` / AWB / payment link, never captures, never refunds, never sends a customer notification, never mutates real `Customer` / `Lead` / `Shipment` / `DiscountOfferLog` / `WhatsAppMessage` / `WhatsAppLifecycleEvent` / `WhatsAppHandoffToCall` rows, never mutates `Order.state` (asserted with `_row_counts()` before/after parity across the full prepare → approve → execute → rollback lifecycle and an explicit `Order.state` snapshot in the happy-path test), never edits any `.env*` file.** Defaults LOCKED: `PHASE8F_REAL_CUSTOMER_CONTROLLED_MUTATION_GATE_ENABLED=false`, `PHASE8F_DIRECTOR_APPROVED_ONE_SHOT_REAL_MUTATION=false`, `PHASE8F_ALLOW_REAL_CUSTOMER_ORDER_PAYMENT_MUTATION=false`. **During the original Phase 8F framework implementation turn, no execute command was run on the VPS** — the happy-path execute + rollback tests exercise the test database fixtures exclusively (Partial+Pending real-customer pair built on top of the Phase 7I → 8A → 8B → 8C executed+rolled_back → 8D locked → 8E approved chain). 32 new backend tests + 1 new frontend vitest case; verification baseline now **2180 backend tests + 82 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Before Reading 1, Phase 8F live execute on the VPS still required a separate Director directive AND all three Phase 8F env flags flipped true AND a 15-min structured UTC window referencing the actual Phase 8F gate / attempt / Phase 8E gate id 1 / Order NRG-20435 / Payment PAY-30125. Phase 7E-Live-B (real customer WhatsApp send) and Phase 7G-Live (real customer courier execution) remain NOT approved.**

**Phase 8E-Hotfix-1 status update:** Phase 8E-Hotfix-1 — Candidate pool inspector + accept `Partial` + `Pending` review-only candidate — is **shipped**. The Phase 8E candidate validator now accepts `Order.payment_status ∈ {"Pending", "Partial"}` (was `"Pending"` only) when `Payment.status="Pending"` AND the Order stage is non-terminal AND the row pair carries no Phase 8C sandbox markers AND `Payment.order_id == Order.id`; when `Order.payment_status="Partial"` the candidate carries an explicit `phase8e_candidate_partial_order_pending_payment_review_only` warning and `candidate_validation_passed=True`. The on-VPS strict-Pending/Pending query returned 0 eligible candidates; the live pool contains 6 `Partial`+`Pending` real-customer pairs (`Confirmed` / `Interested` / `Order Punched` / `Out for Delivery` stages, masked phones `1203 / 5185 / 0524 / 5864 / 4506 / 9166`, masked names like `D**** B*****` / `G**** K*****`). New `apps.payments.phase8e_real_customer_payment_order_pilot.inspect_phase8e_real_customer_candidate_pool(limit=50, include_blocked=False)` is a pure read-only classifier — every Order + Payment row pair currently in the system is bucketed into `strict_pending_pending` / `partial_pending_review_only` / `blocked_terminal_stage` / `blocked_payment_not_pending` / `blocked_order_status_not_pending_or_partial` / `blocked_order_payment_mismatch` / `blocked_phase8c_sandbox` / `blocked_missing_required_data`; phones are masked to last-4, names are masked to first-letter-of-each-word, raw `Payment.raw_response` / full `gateway_reference_id` / full payment URLs / Razorpay payload NEVER appear in the response (`payment_reference` is truncated to first 8 chars as `paymentReferencePrefix`). New CLI command `python manage.py inspect_phase8e_real_customer_candidate_pool [--limit N] [--include-blocked] [--json] [--no-audit]` (the only path that writes the new audit kind). 1 new audit kind: `phase8e.pilot.pool_inspected` (28 chars, ≤ 64 char budget). 1 new read-only auth-protected GET DRF endpoint: `/api/v1/saas/phase8/real-customer-payment-order-pilot-candidate-pool/?limit=N&include_blocked=true|false`; POST/PATCH/DELETE return 405. Frontend `/saas-admin` Phase 8E section gains a "Phase 8E-Hotfix-1 Candidate Pool" sub-section (`data-testid="phase8e-candidate-pool-subsection"`) showing the total linked pairs, strict / partial / recommended counts, blocked counts by reason, and the top recommended masked candidate; no "Run pool inspector" / "Select candidate" / "Approve candidate" / "Execute candidate" / "Prepare candidate" / "Dry-run candidate" / "Mark Partial as Paid" / "Send WhatsApp" / "Notify customer" / "Capture" / "Refund" buttons exist anywhere on the UI. **Phase 8E-Hotfix-1 NEVER mutates `Order.payment_status` / `Order.state` / `Payment.status` / `Customer` / `Lead` / `Shipment` / `DiscountOfferLog` / `WhatsAppMessage` (asserted with `_row_counts()` before/after parity across the full prepare → select → dry-run → approve lifecycle on a Partial+Pending candidate), NEVER calls Razorpay / Meta Cloud / Delhivery / Vapi, NEVER sends or queues WhatsApp, NEVER creates a `Shipment` / AWB / payment link, NEVER captures / refunds, NEVER sends a customer notification, NEVER imports the Razorpay / Meta Cloud / Delhivery / WhatsApp-send / `dotenv` modules (asserted with static-file scan on actual import lines), NEVER edits any `.env*` file.** Approval still only flips status to `approved_for_future_phase8f_real_customer_controlled_mutation` — accepting a `Partial`+`Pending` candidate is review-only and does NOT authorise any mutation. 9 new backend tests + 1 new frontend vitest case (the `phase8e-candidate-pool-subsection` renders + the new api method is exposed + no forbidden buttons exist). Verification baseline now **2148 backend tests + 81 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Phase 8F (real customer controlled mutation), Phase 7E-Live-B (real customer WhatsApp send), and Phase 7G-Live (real customer courier execution) all remain NOT approved.**

**Phase 8E status update:** Phase 8E — Real Customer Payment → Order Mutation Pilot Gate — is shipped (review / dry-run only; CLI-only review state changes; **never run on the VPS**). New `payments.RazorpayRealCustomerPaymentOrderMutationPilotGate` + `payments.RazorpayRealCustomerPaymentOrderMutationCandidate` + `payments.RazorpayRealCustomerPaymentOrderMutationPilotDryRun` models + migration `payments.0023_phase8e_real_customer_payment_order_pilot` (pure CreateModel; no `RunPython`). New service `apps.payments.phase8e_real_customer_payment_order_pilot` ships preview / prepare / select-candidate / dry-run / approve / reject / archive lifecycle helpers + the defensive `assert_phase8e_no_business_mutation` guard (7 gate-level locked-False booleans + 3 candidate-level locked-False booleans + 7 dry-run-level locked-False booleans + full business-row count delta over `Order` / `Payment` / `Shipment` / `DiscountOfferLog` / `Customer` / `Lead` / `WhatsAppMessage` / `WhatsAppLifecycleEvent` / `WhatsAppHandoffToCall` / `WorkflowStep` / `RescueAttempt`). Eligibility refuses unless the source Phase 8D evidence lock is `status=locked` AND `final_db_restored_snapshot=True` AND `phase8c_gate_status_snapshot="rolled_back"` AND the runtime kill switch is enabled AND Phase 7E-Live-B / 7G-Live remain not-approved. Candidate selection requires `Order.id` + `Payment.id` where `Payment.order_id == Order.id`, the row pair carries **no** Phase 8C sandbox markers (Phase 8C `phase8c::controlled::` / `phase8c-controlled-` / `internal-test` / `sandbox` substrings, OR `confirmation_checklist.phase8c_sandbox=True`, OR `Payment.raw_response.phase8c_sandbox=True` are all explicitly rejected), the Order stage is not terminal (`DELIVERED` / `RTO` / `CANCELLED` rejected), and both `Order.payment_status` AND `Payment.status` are still `Pending`. **PII masking**: the candidate row stores ONLY `order_customer_name_masked` (first-letter-of-each-word + asterisks, e.g. `"P***** S*****"`) + `order_phone_last4` (last 4 digits only) + `payment_reference[:120]`; the serializer truncates `payment_reference` to its first 8 chars when surfacing it as `paymentReferencePrefix`, and `_safe_audit_payload` scrubs a **31-entry forbidden-keys** tuple (`token`, `phone`, `customer_phone`, `email`, `customer_email`, `address`, `card`, `vpa`, `upi`, `raw_response`, `raw_payload`, `gateway_reference_id`, `payment_url`, `customer_name`, `META_WA_TOKEN`, `RAZORPAY_KEY_SECRET`, …). 8 strictly-CLI management commands (`inspect_phase8e_real_customer_payment_order_pilot`, `preview_…` (`--phase8d-lock-id`), `prepare_…` (`--phase8d-lock-id`), `select_phase8e_real_customer_candidate` (`--gate-id --order-id --payment-id`), `dry_run_…` (`--gate-id --candidate-id`), `approve_…` (`--gate-id --reason` required; refuses unless `gate.status=dry_run_passed` AND at least one passed dry-run AND at least one validated candidate; flips status to `approved_for_future_phase8f_real_customer_controlled_mutation` only — does **NOT** enable any real mutation), `reject_…` (`--gate-id --reason` required), `archive_…` (`--gate-id --reason` required)). **No execute command exists.** 6 read-only auth-protected GET DRF endpoints under `/api/v1/saas/phase8/real-customer-payment-order-pilot-{readiness,gates,gates/<pk>,preview,candidates/<gate_id>,dry-runs/<gate_id>}/`; POST/PATCH/DELETE return 405; **no POST endpoint dispatches state changes** (review is CLI-only). 10 audit kinds (each ≤ 64 chars): `phase8e.pilot.{readiness_inspected,previewed,prepared,candidate_selected,dry_run_passed,dry_run_failed,approved,rejected,archived,blocked}`. Frontend `/saas-admin` gains a read-only "Phase 8E Real Customer Payment → Order Pilot" section (`data-testid="phase8e-real-customer-payment-order-pilot-section"`). **Phase 8E never calls Razorpay / Meta Cloud / Delhivery / Vapi, never sends or queues WhatsApp, never creates a `Shipment` / `WorkflowStep` / `RescueAttempt` / AWB / payment link, never captures, never refunds, never sends a customer notification, never mutates real `Order` / `Payment` / `Customer` / `Lead` / `Shipment` / `DiscountOfferLog` / `WhatsAppMessage` rows, never imports `apps.shipments.integrations.delhivery_client` / `apps.whatsapp.services.send_*` / `apps.whatsapp.services.queue_template_message` / `meta_cloud_client` / `apps.payments.integrations.razorpay_client` / `razorpay` / `dotenv` (asserted with static-file scan that checks actual import lines, not docstring mentions), never edits any `.env*` file, never exposes raw phone / email / address / `Payment.raw_response` / full `gateway_reference_id`.** Approval flips status to `approved_for_future_phase8f_real_customer_controlled_mutation` only — Phase 8F (real customer controlled mutation), Phase 7E-Live-B, and Phase 7G-Live all remain **not approved**. Defaults LOCKED: `PHASE8E_REAL_CUSTOMER_PAYMENT_ORDER_PILOT_ENABLED=false`. 28 new backend tests + 1 new frontend vitest case; verification baseline now **2139 backend tests + 80 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Phase 8F (real customer controlled mutation) remains NOT approved. Phase 7E-Live-B (real customer WhatsApp send) remains NOT approved. Phase 7G-Live (real customer courier execution) remains NOT approved.**

**Phase 8D-Hotfix-1 status update:** Phase 8D-Hotfix-1 — Resolve Phase 8C evidence via rollback record (not `attempt.status` only) — is **shipped**. The on-VPS Phase 8C attempt id=3 was set to `status="blocked"` by a later post-rollback re-run even though the executed + rolled_back evidence remained valid (`executed_at=2026-05-12 11:11:50 UTC`, `recorded_signoff_window_valid=True`, all `*_mutation_was_made` flags True, every provider/send/courier/notification flag False; rollback id=1 has `status=rollback_recorded` + `rollback_was_made=True` + `restored_*=Pending` + final Order/Payment both `Pending`). The old Phase 8D resolver filtered Phase 8C attempts on `status=ROLLED_BACK` only — which missed attempt id=3 and produced `phase8d_source_phase8c_attempt_not_found`. The new resolver `_resolve_phase8c_evidence_via_rollback(phase8c_gate)` in `apps.payments.phase8d_controlled_mutation_evidence_lock` finds the attempt via the rollback record (`attempt__gate=phase8c_gate` + `rollback.status=rollback_recorded` + `rollback.rollback_was_made=True` + `rollback.restored_order_status="Pending"` + `rollback.restored_payment_status="Pending"`, ordered by `-rolled_back_at`); then `_validate_phase8c_attempt` drops the `status==ROLLED_BACK` check while keeping every evidence check (`executed_at` set, `recorded_signoff_window_valid=True`, `order/payment/business_mutation_was_made=True`, every provider/send/courier/notification flag False); `_validate_phase8c_rollback_record` adds explicit `restored_order_status==Pending` / `restored_payment_status==Pending` blockers; `_validate_final_target_state` adds the new `phase8d_target_payment_raw_response_phase8c_sandbox_must_be_true` check so the live Payment row must still carry the explicit sandbox proof. `_build_evidence_json` adds 5 top-level normalized fields — `executionEvidenceValid`, `rollbackEvidenceValid`, `attemptStatusAtEvidenceLock` (e.g. `"blocked"`), `rollbackStatus` (e.g. `"rollback_recorded"`), `finalDbRestored` — so downstream readers consume canonical signals rather than re-deriving from the nested phase8c snapshot. `inspect_phase8d_controlled_mutation_evidence_lock_readiness` refines the `eligiblePhase8CGateCount` to join through `attempts__rollbacks` so a gate is "eligible" only when both `gate.status=rolled_back` AND at least one valid rollback record exists. **Phase 8D-Hotfix-1 did NOT execute Phase 8C, did NOT roll back Phase 8C, did NOT call Razorpay / Meta Cloud / Delhivery / Vapi, did NOT send or queue WhatsApp, did NOT send a customer notification, did NOT create a `Shipment` / AWB / payment link, did NOT capture or refund, did NOT mutate any real `Order` / `Payment` / `Customer` / `Lead` / `Shipment` / `DiscountOfferLog` / `WhatsAppMessage` row, did NOT edit any `.env*` file.** No new audit kind, no new model, no new migration, no new API endpoint, no new CLI command — surgical resolver patch only. 10 net new backend tests in [`tests/test_phase8d_controlled_mutation_evidence_lock.py`](backend/tests/test_phase8d_controlled_mutation_evidence_lock.py): preview / prepare / lock all succeed against `attempt.status="blocked"` evidence chain; resolver returns `(None, None)` when rollback missing; readiness counter excludes gates without a valid rollback; payment `raw_response.phase8c_sandbox=False` blocks; payment `raw_response.phase8c_sandbox` missing blocks; rollback `restored_order_status≠Pending` blocks; lock-row `evidence_json` carries all 5 normalized hotfix keys; end-to-end no-side-effects assertion; full Phase 8D test file remains green at 36 passed. Verification baseline now **2111 backend tests + 79 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green.

**Phase 8D status update:** Phase 8D — Phase 8C Controlled Mutation Evidence Lock — is shipped (lock-only meta-audit; CLI-only review state changes; **never run on the VPS**). New `payments.RazorpayPaymentOrderControlledMutationEvidenceLock` model + migration `payments.0022_phase8d_controlled_mutation_evidence_lock` (pure CreateModel; no `RunPython`). New service `apps.payments.phase8d_controlled_mutation_evidence_lock` ships preview / prepare / lock / reject / archive lifecycle helpers + the defensive `assert_phase8d_no_provider_or_business_mutation` guard (8 locked-False snapshot booleans + 11 protected business-row count delta). The Phase 8D evidence lock chains through Phase 8C gate (`status=rolled_back`, `dry_run_passed=True`) → Phase 8C attempt (`status=rolled_back`, `executed_at` present, `recorded_signoff_window_valid=True`, `order_mutation_was_made=True`, `payment_mutation_was_made=True`, `business_mutation_was_made=True`, every customer-facing flag still False) → Phase 8C rollback (`status=rollback_recorded`, `rollback_was_made=True`) → Phase 8B → 8A → 7I → 7D. Eligibility additionally requires the target Order's `payment_status="Pending"` AND target Payment's `status="Pending"` (final DB state must show the rollback restored the originals); the runtime kill switch must be enabled; Phase 7E-Live-B / 7G-Live / broad customer automation all remain not-approved. 6 strictly-CLI management commands (`inspect_phase8d_controlled_mutation_evidence_lock`, `preview_…`, `prepare_…`, `lock_…_lock --reason "…"` (require_reason=True; revalidates eligibility at lock time so a tampered Phase 8C chain refuses to be locked), `reject_…_lock --reason "…"` (require_reason=True; refuses unless lock is `draft`/`pending_manual_review`/`blocked`), `archive_…_lock --reason "…"` (require_reason=True)). 4 read-only auth-protected GET DRF endpoints under `/api/v1/saas/phase8/controlled-mutation-evidence-{lock-readiness,locks,locks/<pk>,lock-preview}/`; POST/PATCH/DELETE return 405; **no POST lock / reject / archive endpoint exists**. 7 audit kinds (each ≤ 64 chars): `phase8d.evidence.{readiness_inspected,previewed,prepared,locked,rejected,archived,blocked}`. Audit payloads NEVER carry tokens / phones / addresses / raw secrets / Director sign-off text (scrubbed against a 23-entry `PHASE_8D_FORBIDDEN_PAYLOAD_KEYS` tuple). Frontend `/saas-admin` gains a read-only "Phase 8D Controlled Mutation Evidence Lock" section (`data-testid="phase8d-controlled-mutation-evidence-lock-section"`) — composite snapshot grid, Pending → Paid → Pending timeline view, locked-False contract chips, "Lock-only · CLI-only Review" banner; **no Lock Evidence / Reject Lock / Archive Lock / Re-execute Phase 8C / Re-rollback Phase 8C / Apply Mutation / Mark Paid / Send WhatsApp / Create Shipment / Capture / Refund / Notify Customer / Call Razorpay / Call Delhivery / Go Live / Edit `.env` buttons exist on the UI** (asserted in vitest). **Phase 8D never executes Phase 8C again, never rolls back Phase 8C again, never calls Razorpay, never calls Meta Cloud, never calls Delhivery, never calls Vapi (asserted with static-file scan that checks actual import lines, not docstring mentions), never sends or queues WhatsApp, never creates a `Shipment` / AWB / payment link, never captures / refunds, never sends a customer notification, never mutates real `Order` / `Payment` / `Customer` / `Lead` / `Shipment` / `DiscountOfferLog` / `WhatsAppMessage` rows (asserted with `_row_counts()` before/after parity across the full prepare → lock → archive lifecycle), never edits any `.env*` file.** Lock approval flips status to `locked` only — it does **NOT** authorise any live execution. Service-module static-file scan asserts no top-level Razorpay client / Meta Cloud client / Delhivery client / WhatsApp send helper / dotenv import. 26 new backend tests + 1 new frontend vitest case; verification baseline now **2101 backend tests + 79 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Phase 7E-Live-B (real customer WhatsApp send), Phase 7G-Live (real customer courier execution), and broad customer automation all remain NOT approved.**

**Phase 8C-Hotfix-1 status update:** Phase 8C-Hotfix-1 — Safe internal sandbox `Order` + `Payment` fixture seed — is **shipped**. New CLI-only, idempotent, audit-logged management command `python manage.py seed_phase8c_internal_controlled_mutation_fixture [--apply] [--json]` creates exactly **one** safe internal sandbox pair: `Order.id="phase8c-controlled-order-001"` (contains the `phase8c-controlled-` marker so `_order_is_internal_sandbox` returns `True`) and `Payment.id="phase8c-controlled-payment-001"` with `gateway_reference_id="phase8c-controlled-gateway-ref-001"` and `raw_response={"phase8c_sandbox": True, "internal_test": True, "created_by": "seed_phase8c_internal_controlled_mutation_fixture", "real_customer": False, "provider_call": False}` (direct proof via `raw_response.phase8c_sandbox=True` so `_payment_is_internal_sandbox` returns `True`). Default is **dry-run**; `--apply` writes the rows inside `transaction.atomic()` with an inline row-count guard that asserts only Order +0/+1 and Payment +0/+1 grew (the Phase 5D `apps.whatsapp.signals.post_save` lifecycle listener auto-creates a `WhatsAppLifecycleEvent` observability row per Order.save() with no actual customer-facing send — the row-count guard tolerates this metadata while asserting `WhatsAppMessage` / `Customer` / `Lead` / `Shipment` / `DiscountOfferLog` / `WhatsAppHandoffToCall` / `WorkflowStep` / `RescueAttempt` all stay at delta=0). Idempotent: a rerun reuses both rows and returns `createdOrder=False / createdPayment=False / reusedOrder=True / reusedPayment=True / countDeltas={} / nextAction=reused_existing_phase8c_fixture_run_dry_run`; a fresh `--apply` returns `createdOrder=True / createdPayment=True / countDeltas={order:1, payment:1} / nextAction=run_phase8c_dry_run`. Three new audit kinds (each ≤ 64 chars; Phase 6T-hotfix length budget): `phase8c.fixture.{seeded,dry_run,blocked}`. Audit payloads NEVER carry tokens / phones / addresses / raw secrets (scrubbed against a 19-entry forbidden-keys tuple). **Phase 8C-Hotfix-1 never calls Razorpay, never calls Meta Cloud, never calls Delhivery, never calls Vapi (asserted with static-file scan that checks actual import lines, not docstring mentions), never sends or queues WhatsApp, never creates a `Shipment` / `WorkflowStep` / `RescueAttempt` / AWB / `DiscountOfferLog`, never sends a customer notification, never mutates `Customer` / `Lead`, never edits any `.env*` file.** 9 net new backend tests (static-file scan; audit-kind length budget; dry-run creates no rows; apply creates exactly one Order + one Payment with `countDeltas={order:1, payment:1}`; rerun is idempotent and reuses; apply does not send WhatsApp / notify customer / grow protected tables; Phase 8C `_order_is_internal_sandbox` / `_payment_is_internal_sandbox` / `_validate_target_safety` / `_validate_target_references` all accept the seeded pair; `--json` output includes every required key) — full Phase 8C test file remains green at 32 passed. Verification baseline now **2075 backend tests + 78 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **The seeded pair is the canonical target for Phase 8C dry-run on the VPS** — operators can now run `python manage.py dry_run_phase8c_payment_order_controlled_mutation --gate-id <ID> --target-order-id phase8c-controlled-order-001 --target-payment-id phase8c-controlled-payment-001 --target-order-reference "phase8c::controlled::order::001" --target-payment-reference "phase8c::controlled::payment::001" --json` after `seed_phase8c_internal_controlled_mutation_fixture --apply`.

**Phase 8C status update:** Phase 8C — Controlled Real Payment → Order Mutation framework — is shipped (CLI-only one-shot mutation; **never run on the VPS** unless Director separately approves execution). New `payments.RazorpayPaymentOrderControlledMutationGate` + `payments.RazorpayPaymentOrderControlledMutationAttempt` + `payments.RazorpayPaymentOrderControlledMutationRollback` models + migration `payments.0021_phase8c_controlled_payment_order_mutation` (pure CreateModel; no `RunPython`). New service `apps.payments.phase8c_payment_order_controlled_mutation` ships preview / prepare / dry-run / approve / execute / rollback / reject / archive lifecycle helpers + the canonical `_build_evidence_json` composer (Phase 8B + 8A + 7I + 7D evidence) + two defensive guards (`assert_phase8c_no_unauthorized_side_effect` over 8 gate locked-False booleans + 5 attempt/rollback locked-False booleans + 11 protected business-row counts; `assert_phase8c_no_provider_send_or_courier` over the provider/send/courier flags) + the lazy `apps.saas.utc_window` import inside execute for the 15-minute structured Director sign-off window check. Eligibility refuses unless the source Phase 8B gate is `status=approved_for_future_phase8c_controlled_mutation_review` AND `dry_run_passed=True` AND `rollback_dry_run_passed=True` AND its locked-False contract still holds AND the Phase 7I lock is `status=locked` AND the runtime kill switch is enabled AND Phase 7E-Live-B / 7G-Live remain not-approved. **Target Order + Payment safety proof** is enforced at dry-run AND execute time: the target rows must either have an `id` / `confirmation_notes` / `gateway_reference_id` containing one of `phase8c::controlled::` / `phase8c-controlled-` / `internal-test` / `sandbox` OR the JSON marker `phase8c_sandbox=true` on `Order.confirmation_checklist` / `Payment.raw_response`. If proof is absent the dry-run blocks with `phase8c_target_order_not_proven_internal_sandbox` and `phase8c_target_payment_not_proven_internal_sandbox`. 9 strictly-CLI management commands (`inspect_phase8c_payment_order_controlled_mutation`, `preview_phase8c_payment_order_controlled_mutation --phase8b-gate-id N`, `prepare_phase8c_payment_order_controlled_mutation --phase8b-gate-id N`, `dry_run_phase8c_payment_order_controlled_mutation --gate-id N --target-order-id … --target-payment-id … --target-order-reference "phase8c::controlled::order::…" --target-payment-reference "phase8c::controlled::payment::…"`, `approve_phase8c_payment_order_controlled_mutation --gate-id N --reason "…"` (require_reason=True; refuses unless `gate.status=dry_run_passed` AND a `pending_director_signoff` attempt exists), `execute_phase8c_payment_order_controlled_mutation --attempt-id N --confirm-one-shot-mutation --director-signoff "phase8c_attempt_id_N phase8b_gate_id_M BEGIN_UTC=… END_UTC=…" --operator-name "…"` (refuses unless **three** env flags are true + kill switch enabled + structured 15-min UTC window + target safety proof + no prior executed attempt — mutates ONLY `Order.payment_status` and `Payment.status` to "Paid"), `rollback_phase8c_payment_order_controlled_mutation --attempt-id N --reason "…"` (record-only restore of original statuses; require_reason=True), `reject_phase8c_payment_order_controlled_mutation --gate-id N --reason "…"` (require_reason=True), `archive_phase8c_payment_order_controlled_mutation --gate-id N --reason "…"` (require_reason=True)). 6 read-only auth-protected GET DRF endpoints under `/api/v1/saas/phase8/`: `payment-order-controlled-mutation-readiness/`, `payment-order-controlled-mutation-gates/`, `payment-order-controlled-mutation-gates/<pk>/`, `payment-order-controlled-mutation-preview/?phase8b_gate_id=<ID>`, `payment-order-controlled-mutation-attempts/<gate_id>/`, `payment-order-controlled-mutation-rollbacks/<attempt_id>/`; POST/PATCH/DELETE return 405; **no POST endpoint dispatches state changes or execute** (review + execute are CLI-only). 12 audit kinds (each ≤ 64 chars): `phase8c.payment_order.{readiness_inspected,previewed,prepared,dry_run_passed,dry_run_failed,approved,executed,rollback_recorded,rejected,archived,blocked,failed}`. Frontend `/saas-admin` gains a read-only "Phase 8C Controlled Payment → Order Mutation" section (`data-testid="phase8c-payment-order-controlled-mutation-section"`). **Phase 8C never calls Razorpay / Meta Cloud / Delhivery / Vapi, never sends or queues WhatsApp, never creates a `Shipment` / `WorkflowStep` / `RescueAttempt` / AWB / payment link, never captures, never refunds, never sends a customer notification, never mutates real `Customer` / `Lead` / `Shipment` / `DiscountOfferLog` / `WhatsAppMessage` / `WhatsAppLifecycleEvent` / `WhatsAppHandoffToCall` rows (asserted with `_row_counts()` before/after parity across the full prepare → dry-run → approve → execute → rollback lifecycle), never imports `apps.shipments.integrations.delhivery_client` / `apps.whatsapp.services.send_*` / `apps.whatsapp.services.queue_template_message` / `meta_cloud_client` / `apps.payments.integrations.razorpay_client` / `razorpay` / `dotenv` (asserted with static-file scan that checks actual import lines, not docstring mentions), never edits any `.env*` file.** The execute path mutates ONLY `Order.payment_status` and `Payment.status` to `"Paid"` (the model-valid enum value) — symbolic markers `paid_controlled_phase8c` / `captured_controlled_phase8c` live on the gate row for evidence only and are NEVER written to the model fields. Defaults LOCKED: `PHASE8C_PAYMENT_ORDER_CONTROLLED_MUTATION_GATE_ENABLED=false`, `PHASE8C_DIRECTOR_APPROVED_ONE_SHOT_MUTATION=false`, `PHASE8C_ALLOW_INTERNAL_ORDER_PAYMENT_MUTATION=false`. **No execute command was run during this turn** — the happy-path execute test exercises the test database fixtures exclusively. 32 new backend tests + 1 new frontend vitest case; verification baseline now **2066 backend tests + 78 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Phase 7E-Live-B (real customer WhatsApp send), Phase 7G-Live (real customer courier execution), and broad customer automation all remain NOT approved.**

**Phase 8B status update:** Phase 8B — Payment → Order Mutation Review Gate — is shipped (review / dry-run only; CLI-only review state changes; **never run on the VPS**). New `payments.RazorpayPaymentOrderMutationReviewGate` + `payments.RazorpayPaymentOrderMutationReviewDryRun` models + migration `payments.0020_phase8b_payment_order_mutation_review_gate` (pure CreateModel; no `RunPython`). New service `apps.payments.phase8b_payment_order_mutation_review` ships preview / prepare / dry-run / rollback-dry-run / approve / reject / archive / summarize / inspect-readiness lifecycle helpers + the canonical `_build_evidence_json` composer (Phase 8A gate + Phase 7I lock + Phase 7D attempt) + the defensive `assert_phase8b_no_business_mutation` guard (6 gate-level locked-False booleans — `real_mutation_allowed`, `real_order_mutation_allowed`, `real_payment_mutation_allowed`, `customer_notification_allowed`, `whatsapp_allowed`, `courier_allowed`; 6 dry-run-level locked-False booleans — `would_mutate_order`, `would_mutate_payment`, `would_notify_customer`, `would_send_whatsapp`, `would_call_courier`, `would_create_shipment`; full business-row count delta over `Order` / `Payment` / `Shipment` / `DiscountOfferLog` / `Customer` / `Lead` / `WhatsAppMessage` / `WhatsAppLifecycleEvent` / `WhatsAppHandoffToCall` / `WorkflowStep` / `RescueAttempt`). Eligibility refuses unless the source Phase 8A gate is `status=approved_for_future_phase8b_review` AND its locked-False contract still holds AND the chained Phase 7I lock is `status=locked` AND the chained Phase 7D attempt is `status=rolled_back` with `business_mutation_was_made=False` AND `customer_notification_sent=False` AND the runtime kill switch is enabled AND Phase 8C / 7E-Live-B / 7G-Live remain not-approved. 8 strictly-CLI management commands (`inspect_phase8b_payment_order_mutation_review_gate`, `preview_phase8b_payment_order_mutation_review_gate --phase8a-gate-id N`, `prepare_phase8b_payment_order_mutation_review_gate --phase8a-gate-id N`, `dry_run_phase8b_payment_order_mutation_review_gate --gate-id N --target-order-reference "phase8b::review::order::…"` (refuses unless reference starts with `phase8b::review::order::` / `phase8b-review-` / `review::phase8b::`), `rollback_dry_run_phase8b_payment_order_mutation_review_gate --dry-run-id N --reason "…"` (require_reason=True), `approve_phase8b_payment_order_mutation_review_gate --gate-id N --reason "…"` (require_reason=True; refuses unless `gate.status=dry_run_passed` AND at least one passed dry-run AND a recorded rollback dry-run; flips status to `approved_for_future_phase8c_controlled_mutation_review` only — does **NOT** enable any real mutation), `reject_phase8b_payment_order_mutation_review_gate --gate-id N --reason "…"` (require_reason=True; refuses unless gate is `draft`/`pending_manual_review`/`dry_run_passed`/`blocked`), `archive_phase8b_payment_order_mutation_review_gate --gate-id N --reason "…"` (require_reason=True)). 5 read-only auth-protected GET DRF endpoints under `/api/v1/saas/phase8/`: `payment-order-mutation-review-readiness/`, `payment-order-mutation-review-gates/`, `payment-order-mutation-review-gates/<pk>/`, `payment-order-mutation-review-preview/?phase8a_gate_id=<ID>`, `payment-order-mutation-review-dry-runs/<gate_id>/`; POST/PATCH/DELETE return 405; **no POST endpoint dispatches state changes** (review is CLI-only). 10 audit kinds (each ≤ 64 chars): `phase8b.payment_order.{readiness_inspected,previewed,prepared,dry_run_passed,dry_run_failed,rollback_recorded,approved,rejected,archived,blocked}`. Frontend `/saas-admin` gains a read-only "Phase 8B Payment → Order Mutation Review Gate" section (`data-testid="phase8b-payment-order-mutation-review-section"`). **Phase 8B never calls Razorpay / Meta Cloud / Delhivery / Vapi, never sends or queues WhatsApp, never creates a `Shipment` / `WorkflowStep` / `RescueAttempt` / AWB / payment link, never captures, never refunds, never sends a customer notification, never mutates real `Order` / `Payment` / `Shipment` / `DiscountOfferLog` / `Customer` / `Lead` / `WhatsAppMessage` / `WhatsAppLifecycleEvent` / `WhatsAppHandoffToCall` rows, never imports `apps.shipments.integrations.delhivery_client` / `apps.whatsapp.services.send_*` / `apps.whatsapp.services.queue_template_message` / `meta_cloud_client` / `apps.payments.integrations.razorpay_client` / `razorpay` / `dotenv` (asserted with static-file scan that checks actual import lines, not docstring mentions), never edits any `.env*` file.** Review-only references enforced; the dry-run snapshot before/after row counts are asserted equal for every business table at every state transition. Approval flips status to `approved_for_future_phase8c_controlled_mutation_review` only — Phase 8C (controlled real mutation), Phase 7E-Live-B, and Phase 7G-Live remain **not approved**. Defaults LOCKED: `PHASE8B_PAYMENT_ORDER_MUTATION_REVIEW_GATE_ENABLED=false`. 27 new backend tests + 1 new frontend vitest case; verification baseline now **2034 backend tests + 77 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Phase 8C (controlled real mutation) remains NOT approved. Phase 7E-Live-B (real customer WhatsApp send) remains NOT approved. Phase 7G-Live (real customer courier execution) remains NOT approved.**

**Phase 8A status update:** Phase 8A — Payment → Order Mutation Sandbox Gate — is shipped (sandbox / dry-run only; CLI-only review state changes; **never run on the VPS**). New `payments.RazorpayPaymentOrderMutationSandboxGate` + `payments.RazorpayPaymentOrderMutationDryRun` models + migration `payments.0019_phase8a_payment_order_mutation_sandbox`. New service `apps.payments.phase8a_payment_order_mutation_sandbox` ships preview / prepare / dry-run / rollback-dry-run / approve / reject / archive / summarize / inspect-readiness lifecycle helpers + the canonical `_build_evidence_json` composer (Phase 7I lock + Phase 7D attempt evidence) + the defensive `assert_phase8a_no_business_mutation` guard (6 gate-level locked-False booleans + 5 dry-run-level locked-False booleans + business-row-table count delta over `Order` / `Payment` / `Shipment` / `DiscountOfferLog` / `Customer` / `Lead` / `WhatsAppMessage` / `WhatsAppLifecycleEvent` / `WhatsAppHandoffToCall` / `WorkflowStep` / `RescueAttempt`). 8 strictly-CLI management commands (`inspect_phase8a_payment_order_mutation_sandbox`, `preview_phase8a_payment_order_mutation_sandbox --phase7i-lock-id N`, `prepare_phase8a_payment_order_mutation_sandbox --phase7i-lock-id N`, `dry_run_phase8a_payment_order_mutation_sandbox --gate-id N --synthetic-order-reference "phase8a::sandbox::…"` (refuses unless reference starts with `phase8a::sandbox::` / `phase8a-sandbox-` / `sandbox::`), `rollback_dry_run_phase8a_payment_order_mutation_sandbox --dry-run-id N --reason "…"` (require_reason=True), `approve_phase8a_payment_order_mutation_sandbox --gate-id N --reason "…"` (require_reason=True; refuses unless gate.status=`dry_run_passed` AND at least one passed dry-run exists; flips status to `approved_for_future_phase8b_review` only — does **NOT** enable any real mutation), `reject_phase8a_payment_order_mutation_sandbox --gate-id N --reason "…"` (require_reason=True; refuses unless gate is `draft`/`pending_manual_review`/`dry_run_passed`/`blocked`), `archive_phase8a_payment_order_mutation_sandbox --gate-id N --reason "…"` (require_reason=True)). 5 read-only auth-protected GET DRF endpoints under `/api/v1/saas/phase8/`: `payment-order-mutation-sandbox-readiness/`, `payment-order-mutation-sandbox-gates/`, `payment-order-mutation-sandbox-gates/<pk>/`, `payment-order-mutation-sandbox-preview/?phase7i_lock_id=<ID>`, `payment-order-mutation-sandbox-dry-runs/<gate_id>/`; POST/PATCH/DELETE return 405; **no POST endpoint dispatches state changes** (review is CLI-only). 10 audit kinds (each ≤ 64 chars): `phase8a.payment_order.{readiness_inspected,previewed,prepared,dry_run_passed,dry_run_failed,rollback_recorded,approved,rejected,archived,blocked}`. Frontend `/saas-admin` gains a read-only "Phase 8A Payment → Order Mutation Sandbox Gate" section (`data-testid="phase8a-payment-order-mutation-sandbox-section"`). **Phase 8A never calls Razorpay / Meta Cloud / Delhivery / Vapi, never sends or queues WhatsApp, never creates a `Shipment` / `WorkflowStep` / `RescueAttempt` / AWB / payment link, never captures, never refunds, never sends a customer notification, never mutates real `Order` / `Payment` / `Shipment` / `DiscountOfferLog` / `Customer` / `Lead` / `WhatsAppMessage` / `WhatsAppLifecycleEvent` / `WhatsAppHandoffToCall` rows, never imports `apps.shipments.integrations.delhivery_client` / `apps.whatsapp.services.send_*` / `apps.whatsapp.services.queue_template_message` / `meta_cloud_client` / `apps.payments.integrations.razorpay_client` / `razorpay` / `dotenv` (asserted with static-file scan that checks actual import lines, not docstring mentions), never edits any `.env*` file.** Synthetic-only references enforced (`phase8a::sandbox::…` / `phase8a-sandbox-…` / `sandbox::…`); the dry-run snapshot before/after row counts are asserted equal for every business table at every state transition. Approval flips status to `approved_for_future_phase8b_review` only — it does **NOT** enable any real mutation. Defaults LOCKED: `PHASE8A_PAYMENT_ORDER_MUTATION_SANDBOX_ENABLED=false`. 26 new backend tests + 1 new frontend vitest case; verification baseline now **2007 backend tests + 76 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Phase 8B (real payment → order mutation) remains NOT approved. Phase 7E-Live-B (real customer WhatsApp send) remains NOT approved. Phase 7G-Live (real customer courier execution) remains NOT approved.**

**Phase 7I status update:** Phase 7I — Final Phase 7 Payment + WhatsApp + Courier Audit Lock — is **shipped (lock-only meta-audit; CLI-only review state changes; never run on the VPS)**. New `payments.RazorpayPhase7FinalAuditLock` model + migration `payments.0018_phase7i_final_audit_lock`. New service `apps.payments.phase7_final_audit_lock` ships preview / prepare / approve / reject / archive lifecycle helpers + the defensive `assert_phase7i_no_provider_or_business_mutation` guard (11 locked-False snapshot booleans across the four source phases + 11 business-row count delta). Eligibility composes four source records: Phase 7D Razorpay TEST attempt (`status in {executed, rolled_back}` + business/customer/shipment booleans False), Phase 7E-Live-A internal allowed-list WhatsApp send attempt (`status=rollback_recorded` + non-empty `provider_message_id` + `whatsapp_message_created=True` + `recorded_signoff_window_valid=True` + `claim_vault_grounded=True` + `recipient_scope=internal_staff_allow_list` + every customer / business / real-customer-phone boolean False), Phase 7G Delhivery TEST/MOCK courier attempt (`status=rolled_back_recorded` + `awb_created=True` + `rollback_status=recorded_only_no_provider_cancel` + every shipment / business / customer-notification boolean False), Phase 7H courier execution evidence lock (`status=locked` + every snapshot boolean False). Cross-chain consistency check ensures Phase 7H points at the supplied Phase 7G AND Phase 7G's source Phase 7D matches the supplied one. 6 strictly-CLI management commands (`inspect_phase7i_final_audit_lock`, `preview_phase7i_final_audit_lock`, `prepare_phase7i_final_audit_lock --phase7g-attempt-id N --phase7h-evidence-lock-id N [--phase7e-live-attempt-id N] [--phase7d-attempt-id N]`, `approve_phase7i_final_audit_lock --lock-id N --reason "…"`, `reject_phase7i_final_audit_lock --lock-id N --reason "…"`, `archive_phase7i_final_audit_lock --lock-id N --reason "…"`). 4 read-only auth-protected GET DRF endpoints under `/api/v1/saas/phase7/final-audit-lock-{readiness,locks,locks/<pk>,preview}/`; POST/PATCH/DELETE return 405; **no POST lock / approve / reject / archive endpoint exists**. 7 audit kinds (each ≤ 64 chars): `phase7i.final_audit.{readiness_inspected,previewed,prepared,locked,rejected,archived,blocked}`. Frontend `/saas-admin` gains a read-only "Phase 7I Final Audit Lock" section (`data-testid="phase7i-final-audit-lock-section"`). **Phase 7I never calls Razorpay, never calls Meta Cloud, never calls Delhivery, never calls Vapi, never sends or queues WhatsApp, never creates a `Shipment` / AWB / payment link, never captures / refunds, never sends a customer notification, never mutates real `Order` / `Payment` / `Customer` / `Lead` / `DiscountOfferLog` rows, never edits any `.env*` file.** Approval flips status to `locked` only — it does NOT authorize any live execution. Service-module static-file scan asserts no top-level Razorpay client / Meta Cloud client / Delhivery client / WhatsApp send helper / dotenv import. The on-VPS Phase 7H lock id 1 + Phase 7E-Live-A attempt id 3 + Phase 7G attempt id 2 + Phase 7D attempt id 1 are now Phase 7I lock-eligible — but no Phase 7I lock was prepared during this turn. 27 new backend tests + 1 new frontend vitest case; verification baseline now **1981 backend tests + 75 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Phase 7E-Live-B (real customer WhatsApp send) remains NOT approved. Phase 7G-Live (real customer courier execution) remains NOT approved.**

**Phase 7E-Live-A-Hotfix-3 status update:** Phase 7E-Live-A-Hotfix-3 — Safe retry after no-provider manual rollback before execution — is **shipped**. The retry-eligibility predicate `_retry_eligible` in `apps.payments.razorpay_whatsapp_internal_send` now has **two qualifying paths** (both require the shared locked-False contract via a new helper `_no_meta_side_effect`): (a) the existing Hotfix-2 path — `status in {rollback_recorded, failed}` + `provider_call_attempted=True` + `meta_cloud_call_attempted=True` + the failure reason matches the known pre-hotfix wrapper marker; (b) the new Hotfix-3 path — `status=rollback_recorded` + `provider_call_attempted=False` + `meta_cloud_call_attempted=False` + `executed_at is None` + `failed_at is None` (manual rollback before execute ever ran; strictly safer than Hotfix-2 because the wrapper was never even reached). Both paths require every Meta-side / business-side boolean (`provider_message_id` empty, `provider_status` empty, `whatsapp_message_created` / `whatsapp_message_queued` / `customer_notification_sent` / `business_mutation_was_made` / `real_customer_allowed` / `real_customer_phone_used` all False) to hold via `_no_meta_side_effect`. Auto-retry refused when any of those flips True — manual review only with `nextAction=phase7e_live_attempt_terminal_manual_review_required`. The retry path remains **database-only**: the lazy `_send_internal_template_via_meta_cloud` wrapper is `MagicMock.assert_not_called` in the retry-no-wrapper test; the eleven business / send / courier row counts stay constant across the retry; the new retry row's locked-False contract starts intact. **Hotfix-3 did NOT execute any WhatsApp send, did NOT call Meta Cloud, did NOT send to a real customer, did NOT mutate any business row, did NOT send a customer notification, did NOT touch a real customer phone, did NOT edit any `.env*` file.** The `PHASE7E_LIVE_INTERNAL_WHATSAPP_SEND_ENABLED` env flag remains locked-OFF. The on-VPS Phase 7E-Live attempt id 2 (`rollback_recorded` with `provider_call_attempted=false`, `meta_cloud_call_attempted=false`, `executed_at=null`, `failed_at=null`) is now retry-eligible and the next prepare against that gate would mint `phase7e_live::internal_send::phase7e_gate::1::retry::3` — but no retry was minted during this turn. 13 net new backend tests (no-provider rollback retry path with idempotency-key check + old-row immutability + locked-False contract intact; retry sequence increments to 3 when both attempt 1 + attempt 2 exist; provider-call-attempted blocks; meta-cloud-call-attempted blocks; executed-at-set blocks; failed-at-set-with-unknown-reason blocks; provider-message-id-set blocks; whatsapp-message-created blocks; real-customer-phone-used blocks; customer-notification-sent blocks; business-mutation blocks; retry path never calls Meta wrapper — `assert_not_called`; VPS-state mirror end-to-end test that walks attempt-1 = wrapper-failure → attempt-2 = no-provider rollback → attempt-3 = fresh retry-prepared row). Verification baseline now **1954 backend tests + 74 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Phase 7E-Live-B (real customer WhatsApp send) remains NOT approved.**

**Phase 7E-Live-A-Hotfix-2 status update:** Phase 7E-Live-A-Hotfix-2 — Safe retry after old wrapper-method failed attempt — is **shipped**. `prepare_phase7e_live_internal_send` now selects the **latest attempt** against the source Phase 7E gate (not just the first row that matched the original idempotency key). When that latest attempt is `rollback_recorded` or `failed` AND `provider_message_id` is empty AND `provider_status` is empty AND every Meta-side boolean (`whatsapp_message_created`, `whatsapp_message_queued`, `customer_notification_sent`, `business_mutation_was_made`, `real_customer_allowed`, `real_customer_phone_used`) is False AND the warnings list contains the **known pre-hotfix wrapper-method marker** (`Meta Cloud client does not expose send_template_message`), prepare mints a fresh retry attempt with a new idempotency key `phase7e_live::internal_send::phase7e_gate::<gate>::retry::<N>` (`N=2,3,…`). Original row is **never** mutated. Non-terminal latest attempts (`draft` / `pending_director_signoff` / `approved_for_internal_one_shot_send` / `blocked`) are reused as before. Terminal attempts with any Meta-side effect (provider_message_id set, whatsapp_message_created/queued True, real_customer_phone_used True, customer_notification_sent True, business_mutation_was_made True) OR with an **unknown failure reason** refuse auto-retry with `nextAction=phase7e_live_attempt_terminal_manual_review_required` — manual review only so the on-call human can determine whether a real WhatsApp landed. New audit kind `phase7e.internal_send.retry_prepared` (36 chars, ≤ 64-char budget) registered in `apps.audit.signals`; payload carries `retry_sequence` + `previous_attempt_id` but never tokens / phones / addresses / raw secrets (asserted in a positive + negative test). The retry path is **database-only**: `_send_internal_template_via_meta_cloud` is patched with `mock.MagicMock` and asserted `assert_not_called` in the retry-no-wrapper test; the eleven business / send / courier row counts (`_row_counts()`) stay unchanged across the retry. **Hotfix-2 did NOT run any execute command, did NOT call Meta Cloud, did NOT send WhatsApp, did NOT mutate any business row, did NOT send a customer notification, did NOT touch a real customer phone, did NOT edit any `.env*` file.** The `PHASE7E_LIVE_INTERNAL_WHATSAPP_SEND_ENABLED` env flag remains locked-OFF. The VPS Phase 7E-Live attempt id 1 (`rollback_recorded` with the wrapper-method warning) is now retry-eligible. 11 net new backend tests (audit-kind length budget; rolled-back wrapper-failure retry path; failed-state wrapper-failure retry; terminal-with-`provider_message_id` blocks retry; terminal-with-`whatsapp_message_created` blocks retry; terminal-with-`real_customer_phone_used` blocks retry; unknown-failure-reason blocks retry; retry path never calls Meta wrapper — `assert_not_called`; pending-state reuse; retry-chain sequence increments through 3 generations; safe-audit-payload scrubs 9 forbidden keys + asserts `retry_sequence` / `previous_attempt_id`). Verification baseline now **1941 backend tests + 74 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Phase 7E-Live-B (real customer WhatsApp send) remains NOT approved.**

**Phase 7E-Live-A status update:** Phase 7E-Live-A — Internal allowed-list WhatsApp one-shot send gate — is **shipped (CLI-only execute path; never run on the VPS; locked-OFF; never sends to a real customer phone)**. New `payments.RazorpayWhatsAppInternalSendAttempt` model + migration `payments.0017_phase7h_evidence_lock_and_phase7e_live_internal_send`. New service `apps.payments.razorpay_whatsapp_internal_send` ships preview / prepare / approve / reject / execute / rollback / readiness lifecycle helpers + the defensive `assert_phase7e_live_no_business_mutation` guard (over 4 locked-False booleans + 11 business-row count delta — only the WhatsApp outbound row is allowed to grow). The Meta Cloud client is **lazy-imported only inside `_send_internal_template_via_meta_cloud`** and only invoked inside the guarded execute path (asserted by static-file scan tests; tests mock the wrapper so the real network is never hit). 8 strictly-CLI management commands (`inspect_phase7e_live_internal_whatsapp_send_readiness`, `preview_…`, `prepare_… --gate-id --template-name --template-language --allowed-recipient-last4`, `approve_… --reason --director-signoff`, `reject_…`, `execute_phase7e_live_internal_whatsapp_send` (the **only** Meta-Cloud-touching command — refuses unless `PHASE7E_LIVE_INTERNAL_WHATSAPP_SEND_ENABLED=true` + `WHATSAPP_LIVE_META_LIMITED_TEST_MODE=true` + `--confirm-internal-whatsapp-send` + `--director-signoff` with structured `BEGIN_UTC=` / `END_UTC=` markers (≤ 15 min, fresh, now-in-window — reuses `apps.saas.utc_window.validate_within_director_window`) + `--operator-name` + recipient last-4 still resolves to `WHATSAPP_LIVE_META_ALLOWED_TEST_NUMBERS` + kill switch enabled + all six broad-automation flags off + no prior provider call), `rollback_…` (record-only — never calls Meta Cloud cancel), `inspect_…_attempts`). 4 read-only auth-protected GET DRF endpoints under `/api/v1/saas/whatsapp/internal-send-{readiness,attempts,attempts/<pk>,preview}/`; POST/PATCH/DELETE return 405; **no POST execute / approve / reject / archive endpoint exists**. 9 audit kinds (each ≤ 64 chars). Frontend `/saas-admin` gains a read-only "Phase 7E-Live-A Internal WhatsApp One-shot" section (`data-testid="phase7e-live-internal-send-section"`). **Phase 7E-Live-A never sends to a real customer phone (only an allow-list entry from `WHATSAPP_LIVE_META_ALLOWED_TEST_NUMBERS`), never queues broad automation, never calls Delhivery / Razorpay / Vapi, never sends a customer notification, never mutates real `Order` / `Payment` / `Customer` / `Lead` / `DiscountOfferLog` rows, never sends freeform medical text (approved Meta template only with Claim Vault grounding), never edits any `.env*` file.** Defaults LOCKED: `PHASE7E_LIVE_INTERNAL_WHATSAPP_SEND_ENABLED=false`. **Phase 7E-Live-B (real customer WhatsApp send) remains NOT approved.**

**Phase 7H status update:** Phase 7H — Final audit/evidence lock for the completed Phase 7G TEST/MOCK courier execution — is **shipped (lock-only; CLI-only review state changes)**. New `payments.RazorpayCourierExecutionEvidenceLock` model + migration `payments.0017_phase7h_evidence_lock_and_phase7e_live_internal_send`. New service `apps.payments.razorpay_courier_execution_evidence_lock` ships preview / prepare / approve / reject / archive lifecycle helpers + the defensive `assert_phase7h_no_provider_or_business_mutation` guard (3 locked-False snapshot booleans + 11 business-row count delta). Eligibility refuses unless the source Phase 7G attempt is `status=rolled_back_recorded` AND `provider_call_attempted=True` AND `delhivery_call_attempted=True` AND `awb_created=True` AND `provider_object_id` present AND `recorded_signoff_window_valid=True` AND `rollback_status=recorded_only_no_provider_cancel` AND every locked-False boolean (`shipment_created`, `business_mutation_was_made`, `customer_notification_sent`, `real_*_mutation_was_made`) still False. 6 strictly-CLI management commands (`inspect_phase7h_courier_execution_evidence_lock`, `preview_…_lock`, `prepare_…_lock`, `approve_…_lock --reason`, `reject_…_lock --reason`, `archive_…_lock --reason`). 4 read-only auth-protected GET DRF endpoints under `/api/v1/saas/delhivery/courier-execution-evidence-lock-{readiness,locks,locks/<pk>,preview}/`; POST/PATCH/DELETE return 405; **no POST lock / approve / reject / archive endpoint exists**. 7 audit kinds (each ≤ 64 chars). Frontend `/saas-admin` gains a read-only "Phase 7H Courier Execution Evidence Lock" section (`data-testid="phase7h-courier-evidence-lock-section"`). **Phase 7H never calls Delhivery, never creates a `Shipment` / `WorkflowStep` / `RescueAttempt` row, never creates an AWB, never sends or queues WhatsApp, never calls Meta Cloud / Razorpay / Vapi, never sends a customer notification, never mutates real `Order` / `Payment` / `Customer` / `Lead` / `DiscountOfferLog` rows, never edits any `.env*` file.** Approval flips status to `locked` only — it does NOT authorize any live execution. The completed Phase 7G attempt id 2 (`DLH35391376`, `Pickup Scheduled`, `rolled_back_recorded`, `recorded_only_no_provider_cancel`) is now lock-eligible. **Phase 7G-Live (real customer courier execution) remains NOT approved.** Verification baseline now **1925 backend tests + 74 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green.

**Phase 7G-Hotfix-2 status update:** Phase 7G-Hotfix-2 — Safe retry after pre-window blocked non-provider attempt — is **shipped**. `prepare_phase7g_courier_execution_attempt` now selects the *latest* attempt against the source Phase 7F gate (not just any row that matches the original idempotency key). When the latest attempt is `rolled_back_recorded` AND every provider / business / send boolean is False AND `executed_at is None` (i.e. the attempt was blocked **before** the lazy Delhivery wrapper ran), prepare mints a fresh retry attempt with a new idempotency key `phase7g::courier_execution::phase7f_gate::<gate>::retry::<N>` (`N=2,3,…`). The original row is **never** mutated. Non-terminal latest attempts (`draft` / `pending_director_signoff` / `approved_for_one_shot_run` / `blocked`) are reused as before. Terminal attempts that *did* touch the provider (`executed`, `failed`, or `rolled_back_recorded` with any provider/business boolean True) refuse auto-retry with `nextAction=phase7g_attempt_terminal_manual_review_required` — manual review only, so the on-call operator can decide whether an AWB actually landed. New audit kind `razorpay.courier_execution.retry_prepared` (41 chars, well under the 64-char budget) registered in `apps.audit.signals`; payload carries `retry_sequence` + `previous_attempt_id` but never tokens / phones / addresses / raw secrets (asserted in tests). No execute command was run during this turn. No Delhivery API request, no AWB created, no Shipment row written, no business mutation, no WhatsApp send / queue, no Meta Cloud / Razorpay / Vapi call, no customer notification, no `.env*` edit. The three `PHASE7G_*` env flags remain locked-OFF. 9 new backend tests (retry-eligible terminal mints fresh row + idempotency-key check + audit-kind length budget + provider-touched terminal does not auto-retry + executed terminal does not auto-retry + failed terminal stays safe + retry path never calls Delhivery wrapper + pending-state reuse + retry-chain sequence increments + safe-audit-payload scrub). Verification baseline now **1882 backend tests + 72 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Phase 7G-Live (real customer courier execution) remains NOT approved. Phase 7H remains NOT approved.**

**Phase 7G-Hotfix-1 status update:** Phase 7G-Hotfix-1 — Structured UTC Window Guard for Delhivery execute command — is **shipped**. `execute_phase7g_courier_one_shot` (and the `execute_delhivery_courier_one_shot` CLI) now refuse to dispatch unless the Director sign-off contains structured `BEGIN_UTC=<ISO-Z>` / `END_UTC=<ISO-Z>` markers, window length ≤ 15 minutes (reuses `apps.saas.utc_window.validate_within_director_window` with the existing `max_window_seconds=900` default), window not stale > 24h, `END_UTC > BEGIN_UTC`, and `now ∈ [window_start, window_end]`. Refusal happens **before the lazy `_create_awb_via_dedicated_wrapper` import + Delhivery client touch**. New blockers exposed as `phase7g_director_signoff_missing_structured_utc_window`, `phase7g_director_signoff_malformed_structured_utc_window`, `phase7g_director_signoff_window_too_long_max_15_min`, `phase7g_director_signoff_window_stale_more_than_24h_old`, `phase7g_now_before_director_signoff_utc_window_start`, `phase7g_now_after_director_signoff_utc_window_end`. On a successful execute the parsed window is persisted onto the attempt row (`recorded_signoff_window_valid=True`, `recorded_signoff_window_start_utc`, `recorded_signoff_window_end_utc`); existing nullable schema from Phase 7G migration `payments.0016` already supports this — **no new migration required**. No execute command was run during this turn. No Delhivery API request was made, no AWB created, no Shipment row written, no WhatsApp send / queue, no Meta Cloud / Razorpay / Vapi call, no customer notification, no business mutation, no `.env*` edit. The three `PHASE7G_*` env flags remain locked-OFF. 11 new backend tests (10 service + CLI guard cases + 1 static-grep assertion + 1 CLI-help assertion); verification baseline now **1873 backend tests + 72 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Phase 7G-Live (real customer courier execution) remains NOT approved. Phase 7H remains NOT approved.**

**Phase 7F status update:** Phase 7F — Delhivery / Courier Controlled Readiness Gate — is shipped (gate-only, CLI-only review state changes). New `payments.RazorpayCourierReadinessGate` + `payments.RazorpayCourierReadinessDryRunRecord` models + migration `payments.0015_phase7f_courier_readiness_gate`. New service `apps.payments.razorpay_courier_readiness` ships preview / prepare / dry-run / rollback-dry-run / approve / reject lifecycle helpers + the canonical `build_phase7f_courier_readiness_contract` + the defensive `assert_phase7f_no_courier_or_business_mutation` guard (over 24 locked-False booleans + 11 business-row-table count delta). 8 strictly-CLI management commands (`inspect_delhivery_courier_readiness`, `preview_…_gate`, `prepare_…_gate`, `dry_run_…_gate`, `rollback_dry_run_…_gate`, `approve_…_gate` (require non-empty reason; **NO `--director-signoff` argument** — Phase 7F approval is a status transition only; refuses unless dry-run + rollback-dry-run + Hotfix-1 all pass), `reject_…_gate` (require non-empty reason; refuses unless gate is `draft`/`pending_manual_review`), `inspect_delhivery_courier_readiness_gates`). **Archive command intentionally deferred** (mirrors Phase 7E pattern). 5 read-only auth-protected GET DRF endpoints under `/api/v1/saas/delhivery/courier-readiness*`; POST/PATCH/DELETE return 405. 13 audit kinds (each ≤ 64 chars). Frontend `/saas-admin` gains a read-only "Delhivery / Courier Readiness" section (`data-testid="delhivery-courier-readiness-section"`). **Phase 7F never calls Delhivery, never creates a `Shipment` / `WorkflowStep` / `RescueAttempt` row, never creates an AWB, never books a pickup, never generates a courier label, never sends or queues WhatsApp, never calls Meta Cloud / Razorpay / Vapi, never sends a customer notification, never mutates real business rows, never imports `apps.shipments.integrations.delhivery_client.create_awb` / `_create_via_sdk` / `apps.shipments.services.create_shipment` / `create_rescue_attempt` / `update_rescue_outcome` / `apps.whatsapp.services.send_*` / `meta_cloud_client` / `apps.payments.integrations.razorpay_client` / `dotenv` (static-file scan), never edits any `.env*` file.** Approval flips status to `approved_for_future_phase7g_or_courier_execution_review` only — it does **NOT** enable any provider call. Defaults LOCKED: `PHASE7F_COURIER_READINESS_GATE_ENABLED=false`. 87 new backend tests + 2 new frontend tests; verification baseline is **1792 backend tests + 70 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Phase 7G (live courier execution) requires a separate Director directive AND a future "execute-window guard for Delhivery" extension reusing `apps.saas.utc_window.validate_within_director_window` (15-minute cap). Phase 7G remains NOT approved.**

**Phase 7D-Hotfix-1 status update:** Phase 7D-Hotfix-1 — Structured UTC Window Guard — is **shipped**. New `apps.saas.utc_window.validate_within_director_window` (alias `validate_execution_window`, default `max_window_seconds=900`). Both `execute_razorpay_controlled_pilot_test_order` and `execute_single_razorpay_test_order` now refuse to dispatch unless the Director sign-off contains structured `BEGIN_UTC=<ISO-Z>` / `END_UTC=<ISO-Z>` markers, window length ≤ 15 minutes, window not stale > 24h, and `now ∈ [window_start, window_end]`. Two schema-only migrations: `payments.0014_phase7d_hotfix_director_signoff_window` + `saas.0007_phase7d_hotfix_director_signoff_window` add 3 nullable fields each (`recorded_signoff_window_valid: BooleanField(null=True)`, `_start_utc`, `_end_utc`); historical rows stay `NULL` — Phase 7D attempt id 1 (`order_SmThqpK6sc6Dhs`) remains the canonical legacy free-text row and is NOT edited. No execute command was run during this turn. No Razorpay HTTP request, no WhatsApp send / queue, no Meta Cloud / Delhivery call, no shipment / AWB, no payment link / capture / refund, no customer notification, no business mutation, no `.env*` edit. Verification baseline now **1705 backend tests + 68 frontend tests** (+33 net new), lint/check/build green. Phase 7F (Delhivery) / Phase 7E-Live remain **not approved**.

**Phase 7G status update:** Phase 7G — One-shot Delhivery TEST/MOCK Courier Execution Gate — is shipped (CLI-only execution path; locked-OFF; **never run on the VPS**; **no Shipment row at execute time**). New `payments.RazorpayCourierExecutionAttempt` + `payments.RazorpayCourierExecutionRollback` models + migration `payments.0016_phase7g_courier_execution_attempt`. New service `apps.payments.razorpay_courier_execution` ships preview / prepare / approve / reject / execute / rollback / readiness lifecycle helpers + the canonical `build_phase7g_courier_execution_contract` + the defensive `assert_phase7g_no_unauthorised_mutation` guard (six locked-False booleans + business-row count delta). 8 strictly-CLI management commands (`inspect_delhivery_courier_execution_readiness`, `preview_…`, `prepare_…`, `approve_…`, `reject_…`, `execute_delhivery_courier_one_shot` (the **only** Delhivery-touching command — refuses unless three Phase 7G env flags + `--confirm-one-shot-courier-execution` + `--director-signoff` mentions Phase 7F gate id + `--operator-name` + `--mode-acknowledgement` matches live `DELHIVERY_MODE` ∈ {`mock`, `test`} + `--rollback-record-only-acknowledged` + kill switch enabled + source chain Phase 7F → 7E → 7D → 7B → 6T green + no prior idempotency-locked provider call), `rollback_…` (record-only — never calls Delhivery cancel; `rollback_status=recorded_only_no_provider_cancel`; `completed` value is intentionally not in the enum), `inspect_…_attempts`). Archive + recover commands intentionally deferred. 5 read-only auth-protected GET DRF endpoints under `/api/v1/saas/delhivery/courier-execution-{readiness,attempts,attempts/<pk>,preview,rollbacks/<attempt_id>}/`; POST/PATCH/DELETE return 405; **no POST execute / approve / reject / archive endpoint exists**. 13 audit kinds (each ≤ 64 chars). Frontend `/saas-admin` gains a read-only "Razorpay Delhivery Courier One-shot TEST/MOCK Execution" section (`data-testid="razorpay-courier-execution-section"`). **Phase 7G never creates a `Shipment` row at execute time** — provider/AWB summary lives on the attempt row only, `shipment_created` / `business_mutation_was_made` / `real_shipment_mutation_was_made` stay `False` permanently, and `awb_created` flips `True` only because Delhivery returned an AWB value. **Phase 7G never sends WhatsApp, never queues an outbound, never calls Meta Cloud / Razorpay / Vapi, never books a courier pickup separately, never generates / prints a courier label, never sends a customer notification, never mutates real `Order` / `Payment` / `Customer` / `Lead` / `DiscountOfferLog` rows, never edits any `.env*` file.** The Delhivery client (`apps.shipments.integrations.delhivery_client.create_awb`) is **lazy-imported only inside `_create_awb_via_dedicated_wrapper`** and only invoked inside the guarded execute path (asserted by static-file scan tests). Synthetic payload customer name is the literal `"Phase 7G TEST"`; phone is last-4 only (`"0000"`); address line is `"[redacted]"`; pin is the fixed prefix `"11000"`. Defaults LOCKED: `PHASE7G_COURIER_EXECUTION_ENABLED=false`, `PHASE7G_DIRECTOR_APPROVED_ONE_SHOT_COURIER_EXECUTION=false`, `PHASE7G_ALLOW_DELHIVERY_TEST_AWB=false`. 70 new backend tests + 2 new frontend tests; verification baseline is **1862 backend tests + 72 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Phase 7G-Live (real customer courier execution) remains NOT approved. Phase 7H remains NOT approved.**

**Phase 7E status update:** Phase 7E — Controlled Internal WhatsApp Notification Readiness Gate — is shipped (gate-only, CLI-only review state changes). New `payments.RazorpayWhatsAppInternalNotificationGate` + `payments.RazorpayWhatsAppInternalNotificationDryRunRecord` models + migration `payments.0013_phase7e_whatsapp_internal_notification_gate`. New service `apps.payments.razorpay_whatsapp_internal_notification` ships preview / prepare / dry-run / rollback-dry-run / approve / reject / archive lifecycle helpers + the canonical `build_phase7e_whatsapp_internal_notification_contract` + the defensive `assert_phase7e_no_send_or_business_mutation` guard (over 14 locked-False booleans + business-row count delta). New shared utility `apps.saas.utc_window` ships pure `parse_director_signoff_window` + `validate_review_window`; Phase 7E uses it for review-only approval validation, and Phase 7D-Hotfix-1 (separate later turn) will reuse and extend it for execute-window guards on `execute_razorpay_controlled_pilot_test_order` and `execute_single_razorpay_test_order`. 8 strictly-CLI management commands (`inspect_razorpay_whatsapp_internal_notification_readiness`, `preview_…_gate`, `prepare_…_gate`, `dry_run_…_gate`, `rollback_dry_run_…_gate`, `approve_…_gate` (require non-empty reason + structured `BEGIN_UTC=...`/`END_UTC=...` markers + literal `phase7d_attempt_id_<ID>` reference + window length ≤ 24h; legacy free-text source Phase 7D signoff requires `--acknowledge-source-phase7d-window-violation` AND `--reason` literally contains `acknowledged_phase7d_window_violation_ref_attempt_<ID>`), `reject_…_gate` (require non-empty reason; refuses unless gate is `draft`/`pending_manual_review`), `inspect_razorpay_whatsapp_internal_notification_gates`). 5 read-only auth-protected GET DRF endpoints under `/api/v1/saas/razorpay/whatsapp-internal-notification-{readiness,gates,gates/<pk>,preview,dry-runs/<gate_id>}/`; POST/PATCH/DELETE return 405. 14 audit kinds (each ≤ 64 chars). Frontend `/saas-admin` gains a read-only "Razorpay → WhatsApp Internal Notification Readiness" section (`data-testid="razorpay-whatsapp-internal-notification-section"`). **Phase 7E never sends WhatsApp, never queues an outbound, never calls Meta Cloud / Delhivery / Vapi, never creates a shipment / AWB / payment link, never captures, never refunds, never sends a customer notification, never mutates real business rows, never imports `apps.whatsapp.services.send_*` / `meta_cloud_client` / `dotenv` (static-file scan), never edits any `.env*` file.** Approval flips status to `approved_for_future_phase7f_or_7e_send_review` only — it does **NOT** enable any send path. Defaults LOCKED: `PHASE7E_WHATSAPP_INTERNAL_NOTIFICATION_GATE_ENABLED=false`. 91 new backend tests (71 service + 20 utility) + 2 new frontend tests; verification baseline is **1672 backend tests + 68 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. **Phase 7F (Delhivery shipment) / Phase 7E-Live (real customer WhatsApp send) remain NOT approved. Phase 7D-Hotfix-1 (structured UTC window guard for execute commands) is mandatory before any future provider-touching command runs.**

**Phase 7D status update:** Phase 7D — Razorpay Controlled Pilot one-shot internal TEST execution capability — is implemented and was **executed once on 2026-05-07** (provider order id `order_SmThqpK6sc6Dhs`, attempt id `1`, gate id `1`, `executed_at=2026-05-07T12:42:46Z`, status `rolled_back`, `rollback_status=completed`, `provider_call_attempted=True`, every business / WhatsApp / Delhivery / payment-link / capture / refund / shipment / customer-notification boolean stayed `False`; `safeToRunPhase7DExecution=false`; `nextPhase=7E_not_approved`). The execution fell ~134 seconds **before** the Director-approved UTC window of `2026-05-07T12:45:00Z → 2026-05-07T13:00:00Z`. **No business or customer impact** — every locked-False boolean stayed False, the attempt was rolled back. Phase 7D-Hotfix-1 (structured UTC window guard on `execute_razorpay_controlled_pilot_test_order` + `execute_single_razorpay_test_order`, reusing `apps.saas.utc_window`) is mandatory before any future provider-touching command — see [`docs/PHASE_7D_HOTFIX_1_PLAN.md`](docs/PHASE_7D_HOTFIX_1_PLAN.md). The path through `prepare → approve → execute → rollback / archive / recover` lives entirely behind three locked-OFF env flags (`PHASE7D_RAZORPAY_TEST_EXECUTION_ENABLED=false`, `PHASE7D_DIRECTOR_APPROVED_ONE_SHOT_EXECUTION=false`, `PHASE7D_ALLOW_RAZORPAY_TEST_ORDER=false`). The execute path refuses unless every flag is `true`, the Director sign-off names the exact Phase 7B gate id, the kill switch is enabled, `RAZORPAY_KEY_ID` starts with `rzp_test_`, the source chain (Phase 7B → 6T → 6S → 6R → 6Q → 6P → 6O → 6M) is green, and the locked synthetic payload (`amount=100 paise`, `currency=INR`, receipt `phase7d::ctrl_pilot::gate::<G>::attempt::<A>`) is intact. Phase 7D **never** sends WhatsApp, never queues an outbound, never calls Meta Cloud / Delhivery / Vapi, never creates a shipment / AWB, never creates a payment link, never captures, never refunds, never mutates `Order` / `Payment` / `Shipment` / `DiscountOfferLog` / `Customer` / `Lead`, never sends a customer notification, never edits any `.env*` file, and never exposes a POST execute / approve / reject / archive endpoint (review state changes are CLI-only). 12 audit kinds (all ≤ 64 chars). 9 strictly-CLI management commands (`execute_razorpay_controlled_pilot_test_order` is implemented but has **never been run**; it requires `--confirm-one-shot-test-execution` + `--director-signoff` and refuses on missing flag / non-`rzp_test` key / kill switch off / not-approved attempt / second-run idempotency lock). 5 read-only auth-protected GET DRF endpoints under `/api/v1/saas/razorpay/`; POST/PATCH/DELETE return 405. Frontend `/saas-admin` gains a new "Razorpay Controlled Pilot Execution (One-shot TEST)" section (`data-testid="razorpay-controlled-pilot-execution-section"`) — no execute / approve / send / notify buttons. Verification baseline is **1581 backend tests + 66 frontend tests**, with `makemigrations --check`, `manage.py check`, frontend lint, frontend tests, and frontend build green. Phase 7B gate-only work remains complete; Phase 7C is intentionally unimplemented; Phase 7E (WhatsApp) / 7F (Delhivery) / live customer flow remain **not approved**.

**Phase 7B status update:** Phase 7B Controlled Pilot Execution Gate (gate-only) is complete. It is gate-only and CLI-only for review state changes, with `PHASE7_CONTROLLED_PILOT_GATE_ENABLED=false` by default. The gate prepares / dry-runs / rollback-dry-runs / approves a `RazorpayControlledPilotExecutionGate` row from a locked Phase 6T audit lock; it never executes a pilot, never calls Razorpay / Meta Cloud / Delhivery / Vapi, never sends or queues WhatsApp, never creates a shipment / AWB, never mutates `Order` / `Payment` / `Shipment` / `Customer` / `Lead`, never validates the live `RAZORPAY_KEY_ID`, never edits `.env.production`. Approval flips status to `approved_for_future_phase7c_execution_review` only — Phase 7C / live execution remains **not approved**.

Full-stack AI Business Operating System for Nirogidhara Private Limited (Ayurvedic medicine D2C). React 18 + Vite + TS frontend talks to Django 5 + DRF backend. Director: Prarit Sidana — final authority for high-risk decisions. **Current strategic blueprint:** [`docs/MASTER_BLUEPRINT_V2.md`](docs/MASTER_BLUEPRINT_V2.md) (Master Blueprint v2.0 — supersedes the v1.0 PDF, which is now historical reference only).

Status: Phase 1 + 2A + 2B + 2C + 2D + 2E + 3A + 3B + 3C + 3D + 3E + 4A + 4B + 4C + 4D + 4E + 5A-0 + 5A-1 + 5A + 5B + 5C + 5D + 5E + 5E-Hotfix + 5E-Hotfix-2 + **5E-Smoke** + **5E-Smoke-Fix** + **5E-Smoke-Fix-2** + **5E-Smoke-Fix-3** + **5F-Gate** + **5F-Gate Hardening Hotfix** + **5F-Gate Controlled AI Auto-Reply Test Harness** + **5F-Gate Claim Vault Grounding Fix** + **5F-Gate Controlled Reply Confidence Fix** + **5F-Gate Deterministic Grounded Reply Builder** + **5F-Gate Objection & Handoff Reason Refinement** + **5F-Gate Internal Allowed-Number Cohort Tooling** + **5F-Gate Limited Auto-Reply Flag Plan** + **5F-Gate Real Inbound Deterministic Fallback Fix** + **5F-Gate Auto-Reply Monitoring Dashboard** + **5F-Gate Customer Pilot Readiness** + **6A SaaS Foundation Safe Migration** + **6B SaaS Default Org Data Backfill** + **6C Org-Scoped API Filtering Plan** + **6D Org-Aware Write Path Assignment** + **6E SaaS Admin + Integration Settings Foundation** + **6F Per-Org Runtime Integration Routing Plan** + **6G Controlled Runtime Routing Dry Run + AI Provider Routing** + **6H Controlled Runtime Routing Live Audit Gate** + **6I Single Internal Live Gate Simulation** + **6J Single Internal Provider Test Plan** + **6K-A Single Internal Razorpay Test-Mode Execution Gate** + **6K-B Manual VPS One-Shot Razorpay Test Execution** (`pex_8f309650e9644cfaae4418f9` → `order_Sks3KPf0vntKhf`, ₹1.00, rolled back) + **6L Razorpay Test Execution Audit Review + Webhook Readiness Plan** + **6M-0 MCP Gateway Foundation (dormant)** + **6M Razorpay Webhook Handler (test-mode, dormant by default)** + **6N Razorpay Webhook Business-Mutation Sandbox Plan (planning-only / readiness-only)** + **6O Razorpay Sandbox Status Mapping + Manual Review (sandbox-review-only)** + **6P Controlled Internal Paid-Status Mutation Test (sandbox-ledger-only, CLI-only execution; `RAZORPAY_SANDBOX_PAID_STATUS_MUTATION_ENABLED=false` default; never mutates real `Order` / `Payment` / `Shipment` / `DiscountOfferLog` / `Customer` / `Lead`)** + **6Q Payment → Order Workflow Safety Gate (audit-gate-only, CLI-only review state changes; `RAZORPAY_PAYMENT_ORDER_WORKFLOW_GATE_ENABLED=false` default; no POST API endpoint dispatches gate state changes; never mutates real `Order` / `Payment` / `Shipment` / `DiscountOfferLog` / `Customer` / `Lead`)** + **6R Payment → WhatsApp / Courier Dispatch Readiness (audit-only readiness contract; `RAZORPAY_PAYMENT_DISPATCH_READINESS_ENABLED=false` default; CLI-only review state changes; no POST API endpoint; never sends WhatsApp, never calls Meta Cloud or Delhivery, never creates a shipment / AWB, never mutates real business tables, never calls Razorpay)** + **6S Limited Internal Dispatch Pilot Plan (planning-only; `RAZORPAY_PAYMENT_DISPATCH_PILOT_PLAN_ENABLED=false` default; CLI-only review state changes; no POST API endpoint; never executes a pilot, never sends WhatsApp, never calls Meta Cloud / Delhivery / Razorpay, never creates a shipment / AWB, never mutates real business tables)** + **6T Final Phase 6 Audit + Lock / Controlled Pilot Execution Decision Gate (audit-lock-only; `RAZORPAY_PHASE6_FINAL_AUDIT_LOCK_ENABLED=false` default; CLI-only review state changes; never executes a pilot, never sends or queues WhatsApp, never calls Meta Cloud / Delhivery / Razorpay, never creates a shipment / AWB, never mutates real business tables)** complete. **Test baseline: 1540 backend + 64 frontend, all green.** **Next planned: Phase 7C design-only planning accepted; Phase 7D / Razorpay-only one-shot internal test execution implementation requires fresh explicit Director approval. Phase 7C / live execution remains NOT approved.** **Phase 6D** wires the org/branch context into every business-state create flow without changing API request payloads or breaking single-tenant production. New `apps.saas.write_context` module ships five helpers (`resolve_write_organization`, `resolve_write_branch`, `apply_org_branch`, `get_parent_org_branch`, `assign_org_branch_from_parent`, `assign_org_branch_from_first_parent`). New `apps.saas.signals.auto_assign_org_branch` is a `pre_save` receiver connected to 13 business models (`Lead`, `Customer`, `Order`, `DiscountOfferLog`, `Payment`, `Shipment`, `Call`, `WhatsAppConsent`, `WhatsAppConversation`, `WhatsAppMessage`, `WhatsAppLifecycleEvent`, `WhatsAppHandoffToCall`, `WhatsAppPilotCohortMember`); fires only on create (`_state.adding=True`), only when the FK is `None`, walks the parent chain (`conversation` → `message` → `order` → `shipment` → `payment` → `call` → `customer` → `lead`) for inheritance, and falls back to the seeded default org / branch. NEVER overwrites an explicit assignment. NEVER fires on bulk `update()` (existing rows still need the backfill command). NEVER crashes a save — silent on any unexpected error. `audit.AuditEvent` continues to use the Phase 6C `write_event` auto-org context; not duplicated by the signal. New `apps.saas.write_readiness.compute_org_write_path_readiness` selector + `python manage.py inspect_org_write_path_readiness --json` + read-only `GET /api/v1/saas/write-path-readiness/` (auth required) report: `writeContextHelpersAvailable`, `auditAutoOrgContextEnabled`, 14 `safeCreatePathsCovered` entries (13 signal-wired models + audit auto-org), 14 `deferredCreatePaths` (system / webhook / child-of-message rows that Phase 6E will tackle), 13 `modelsWithOrgBranch`, `recentRowsWithoutOrganizationLast24h`, `recentRowsWithoutBranchLast24h`, `globalTenantFilteringEnabled=false`, `safeToStartPhase6E` boolean, plus a typed `nextAction` ∈ {`fix_write_path_assignment_gaps`, `run_default_org_backfill_again`, `ready_for_phase_6e_org_scoped_write_enforcement_plan`}. Frontend ships a small read-only `WritePathReadinessCard` next to the existing 6B / 6C cards on the dashboard. **No mutation controls anywhere.** **LOCKED in Phase 6D**: FKs stay nullable; `globalTenantFilteringEnabled=false`; no global queryset middleware (Phase 6E); per-org provider credentials remain in env vars (Phase 6E); WhatsApp env flags untouched; broad-automation flags remain LOCKED OFF; campaigns / broadcast remain LOCKED. **Cross-tenant write-leak proof**: a user creating a row gets their active-org assignment; a user from a different org cannot see it via `scoped_queryset_for_user`. **28 new backend tests + 2 new frontend tests; 979 backend + 27 frontend, all green.** Phase 5F (broadcast campaigns) remains LOCKED. **Phase 6C** lays the safe org-scoped filtering foundation without breaking single-tenant production. New `apps.saas.context` module ships seven resolvers (`get_default_organization`, `get_default_branch`, `get_user_active_organization` (active-membership lookup with default-org fallback for users without a membership row), `get_user_active_branch`, `resolve_request_organization` (honours middleware-stamped `request.organization`), `resolve_request_branch`, `user_has_org_access`) and four queryset helpers (`model_has_organization_field`, `filter_queryset_by_organization` (no-op when model has no `organization` field — never crashes), `attach_default_org_filter_if_model_supports_org`, `scoped_queryset_for_user`, `scoped_queryset_for_request`). Superusers see all rows for diagnostics; regular users see only their active-org rows. Inactive / archived organizations and disabled memberships fall back to the default org so existing logins stay working. **`apps.audit.signals.write_event` now auto-attaches the active organization** via three new optional kwargs (`organization`, `request`, `user`) — explicit > request > user > default-org fallback. Backwards-compatible: every existing call site keeps working unchanged. New `apps.saas.readiness.compute_org_scoped_api_readiness` selector + `python manage.py inspect_org_scoped_api_readiness --json` + read-only `GET /api/v1/saas/org-scope-readiness/` (auth required) report the per-phase readiness map: 14 scoped models, 14 unscoped (system/webhook/child) models, 23 scoped read APIs, 9 unscoped write APIs, `auditAutoOrgContextEnabled=true`, `globalTenantFilteringEnabled=false`, plus a typed `nextAction` ∈ {`fix_org_scope_blockers`, `run_default_org_backfill_again`, `ready_for_phase_6d_write_path_org_assignment`}. Frontend ships a small read-only `OrgScopeReadinessCard` next to the existing `SaasCoverageCard` on the dashboard. **No org switcher, no migration buttons, no enable/disable controls anywhere on the UI.** **LOCKED in Phase 6C**: no global queryset-filtering middleware (Phase 6E); FKs stay nullable; write-path org assignment lands in Phase 6D; per-org provider credentials stay in env vars (Phase 6D); WhatsApp env flags untouched; broad-automation flags remain LOCKED OFF; campaigns remain LOCKED. **Two-org leak proofs in tests**: `Customer`, `Order`, `WhatsAppConversation` all assert that scoping returns ONLY the user's org rows. **30 new backend tests + 2 new frontend tests; 951 backend + 25 frontend, all green.** Local readiness reports `safeToStartPhase6D=true` after backfill. Phase 5F (broadcast campaigns) remains LOCKED. **Phase 6B** safely attaches existing single-tenant data to the seeded default organization without breaking current production. **14 business-state models** received nullable `organization` (and `branch` where it makes sense) FKs: `crm.Lead`, `crm.Customer`, `orders.Order`, `orders.DiscountOfferLog` (org only), `payments.Payment`, `shipments.Shipment`, `calls.Call`, `whatsapp.WhatsAppConsent` (org only), `whatsapp.WhatsAppConversation`, `whatsapp.WhatsAppMessage` (org only — branch flows from conversation), `whatsapp.WhatsAppLifecycleEvent` (org only), `whatsapp.WhatsAppHandoffToCall` (org only), `whatsapp.WhatsAppPilotCohortMember` (org only), `audit.AuditEvent` (org only). Every field is `null=True, blank=True, on_delete=SET_NULL` — no constraint enforced yet, no existing factory broken. New idempotent management command `python manage.py backfill_default_organization_data --dry-run|--apply --json` defaults to dry-run; on `--apply` it sets `organization` (and `branch` where the model has that field) on every row currently NULL to the seeded `nirogidhara` org / `main` branch. NEVER overwrites an existing org/branch (asserted with before/after counts in tests). NEVER mutates business state (no status / amount / phone / payload changes). NEVER calls a provider API or sends a message. Emits `saas.default_org_backfill.{started,completed,failed}` audit rows. New `python manage.py inspect_default_organization_coverage --json` reports per-model row counts + `organizationCoveragePercent` / `branchCoveragePercent` + a typed `nextAction` ∈ {`fix_backfill_blockers`, `run_backfill_default_organization_data_apply`, `ready_for_phase_6c_org_scoped_api_filtering_plan`}. Both commands delegate to a shared `apps.saas.coverage.compute_default_organization_coverage` selector. New read-only DRF endpoint `GET /api/v1/saas/data-coverage/` returns the same shape; auth required; carries no secrets / no PII. Frontend ships a small read-only `SaasCoverageCard` on the dashboard showing org coverage % / branch coverage % / missing rows / `nextAction`. **No backfill / migrate / mutation buttons in the UI** — the apply happens only via the management command on the VPS. **LOCKED in Phase 6B**: `globalTenantFilteringEnabled` stays `False`; no middleware filters existing endpoints by organization yet; FKs stay nullable until Phase 6C confirms 100% coverage; WhatsApp env flags untouched; broad-automation flags remain LOCKED OFF; campaigns remain LOCKED. **17 new backend tests; 921 backend + 23 frontend, all green.** Local dry-run on the dev DB reports 654 rows missing org / 226 rows missing branch across the seeded demo data; `--apply` updated all 656 / 226 successfully. Phase 5F (broadcast campaigns) remains LOCKED. **Phase 6A** adds the multi-tenant scaffold without breaking the single-tenant production system. New `apps.saas` Django app ships five tables — `Organization` (top-level tenant — `code='nirogidhara'` is the seeded default), `Branch` (multi-location child), `OrganizationMembership` (org-level role enumeration distinct from `User.role`), `OrganizationFeatureFlag`, `OrganizationSetting` (rows flagged `is_sensitive=True` are filtered out of every public-API selector — Phase 6A ships the schema only, no real credentials live here yet). New `apps.saas.selectors` exposes `get_default_organization`, `get_default_branch`, `get_user_organizations` (falls back to default org when user has no membership rows yet so existing logins keep working), `get_active_organization_for_user`, `get_user_role_in_organization`, `get_organization_feature_flags`, `is_feature_enabled(org, key, default=False)`, `get_non_sensitive_settings`, `get_organization_membership_summary`. New idempotent management command `python manage.py ensure_default_organization --json` creates / reuses the default organization (`Nirogidhara Private Limited`, code `nirogidhara`) + default branch (`Main Branch`, code `main`), attaches every superuser as `owner`, every director user as `owner`, every admin user as `admin`, every other user as `viewer`. Writes one `saas.default_organization.ensured` audit row per run. NEVER mutates `Customer` / `Order` / `Payment` / `Shipment` / `DiscountOfferLog` / `WhatsAppMessage` (asserted with before/after counts in tests). Three new read-only DRF endpoints under `/api/v1/saas/`: `current-organization/` (active org + membership summary + non-sensitive settings + feature flags), `my-organizations/` (every active org the user belongs to), `feature-flags/` (per-org boolean toggles for the user's active org). All endpoints require auth; sensitive settings NEVER appear (asserted in tests); POST/PATCH/DELETE return 405. Frontend ships a small read-only `OrgBadge` component in the topbar showing the org name + user role; three new TypeScript types + three new `api` methods (`getSaasCurrentOrganization`, `getSaasMyOrganizations`, `getSaasFeatureFlags`) with deterministic mock fallback. **No org switcher, no enable/disable, no settings mutation controls anywhere in this phase.** **LOCKED in Phase 6A**: no existing model gets an `organization` FK in this migration; no request middleware filters existing endpoints by organization yet; Customer / Order / Payment / Shipment / WhatsApp data stays un-tenant-scoped — Phase 6C will add scoped filtering once a backfill command + a default-org backstop are both proven safe; WhatsApp env flags are not touched; broad-automation flags remain LOCKED OFF; campaigns remain LOCKED. **22 new backend tests + 3 new frontend tests; 904 backend + 23 frontend, all green.** Phase 5F (broadcast campaigns) remains LOCKED. **Phase 5F-Gate Real Inbound Deterministic Fallback Fix earlier**: The first real-inbound auto-reply test on the VPS (allowed test number suffix `9990`) verified end-to-end after the fix landed: real Meta inbound stored, deterministic Claim-Vault-grounded reply auto-sent, delivered + read on the phone, `replyAutoSentCount=1`, `autoReplyFlagPathUsedCount=1`, `deterministicBuilderUsedCount=1`, `unexpectedNonAllowedSendsCount=0`, mutation counts (Order/Payment/Shipment/DiscountOfferLog) = 0. After the soak Director rolled back to `WHATSAPP_AI_AUTO_REPLY_ENABLED=false` (safe OFF state, gate inspector reports `nextAction=ready_to_enable_limited_auto_reply_flag`). **Phase 5F-Gate Auto-Reply Monitoring Dashboard** ships **a read-only command-center surface for the WhatsApp auto-reply safety gate**, mirroring the soak monitor that proved the fix. New `apps.whatsapp.dashboard` selector module is the single source of truth: `get_auto_reply_gate_summary()` (gate readiness + blockers + masked allow-list), `get_recent_auto_reply_activity(hours)` (audit-kind counts + outbound flow + business-state mutation deltas + forensic non-allowed-send check), `get_internal_cohort_summary()` (per-allowed-number readiness — operator-only `--show-full-numbers` flag is intentionally NOT exposed via the API surface so full E.164 never travels the dashboard wire), `get_recent_whatsapp_audit_events(hours, limit)` (latest WhatsApp-prefixed audit rows scrubbed for sensitive keys like `token` / `verify_token` / `app_secret` / full phones), `get_whatsapp_mutation_safety_summary(hours)` (Order / Payment / Shipment / DiscountOfferLog / WhatsAppLifecycleEvent / WhatsAppHandoffToCall counts in the trailing window — auto-reply path NEVER mutates any of these), `get_unexpected_outbound_summary(hours)` (forensic per-message breakdown of any outbound that landed at a phone outside the live allow-list — phones masked to last-4), `get_whatsapp_monitoring_dashboard(hours)` (single-shot composer that derives a top-level `status` ∈ {`safe_off`, `limited_auto_reply_on`, `needs_attention`, `danger`} the frontend renders directly without re-deriving safety logic). The two existing management commands (`inspect_whatsapp_auto_reply_gate`, `inspect_recent_whatsapp_auto_reply_activity`) now delegate to these selectors so the CLI and the dashboard share one source of truth. Seven new admin-only DRF endpoints under `/api/whatsapp/monitoring/`: `overview/` (combined dashboard), `gate/`, `activity/?hours=N`, `cohort/`, `audit/?hours=N&limit=K`, `mutation-safety/?hours=N`, `unexpected-outbound/?hours=N`. Permission class `_AdminMonitoringPermission` (admin / director / superuser only — view-level even on GET, matching the existing `WhatsAppProviderStatusView` pattern). All endpoints are strictly read-only (POST/PATCH/DELETE return 405). Phone numbers masked to last-4 in every response (`+91*****99001` shape); audit payloads run through `_safe_audit_payload` which drops `token` / `access_token` / `verify_token` / `app_secret` / `META_WA_*` keys before returning, defence-in-depth even though the orchestrator already masks. New frontend page at `/whatsapp-monitoring` (sidebar group "Messaging") rendering: status badge ({Safe OFF / Limited Auto-Reply ON / Needs attention / Danger}), gate status cards (Provider / Limited test mode / Auto-reply enabled / Allowed list size / WABA active / Campaigns locked / Final-send guard / Consent + Claim Vault), broad-automation flag pills (call handoff / lifecycle / rescue / RTO / reorder / campaigns — all OFF/locked when healthy), activity metric cards (inbound / outbound / auto replies sent / deterministic builder / objection replies / blocked / delivered / read / guard blocks / unexpected non-allowed sends), mutation safety section (orders / payments / shipments / discount logs / lifecycle events / handoff events — green confirmation when all zero), internal cohort table (masked phone / suffix / customer found / consent state / latest inbound + outbound / status / ready), recent audit timeline (kind / tone / text / IDs / phone suffix / fallback flag), backend `nextAction` panel (read-only — no enable/disable controls). Auto-refreshes every 30 seconds; explicit Refresh button. **No send buttons. No automation enable/disable controls. No flag-flip controls. No business-data mutation hooks of any kind.** Six new TypeScript types (`WhatsAppMonitoringStatus`, `WhatsAppMonitoringGate`, `WhatsAppMonitoringActivity`, `WhatsAppMonitoringCohort`, `WhatsAppMonitoringMutationSafety`, `WhatsAppMonitoringUnexpectedOutbound`, `WhatsAppMonitoringAuditResponse`, `WhatsAppMonitoringOverview`); seven new `api` methods (`getWhatsAppMonitoringOverview`, `getWhatsAppMonitoringGate`, `getWhatsAppMonitoringActivity`, `getWhatsAppMonitoringCohort`, `getWhatsAppMonitoringAudit`, `getWhatsAppMonitoringMutationSafety`, `getWhatsAppMonitoringUnexpectedOutbound`) with deterministic mock fallback so the dashboard renders without a backend. **Hard rules preserved**: dashboard is read-only end-to-end (asserted in tests via 405-on-POST checks for every endpoint); cohort selector NEVER exposes full E.164 (asserted in tests); audit selector NEVER returns tokens / verify token / app secret (asserted in tests); unauthenticated access blocked; viewer / operations roles blocked; admin / director / superuser only. **22 new backend tests + 6 new frontend tests; 869 backend + 19 frontend, all green.** Phase 5F (broadcast campaigns / growth automation) remains LOCKED. **Phase 5F-Gate Real Inbound Deterministic Fallback Fix** ships **the deterministic Claim-Vault-grounded reply fallback inside the orchestrator's real-inbound webhook auto-reply path**, plus explicit latest-inbound safety isolation. The first real-inbound auto-reply test on the VPS (allowed test number suffix `9990`, real Meta inbound `WAM-100059` "Namaste. Mujhe weight management product ke price aur capsule quantity ke baare me bataye.") was blocked safely with `claim_vault_not_used` even though backend grounding was fully present (`category=weight-management`, `normalized_claim_product=Weight Management`, `claim_row_count=1`, `approved_claim_count=3`, `confidence=0.9`). Root cause: the deterministic grounded fallback existed only inside the controlled-CLI command (`run_controlled_ai_auto_reply_test`); the real-inbound webhook path (`run_whatsapp_ai_agent_for_conversation` → `run_whatsapp_ai_agent`) never benefited from it. Now `apps.whatsapp.ai_orchestration` shares one helper (`_attempt_deterministic_grounded_fallback` + `_dispatch_deterministic_fallback`) wired into the orchestrator at every soft non-safety blocker site (`claim_vault_not_used`, `low_confidence`, `ai_handoff_requested`, `no_action`). When auto-reply is enabled (env flag OR `force_auto_reply=True`), the inbound is a normal product-info inquiry, the category maps to a `Claim.product`, at least one approved phrase exists, and no live safety flag is set on the LATEST inbound, the orchestrator builds the deterministic Hinglish reply (literal approved phrase + ₹3000/30-capsules/₹499 advance + conservative usage + doctor escalation) and dispatches it through `services.send_freeform_text_message` — every existing gate (limited-mode allow-list, consent, CAIO, idempotency) stays in force. The orchestrator also handles discount/price-objection inbounds via the existing `build_objection_aware_reply` (audit `whatsapp.ai.objection_reply_used` + `outcome.notes` carries `deterministic_objection_fallback_used`) so the real-inbound path mirrors the CLI's priority order. **Latest-inbound safety isolation** is hardened: the `whatsapp.ai.safety_downgraded` audit now carries `latest_inbound_message_id`, `latest_inbound_safety_flags`, `history_safety_flags`, and `history_safety_ignored_for_current_safe_query=true` so the operator can see at a glance when older synthetic scenario messages biased the LLM's classification but the corrector flipped flags down based on the latest inbound text. Real safety phrases in the LATEST inbound (medicine khane ke baad ulta asar / chest pain / consumer forum / lawyer) still block exactly as before; the safety stack runs first, the fallback never sees them. **Audit emits**: `whatsapp.ai.deterministic_grounded_reply_used` (or `objection_reply_used`) on success carries `phone_suffix` last-4 only, `category`, `normalized_claim_product`, `claim_row_count`, `approved_claim_count`, `fallback_reason`, `final_reply_source`, `outbound_message_id`, `inbound_message_id`, `used_approved_phrases`, `triggered_by`, `force_auto_reply`, `trigger_path="real_inbound_webhook"`. `whatsapp.ai.auto_reply_flag_path_used` payload extended with `deterministic_fallback_used`, `final_reply_source`, `inbound_message_id`, `outbound_message_id`, `normalized_claim_product`, `claim_vault_used`. The orchestrator flips `decision.safety["claimVaultUsed"]=True` after a successful fallback (truthful — the validator confirmed the reply text literally embeds an approved phrase), so downstream readers (CLI report, audit ledger, OrchestrationOutcome consumers) see the corrected flag. **Hard rules preserved**: zero mutation of `Order` / `Payment` / `Shipment` / `DiscountOfferLog` from any fallback path (asserted with before/after counts in tests); the fallback NEVER runs when `WHATSAPP_AI_AUTO_REPLY_ENABLED=false` and `force_auto_reply=False` (suggestion-only path preserved); the fallback NEVER bypasses the limited-mode allow-list guard, consent, CAIO, or idempotency; non-allowed numbers still emit `whatsapp.ai.auto_reply_guard_blocked` + `whatsapp.ai.deterministic_grounded_reply_blocked`; the broad-automation flags (`callHandoff / lifecycle / rescue / RTO / reorder`) remain LOCKED OFF; campaigns remain LOCKED. **The CLI controlled-test command** (`run_controlled_ai_auto_reply_test`) detects the orchestrator-driven fallback via `outcome.notes` (`deterministic_grounded_fallback_used` / `deterministic_objection_fallback_used`) and propagates `finalReplySource` / `deterministicFallbackUsed` / `claimVaultUsed=true` / `fallbackReason` into its JSON report. Validation now runs against the actual outbound body (not just the LLM's `replyText`) so deterministic-fallback runs see truthful `containsApprovedClaim` / `blockedPhraseFree` results. **13 new backend tests; 847 backend + 13 frontend, all green.** Phase 5F (broadcast campaigns / growth automation) remains LOCKED. **Phase 5F-Gate Limited Auto-Reply Flag Plan** ships **read-only inspector tooling + audit hardening** for the imminent flip of the global `WHATSAPP_AI_AUTO_REPLY_ENABLED=true` env flag, scoped tightly to the existing limited-test allow-list. Two new strictly-read-only management commands. (a) `python manage.py inspect_whatsapp_auto_reply_gate --json` reports every gate the real-inbound webhook auto-reply path depends on — `provider`, `limitedTestMode`, `autoReplyEnabled`, `allowedListSize`, `allowedNumbersMasked` (last-4 only), `wabaSubscription` (active / count / warning / error), `finalSendGuardActive`, `consentRequired`, `claimVaultRequired`, `blockedPhraseFilterActive`, `medicalSafetyActive`, the six broad-automation flags (`callHandoffEnabled / lifecycleEnabled / rescueDiscountEnabled / rtoRescueEnabled / reorderEnabled` — all must remain false during the gate), `campaignsLocked`, plus a `readyForLimitedAutoReply` boolean, a typed `blockers` list, and a typed `nextAction` ∈ {`ready_to_enable_limited_auto_reply_flag`, `keep_auto_reply_disabled_fix_blockers`, `limited_auto_reply_enabled_monitor_real_inbound`}. (b) `python manage.py inspect_recent_whatsapp_auto_reply_activity --hours N --json` is a soak monitor that counts AI activity in the last `N` hours: `inboundAiRunStartedCount`, `replyAutoSentCount`, `replyBlockedCount`, `suggestionStoredCount`, `handoffRequiredCount`, `deterministicBuilderUsedCount`, `objectionReplyUsedCount`, `autoReplyFlagPathUsedCount`, `autoReplyGuardBlockedCount`, `messageDeliveredCount`, `messageReadCount`. Forensic outbound check scans every outbound `WhatsAppMessage.sent_at >= since` with a non-empty `provider_message_id` and counts `unexpectedNonAllowedSendsCount` against the live `WHATSAPP_LIVE_META_ALLOWED_TEST_NUMBERS`. Mutation safety check counts `Order` / `Payment` / `Shipment` / `DiscountOfferLog` rows created in the window. `nextAction` ∈ {`rollback_auto_reply_flag` (any non-allowed leak), `limited_auto_reply_enabled_monitor_real_inbound` (healthy), `no_recent_ai_activity_in_window`, `review_blocked_or_suggestion_paths`}. Both commands NEVER mutate the DB, NEVER write audit rows, NEVER print tokens / verify token / app secret; phones are masked to last-4 (`+91*****99001`). Two new audit kinds at the orchestrator level — `whatsapp.ai.auto_reply_flag_path_used` (emitted only when `WHATSAPP_AI_AUTO_REPLY_ENABLED=true` AND a real customer inbound triggered the auto-send, never on CLI-forced runs) + `whatsapp.ai.auto_reply_guard_blocked` (emitted when the final-send limited-mode allow-list guard refuses an outbound). The orchestrator now stamps `ai_state.lastTriggeredBy` and `ai_state.lastForceAutoReply` on every run so downstream readers / soak inspectors can distinguish CLI-forced runs from real flag-driven runs. Hard rules preserved: zero mutation of `Order` / `Payment` / `Shipment` / `DiscountOfferLog` from any inspector path; campaigns / broadcasts / lifecycle automation / call handoff / rescue / RTO / reorder remain LOCKED OFF; final-send limited-mode allow-list guard still blocks any send that targets a phone outside `WHATSAPP_LIVE_META_ALLOWED_TEST_NUMBERS`; inspectors gracefully handle missing Meta credentials. **16 new backend tests; 834 backend + 13 frontend, all green.** Phase 5F (broadcast campaigns / growth automation) remains LOCKED. **Phase 5F-Gate Internal Allowed-Number Cohort Tooling** ships **safe management-command tooling for expanding the controlled live test from one allowed number to a tiny internal cohort of 2–3 staff numbers** without unlocking any broad automation. Three new commands: (a) `python manage.py inspect_whatsapp_internal_cohort --json` reads `WHATSAPP_LIVE_META_ALLOWED_TEST_NUMBERS` and reports per-number readiness for controlled scenario tests — `customerFound`, `consentState`, `conversationFound`, `latestInbound/Outbound`, `latestAuditAt`, `readyForControlledTest`, `missingSetup` list, plus a global flag snapshot (`autoReplyEnabled`, `callHandoffEnabled`, `lifecycleEnabled`, `rescueDiscountEnabled`, `rtoRescueEnabled`, `reorderEnabled`) and the WABA subscription summary. Phone numbers are masked to last-4 by default; `--show-full-numbers` is an operator-only flag that surfaces full E.164 with a "do not paste publicly" warning. The inspector is strictly read-only — never sends, never mutates the DB, never writes audit rows. (b) `python manage.py prepare_whatsapp_internal_test_number --phone +91XXXXXXXXXX --name "Internal Staff Name" --source internal_cohort_test --json` registers a number that the operator has ALREADY added to `WHATSAPP_LIVE_META_ALLOWED_TEST_NUMBERS` in `.env.production`. Refuses outright if the phone is not on the allow-list. Creates / reuses a `Customer` row, sets `consent_whatsapp=True`, grants `WhatsAppConsent.consent_state="granted"` (with `revoked_at=None`, source / metadata / `phone_suffix` only), writes one new `whatsapp.internal_cohort.number_prepared` audit row. NEVER sends a WhatsApp message, NEVER creates / mutates `Order` / `Payment` / `Shipment` / `DiscountOfferLog`. JSON output masks the phone to last-4; full E.164 NEVER appears in JSON or audit payload. (c) `python manage.py run_whatsapp_internal_cohort_dry_run --json` loops the allow-list and reports per-number scenario readiness (`normal_product_info_ready`, `discount_objection_ready`, `safety_block_ready`, `legal_block_ready`, `human_request_ready`) without sending or mutating. After running the prepare command for each cohort number, the operator runs `run_controlled_ai_auto_reply_test --phone +91XXXXXXXXXX --message "…" --send --json` per number with the existing scenario matrix — the existing safety stack, intent classifier, deterministic grounded / objection-aware fallbacks, final-send limited-mode guard, and audit ledger all stay in force. **Hard rules preserved**: zero broad auto-reply unlock; zero campaigns / broadcasts / lifecycle automation / call handoff / rescue / RTO / reorder enabled; zero mutation of `Order` / `Payment` / `Shipment` / `DiscountOfferLog` from any cohort tooling path; full phone numbers never committed to docs / git / audit payloads; the cohort starts with 2–3 numbers only and is gated by `WHATSAPP_LIVE_META_LIMITED_TEST_MODE=true`. **One new audit kind** (`whatsapp.internal_cohort.number_prepared`); audit payloads carry phone last-4 only, never tokens / verify token / app secret. **19 new backend tests; 818 backend + 13 frontend, all green.** Phase 5F (broadcast campaigns / growth automation) remains LOCKED. **Phase 5F-Gate Objection & Handoff Reason Refinement** shipped the **deterministic intent classifier + objection-aware reply builder + human-request handoff** for the controlled AI auto-reply test command. The Phase 5F-Gate Scenario Matrix Test passed with two refinements needed: (a) discount objections (`thoda mehenga` / `kuch kam ho sakta hai`) were safely refused but the reply was a generic product-info reply instead of an objection-aware one; (b) human-call requests (`mujhe call karwa do, AI se baat nahi`) returned `claim_vault_not_used` instead of a typed `human_advisor_requested` handoff. New `apps.whatsapp.grounded_reply_builder` exports: `classify_inbound_intent(inbound)` → `IntentResult(primary ∈ {unsafe, human_request, discount_objection, product_info, unknown}, discount_objection, objection_type ∈ {discount, price, ""}, purchase_intent, human_request, unsafe, matched)`; `detect_discount_objection`, `detect_human_request`, `detect_purchase_intent`, `detect_unsafe_signal` deterministic helpers; `can_build_objection_reply(...)` eligibility gate that requires a discount-objection signal AND fails closed on unsafe vocabulary; `build_objection_aware_reply(normalized_product, approved_claims, inbound_text, purchase_intent)` composes a Hinglish reply that opens with a short price-concern acknowledgement, embeds the first approved Claim Vault phrase + ₹3000/30-capsules/₹499 advance, then explicitly states no upfront concession is promised and product is supported per approved process — followed by remaining approved phrases, a soft next-step invitation (with purchase-intent-aware wording), conservative usage line, and doctor-escalation; `validate_objection_reply(...)` extends the grounded validator with `objectionPromisedDiscount` / `objectionPromisedDiscountTerm` flags that reject `discount confirmed` / `guaranteed discount` / `50% discount` framing. Controlled-test command priority order: (1) safety blockers (handled by orchestrator); (2) human request — short-circuits the orchestrator with `blockedReason="human_advisor_requested"` + `handoffReason="human_advisor_requested"` + `nextAction="human_handoff_requested"` + `safetyBlocked=false` + a clean `whatsapp.ai.handoff_required` audit row carrying `reason=human_advisor_requested` (NEVER `claim_vault_not_used`); Vapi handoff stays gated by `WHATSAPP_CALL_HANDOFF_ENABLED=false`; (3) unknown category / no Claim Vault → fail closed unchanged; (4) discount/price objection with grounding → objection-aware fallback; (5) normal product-info → existing grounded fallback; (6) else → fail closed. Four new audit kinds (`whatsapp.ai.objection_detected`, `whatsapp.ai.objection_reply_used`, `whatsapp.ai.objection_reply_blocked`, `whatsapp.ai.human_request_detected`); audit payloads NEVER carry tokens. Controlled-test JSON gains `detectedIntent`, `objectionDetected`, `objectionType`, `purchaseIntentDetected`, `humanRequestDetected`, `handoffReason`, `safetyReason`, and a `replyPolicy` block (`upfrontDiscountOffered=false`, `discountMutationCreated=false`, `businessMutationCreated=false`); `finalReplySource` extends to include `"deterministic_objection_reply"` and `"blocked_handoff"`. Hard rules preserved: NO mutation of `Order` / `Payment` / `Shipment` / `DiscountOfferLog` from any controlled-test path; objection reply NEVER mentions a confirmed / guaranteed discount; cure / guarantee / unsafe-claim demand inside an objection sentence still routes to the safety stack (unsafe wins); side-effect / legal / refund / unknown-category still fail closed unchanged; final-send limited-mode guard still refuses non-allowed numbers. **52 new backend tests; 799 backend + 13 frontend, all green.** Phase 5F (broadcast campaigns / growth automation) remains LOCKED. **Phase 5F-Gate Deterministic Grounded Reply Builder** shipped the **backend deterministic Claim-Vault-grounded reply fallback** for the controlled AI auto-reply test harness. After the Confidence Fix, the live `--send` (`WAM-100008`) STILL failed with `claim_vault_not_used` even though the backend proved `claimRowCount=1 / approvedClaimCount=3 / promptGroundingInjected=true / businessFactsInjected=true`. Root cause: relying only on the LLM's self-reported `safety.claimVaultUsed=false` (despite valid backend grounding) is insufficient. New `apps.whatsapp.grounded_reply_builder` module ships three pure functions: (a) `is_normal_product_info_inquiry(inbound_text)` deterministic intent detector with explicit keyword + disqualifier lists; (b) `can_build_grounded_product_reply(...)` eligibility gate (category mapped, approved claims present, no safety flag set, intent qualified); (c) `build_grounded_product_reply(normalized_product, approved_claims, inbound_text, customer_name)` composes a Hinglish reply that literally embeds the first approved phrase + ₹3000/30-capsules/₹499 advance + conservative usage guidance + doctor escalation, then re-validates via `validate_reply_uses_claim_vault` (must contain at least one approved phrase, no blocked phrase, no discount vocabulary). The controlled-test command (`run_controlled_ai_auto_reply_test`) now calls this fallback **only** when the LLM blocked with one of the soft non-safety reasons (`claim_vault_not_used / low_confidence / ai_handoff_requested / auto_reply_disabled / no_action`) AND backend grounding is valid AND the inbound is a normal product-info inquiry; safety blockers (`medical_emergency / side_effect_complaint / legal_threat / blocked_phrase / limited_test_number_not_allowed`) NEVER trigger fallback. The fallback dispatches via the existing `services.send_freeform_text_message` path so the limited-mode allow-list guard, consent, CAIO, and idempotency gates all stay in force. Two new audit kinds (`whatsapp.ai.deterministic_grounded_reply_used`, `whatsapp.ai.deterministic_grounded_reply_blocked`); audit payloads NEVER carry tokens. Controlled-test JSON gains `deterministicFallbackUsed`, `fallbackReason`, `deterministicReplyPreview`, `finalReplySource` (`"llm"` or `"deterministic_grounded_builder"`), and `finalReplyValidation` (`containsApprovedClaim` / `blockedPhraseFree` / `discountOffered` / `safeBusinessFactsOnly` / `passed`). Hard rules preserved: `claimVaultUsed` flips to `true` ONLY because the validator confirmed the reply text literally embeds an approved phrase; the global `WHATSAPP_AI_AUTO_REPLY_CONFIDENCE_THRESHOLD` is unchanged; the orchestrator stays strict for webhook-driven runs (this fallback is controlled-test-only). **24 new backend tests; 747 backend + 13 frontend, all green.** Phase 5F (broadcast campaigns / growth automation) remains LOCKED. **Phase 5F-Gate Controlled Reply Confidence Fix** shipped the **prompt + action discipline** so the live grounded controlled-test reply now actually dispatches: the deployed Claim Vault Grounding Fix correctly mapped `weight-management → Weight Management` and surfaced the Claim row in the prompt, but the LLM still returned `action=handoff` with `confidence=0.7` because the prompt did not tell it explicitly that a normal grounded inquiry must use `action=send_reply` with `confidence ≥ 0.85`. Three pieces. (a) **Business facts injected**: the system prompt now carries an explicit `BUSINESS FACTS YOU MAY STATE FREELY` section listing standard price ₹3000 / 30 capsules, ₹499 fixed advance, conservative usage guidance ("use as directed on label / qualified practitioner"), and the consult-doctor escalation rule for pregnancy / serious illness / allergies / existing medication / adverse reaction. The prompt-context `settings` block also gains `standardCapsuleCount=30`, `currency=INR`, `discountDiscipline` description, and a `businessFactsAllowed` whitelist. Business facts are routing labels and price/quantity facts — they are NOT medical claims. Medical / product-benefit content still comes only from `Claim.approved`. (b) **Action discipline**: the system prompt now carries an explicit `ACTION SELECTION DECISION TREE` (cases A–E mapping grounded-inquiry, no-claim-vault, unknown-category, confirmed-booking, and safety-vocabulary inbounds to the canonical action) and a `FINAL CHECK` paragraph at the end of the schema instructions stating "if the claims block has at least one APPROVED phrase AND every safety flag is false AND the inbound is a normal product/price/quantity/use-guidance question, then you MUST use action='send_reply', confidence ≥ 0.85, safety.claimVaultUsed=true, and replyText must literally include one of the APPROVED phrases — defaulting to action='handoff' on a grounded inquiry is a defect." Confidence threshold (`WHATSAPP_AI_AUTO_REPLY_CONFIDENCE_THRESHOLD=0.75`) is unchanged. (c) **Diagnostics cleanup**: the ambiguous `claim_count` (which sometimes meant rows and sometimes phrases) is split into `claim_row_count` / `approved_claim_count` / `disallowed_phrase_count` across the controlled-test JSON output and the `whatsapp.ai.{category_detected,reply_blocked,handoff_required,controlled_test.blocked}` audit payloads. `claim_count` is preserved as a backward-compat alias for `approved_claim_count`. Controlled-test JSON also adds `confidenceThreshold`, `actionReason`, `sendEligibilitySummary`, and `groundingStatus.{claimRowCount, businessFactsInjected}`. **12 new backend tests; 723 backend + 13 frontend, all green.** Phase 5F (broadcast campaigns / growth automation) remains LOCKED. **Phase 5F-Gate Claim Vault Grounding Fix** shipped the **category-slug → `Claim.product` mapping fix**: the deployed Controlled AI Auto-Reply Test Harness produced two safety-correct blocks on the VPS — first by `low_confidence`, then by `claim_vault_not_used` (`nextAction=blocked_for_unapproved_claim`). Root cause confirmed: `_claims_for_category("weight-management", customer)` ran `Claim.objects.filter(product__icontains="weight-management")` against `Claim(product="Weight Management")` — hyphen vs space — and silently fell through to `product__icontains=customer.product_interest or ""` which **returned every claim row** when the customer's interest was blank. Fix lives in the new `apps.whatsapp.claim_mapping.category_to_claim_product` deterministic table (eight canonical slugs + ~25 aliases like `weight-loss / weight loss / blood purify / male wellness / joint pain`) and `_claims_for_category` is rewritten to use `Claim.product__iexact=normalized_product`, with the empty-string-matches-everything fallback removed. Unknown / empty category fails closed (returns `[]`). Disallowed phrases stay attached to the prompt context, so the avoid list still reaches the LLM. The `whatsapp.ai.controlled_test.blocked` audit + the controlled-test JSON output now carry a full diagnostics block (`detectedCategory / normalizedClaimProduct / claimCount / confidence / action / replyPreview / safetyFlags / groundingStatus.{claimProductFound, approvedClaimCount, disallowedPhraseCount, promptGroundingInjected}`) so the operator can see why a block fired without re-running. The `whatsapp.ai.category_detected`, `whatsapp.ai.reply_blocked`, and `whatsapp.ai.handoff_required` audits gain `category / normalized_claim_product / claim_count / confidence` fields. **40 new backend tests; 711 backend + 13 frontend, all green.** Phase 5F (broadcast campaigns / growth automation) remains LOCKED. **Phase 5F-Gate Controlled AI Auto-Reply Test Harness** shipped the **scoped one-shot live AI reply path** for the allowed test number only — without flipping the global `WHATSAPP_AI_AUTO_REPLY_ENABLED` env (which stays `false` everywhere else). Two pieces: (a) a new **final-send limited-mode guard** in `apps.whatsapp.services._limited_test_mode_blocks_send` runs inside both `services.send_freeform_text_message` (AI auto-reply path) and `services.queue_template_message` (template path) — when `WHATSAPP_PROVIDER=meta_cloud` AND `WHATSAPP_LIVE_META_LIMITED_TEST_MODE=true`, every customer-facing send must target a phone on `WHATSAPP_LIVE_META_ALLOWED_TEST_NUMBERS` or it raises `WhatsAppServiceError(block_reason="limited_test_number_not_allowed")` + writes `whatsapp.send.blocked` audit; (b) `apps.whatsapp.ai_orchestration.run_whatsapp_ai_agent` gains a new `force_auto_reply: bool = False` kwarg so a trusted CLI caller can flip the auto-reply gate ON for one orchestrator call only — every other safety check (Claim Vault, blocked phrase, safety flags, CAIO, matrix, idempotency, the limited-mode final-send guard) still runs. The new `python manage.py run_controlled_ai_auto_reply_test --phone +91… --message "…" [--dry-run|--send] [--json]` command stacks every gate (provider, limited mode, automation flags off including `WHATSAPP_AI_AUTO_REPLY_ENABLED`, allow-list, customer + consent, WABA active), persists ONE synthetic inbound `WhatsAppMessage` per real run, and drives the orchestrator with `force_auto_reply=True`. Default is `--dry-run` (returns `nextAction=dry_run_passed_ready_for_send` without persisting an inbound or hitting the LLM); `--send` runs the full orchestrator + AI dispatch + provider call, returns `nextAction=live_ai_reply_sent_verify_phone` on success or a typed amber action (`add_number_to_allowed_list / grant_consent_on_test_number / enable_meta_cloud_provider / enable_limited_test_mode / disable_automation_flags / fix_waba_subscription / fix_claim_vault_coverage / blocked_for_medical_safety / blocked_for_unapproved_claim / blocked_by_limited_mode_guard / inspect_live_test`). Five new audit kinds (`whatsapp.ai.controlled_test.{started,dry_run_passed,sent,blocked,completed}`); audit payloads NEVER carry tokens, phone is masked to last 4 digits, body previewed at 120 chars. **15 new backend tests; 671 backend + 13 frontend, all green.** Phase 5F (broadcast campaigns / growth automation) remains LOCKED. **Phase 5F-Gate Hardening Hotfix** ships the **post-live-pass diagnostics layer**: the limited live Meta one-number test passed end-to-end on the VPS (`nrg_greeting_intro / hi / UTILITY / APPROVED`, outbound `WAM-100003` sent + delivered to phone, then inbound `WAM-100004` "Namaste webhook test" stored after fixing the empty `GET /{WABA}/subscribed_apps` by `POST` + override-callback). Three gaps surfaced during the run, all fixed here: (1) the second allowed-number send hit a duplicate `idempotency_key` IntegrityError that crashed the CLI with a traceback — now trapped, returns clean JSON with `duplicateIdempotencyKey=true / alreadyQueued|alreadySent / existingMessageId / nextAction=inspect_existing_message` and a new `whatsapp.meta_test.duplicate_idempotency` audit row; (2) `--check-webhook-config` now also runs `GET /{WABA}/subscribed_apps` against Meta Graph and reports `wabaSubscriptionChecked / wabaSubscriptionActive / wabaSubscribedAppCount / wabaSubscriptionWarning / wabaSubscriptionError` + emits a new `whatsapp.meta_test.webhook_subscription_checked` audit row, with `nextAction=subscribe_waba_to_app_webhooks` when `data=[]`; (3) new strictly-read-only `python manage.py inspect_whatsapp_live_test --phone +91XXXXXXXXXX --json` command surfaces customer / consent / conversation / latest outbound + inbound messages / webhook envelopes / status events / WABA subscription / latest `whatsapp.*` audit rows + a typed `nextAction` (run_one_number_send / verify_inbound_webhook_callback / subscribe_waba_to_app_webhooks / observe_status_events_optional / gate_hardened_ready_for_limited_ai_auto_reply_plan). Inspector NEVER mutates DB, NEVER prints tokens / verify token / app secret, gracefully handles missing Meta credentials. **13 new backend tests; 656 backend + 13 frontend, all green.** **Phase 5F (broadcast campaigns / growth automation) remains LOCKED** until the inspector reports `gate_hardened_ready_for_limited_ai_auto_reply_plan` on a real VPS run. **Phase 5F-Gate** ships the **Limited Live Meta WhatsApp One-Number Test harness**: new `apps.whatsapp.meta_one_number_test` module + `python manage.py run_meta_one_number_test` command verify real Meta Cloud API sending against exactly one approved test number, without enabling AI auto-reply, broadcasts, lifecycle automation, rescue / RTO / reorder automation, or freeform sends. Hard stops are stacked: provider must be `meta_cloud`, `WHATSAPP_LIVE_META_LIMITED_TEST_MODE=true`, destination must be in `WHATSAPP_LIVE_META_ALLOWED_TEST_NUMBERS`, template must be `APPROVED + active + UTILITY/AUTHENTICATION` (MARKETING tier refused), every automation flag must remain off — failing any gate refuses the run with a typed `nextAction`. Defaults to `--dry-run`; `--send` is required for a real dispatch and refuses if any safety gate is amber. New options: `--to`, `--template`, `--action-key`, `--language`, `--dry-run`, `--send`, `--verify-only`, `--json`, `--check-webhook-config`. Eight new audit kinds (`whatsapp.meta_test.{started,config_ok,config_failed,blocked_number,template_missing,sent,failed,completed}`); audit payloads NEVER carry tokens (`token`-keyed entries are stripped before persistence). The webhook callback URL printer surfaces `https://ai.nirogidhara.com/api/webhooks/whatsapp/meta/` + verify-token / app-secret presence + the subscribed-fields list so the operator can wire the Meta Developer Console without reading source. **24 new backend tests; 643 backend + 13 frontend, all green.** Phase 5F (broadcast campaigns / growth automation) remains LOCKED until this gate passes on a live test number. **Phase 5E-Smoke-Fix-3** ships the **false-positive safety classification fix**: the VPS OpenAI smoke run reported `overallPassed=false` because the orchestrator wrongly classified `Hi mujhe weight loss product ke baare me batana` as a `side_effect_complaint`. New `apps.whatsapp.safety_validation.validate_safety_flags(inbound_text, safety_flags)` runs server-side immediately before `_safety_block` in `apps.whatsapp.ai_orchestration` — for each blocker flag the LLM set to true (`sideEffectComplaint`, `medicalEmergency`, `legalThreat`), it checks whether the inbound text actually contains the corresponding signal vocabulary (English + Hindi + Hinglish). Flags with no matching vocabulary are flipped to false and a `whatsapp.ai.safety_downgraded` audit row is emitted; flags whose vocabulary IS present (`medicine khane ke baad ulta asar`, `chest pain`, `consumer forum`, `lawyer`) stay flagged exactly as the LLM said. `angryCustomer` and `claimVaultUsed` are never touched. The LLM prompt now carries an explicit `SAFETY FLAG DISCIPLINE` block listing required vocabulary for each flag and stating that a normal product / price / availability inquiry leaves all safety flags false. **28 new backend tests** prove false positives are scrubbed across all three flags in three languages, real safety phrases stay flagged, and the corrector never promotes false→true. **619 backend + 13 frontend tests, all green.** VPS rebuild required so the new orchestrator + prompt land in the container; after rebuild the OpenAI smoke run is expected to report `overallPassed=true`.

**Phase 5E-Smoke-Fix-2** ships the **OpenAI Chat Completions token-parameter compatibility hotfix**: extracts `apps.integrations.ai.openai_client.build_request_kwargs(messages, model, config)` so the SDK call shape is unit-testable. Modern OpenAI Chat models (gpt-4o, gpt-5, o1, o3, …) reject the legacy `max_tokens` parameter and require `max_completion_tokens`; the adapter now always sends `max_completion_tokens` and **never** sends `max_tokens` — the two are never sent together. Zero / unset `max_tokens` from `AIConfig` drops the key entirely so OpenAI doesn't reject an explicit null. **10 new backend tests** pin the kwargs shape, prove no `max_tokens` leaks, and confirm the smoke harness reports `safeFailure=true` when OpenAI rejects an unsupported parameter (the exact failure mode that triggered this hotfix). **591 backend + 13 frontend, all green.**

**Phase 5E-Smoke-Fix** ships the **OpenAI SDK dependency + provider-success semantics for the smoke harness**: `openai>=1.0,<2.0` added to `backend/requirements.txt` (the existing OpenAI adapter already targets the v1 SDK shape — `from openai import OpenAI; client.chat.completions.create(...)`). Smoke harness `ai-reply` scenario now sets four new detail fields when `--use-openai` is passed — `openaiAttempted`, `openaiSucceeded`, `providerPassed`, `safeFailure`. A safe-failure (adapter raised but the customer send stayed safely blocked) is still safety-correct but **does NOT count as a pass**: the scenario reports `passed=false`, `safeFailure=true`, and `overallPassed=false` so operators cannot miss "the SDK isn't installed" / "the API key is wrong" while everything else looks fine. Pre-seeds an outbound on the smoke conversation so the greeting fast-path never short-circuits LLM dispatch — the scripted Hindi/Hinglish/English inbounds (which all start with greeting words) now reliably exercise the adapter path. **6 new backend tests; 579 backend + 13 frontend, all green.**

**Phase 5E-Smoke** ships the **Controlled Mock + OpenAI Smoke Testing Harness**: new `apps.whatsapp.smoke_harness` module + `python manage.py run_controlled_ai_smoke_test` command exercise the WhatsApp AI orchestrator, Claim Vault gates, rescue discount cap math (50% absolute), Vapi handoff (mock mode), and Day-20 reorder sweep without sending any real customer message. Defaults are SAFE: `--dry-run`, `--mock-whatsapp`, `--mock-vapi`, OpenAI off (deterministic mocked LLM decision). Five scenarios — `ai-reply` / `claim-vault` / `rescue-discount` / `vapi-handoff` / `reorder-day20` / `all`. Four new audit kinds (`system.smoke_test.{started,completed,failed,warning}`). Refuses real Meta provider outright. Outputs `--json` for CI / log scraping. **23 new backend tests; 573 backend + 13 frontend, all green.** **Phase 5E-Hotfix-2** strengthens the demo Claim Vault seed so all eight categories report `risk=demo_ok` (not `weak`) on coverage. Adds four universal safe usage-guidance phrases (used as directed on the label / hydration + balanced diet / consult doctor for serious cases / discontinue on adverse reaction) merged into every demo entry; widens `USAGE_HINT_KEYWORDS` to recognize "directed", "label", "practitioner", "hydration", "balanced diet", "routine", "discontinue", "professional advice", "unusual reaction"; bumps the demo seed marker to `version="demo-v2"`. Idempotent — `--reset-demo` is required to upgrade demo-v1 rows in place; real admin / doctor-approved claims are still never overwritten. **550 backend tests + 13 frontend tests, all green.** Production still requires real doctor-approved final claims before full live rollout — automation flags remain OFF until controlled mock + OpenAI testing passes. **Phase 5E-Hotfix** adds two `RenameIndex` migrations (`apps/orders/migrations/0004_rename_orders_disc_order_i_dol_idx_orders_disc_order_i_e49f63_idx_and_more.py` + `apps/whatsapp/migrations/0004_rename_whatsapp_wh_convers_h0_idx_whatsapp_wh_convers_ae1708_idx_and_more.py`) to sync the index names from Phase 5D / 5E hand-rolled migrations to Django's auto-generated suffix names. Surfaced when the VPS reported "models in app(s) 'orders', 'whatsapp' have changes that are not yet reflected in a migration" after pulling commit `8374863`. Migrations are pure metadata renames — no schema rewrite, no data move. Working agreement now requires `python manage.py makemigrations --check --dry-run` to be clean before every commit. Phase 5E ships **Rescue Discount Flow + Day-20 Reorder + Default Claim Vault Seeds**: new `apps.orders.rescue_discount` is the single source of truth for AI rescue discount math — `get_current_total_discount_pct` / `get_discount_cap_remaining` / `validate_total_discount_cap` enforce a locked **50% cumulative cap** across confirmation / delivery / RTO / reorder stages, `calculate_rescue_discount_offer` walks a per-stage ladder (confirmation 5/10/15, delivery 5/10, RTO 10/15/20 with high-risk step-up, reorder 5), and `create_rescue_discount_offer` writes a new `apps.orders.DiscountOfferLog` row in every outcome (offered / accepted / rejected / blocked / skipped / `needs_ceo_review`). Over-cap or above-auto-band requests automatically mint a CEO AI / admin `ApprovalRequest` via two new matrix rows (`discount.rescue.ceo_review`, `discount.above_safe_auto_band`). Customer acceptance applies the discount through the existing `apps.orders.services.apply_order_discount` service path — Phase 4D / 4E discount handlers stay the only DB mutation point. Phase 5C orchestrator now also persists a `DiscountOfferLog` row on every WhatsApp AI discount proposal so the orders / analytics surfaces see the offer regardless of channel. New `apps.whatsapp.lifecycle` triggers — `whatsapp.confirmation_rescue_discount`, `whatsapp.delivery_rescue_discount`, `whatsapp.rto_rescue_discount`, `whatsapp.reorder_day20_reminder` — flow through the Phase 5A `queue_template_message` pipeline (consent + Claim Vault + matrix + CAIO + idempotency stays in force) and emit dedicated `whatsapp.lifecycle.rescue_discount_{queued,sent}` + `whatsapp.lifecycle.reorder_day20_{queued,sent}` audits. Day-20 reorder cadence: new `apps.whatsapp.reorder.run_day20_reorder_sweep` + `python manage.py run_reorder_day20_sweep` + `send_whatsapp_lifecycle_message_task` + `apps.whatsapp.tasks.run_reorder_day20_sweep_task` cover delivered orders 20–27 days old, idempotent on the lifecycle key. `python manage.py seed_default_claims` (idempotent, with `--reset-demo` flag) sows conservative non-cure / non-guarantee Claim Vault rows for the eight current categories (Weight Management, Blood Purification, Men/Women Wellness, Immunity, Lungs Detox, Body Detox, Joint Care); demo rows are flagged `version="demo-v1"` and surface in coverage reports as `risk=demo_ok` with a "replace before live rollout" note. Five new endpoints — `GET / POST /api/orders/{id}/discount-offers/{,rescue/,{offer_id}/accept/,{offer_id}/reject/}`, `GET /api/whatsapp/reorder/day20/status/`, `POST /api/whatsapp/reorder/day20/run/`. Twelve new audit kinds (`discount.offer.{created,sent,accepted,rejected,blocked,needs_ceo_review}`, `whatsapp.lifecycle.{rescue_discount_queued,rescue_discount_sent,reorder_day20_queued,reorder_day20_sent}`, `compliance.default_claims.seeded`). Four new env vars (all default safe — `WHATSAPP_RESCUE_DISCOUNT_ENABLED=false`, `WHATSAPP_RTO_RESCUE_DISCOUNT_ENABLED=false`, `WHATSAPP_REORDER_DAY20_ENABLED=false`, `DEFAULT_CLAIMS_SEED_DEMO_ONLY=true`). Frontend adds a Rescue Discount cap card to the AI Chat panel (current %, cap remaining, ask count) + new TS types (`DiscountOffer`, `DiscountOfferListResponse`, `CreateRescueOfferPayload`, `ReorderDay20StatusResponse`, `ReorderDay20RunResponse`) + six new api methods. Hard stops still in force: no medical-emergency replies, no freeform claims, no CAIO send, no shipment from chat, **no campaigns, no refunds, no ad-budget execution, never above 50% cumulative discount**. **38 new backend tests + 13 frontend tests, all green (536 backend total).**

Earlier (kept verbatim): Phase 5D shipped **Chat-to-Call Handoff + Lifecycle Automation**: new `apps.whatsapp.call_handoff` service routes the WhatsApp AI agent's handoff reasons (customer asked for a call / low-confidence / AI-handoff-requested) directly through the existing `apps.calls.services.trigger_call_for_lead` Vapi path — never the adapter — and writes a `WhatsAppHandoffToCall` row keyed on `(conversation, inbound_message, reason)` so the same inbound never fires two calls; safety reasons (medical_emergency / side_effect_complaint / legal_threat) record a `skipped` handoff row for human/doctor pickup instead of auto-dialing. AI-booked orders now move directly into the confirmation queue via `apps.orders.services.move_to_confirmation` (Phase 5C's `book_order_from_decision` does the move post-create; metadata records `confirmationMoveFailed` if it can't). New `apps.whatsapp.lifecycle` service + `apps.whatsapp.signals` listen on Order/Payment/Shipment `post_save` and route business events to approved templates (`whatsapp.confirmation_reminder`, `whatsapp.payment_reminder`, `whatsapp.delivery_reminder`, `whatsapp.usage_explanation`, `whatsapp.rto_rescue`) through Phase 5A's `queue_template_message` — every gate (consent + Claim Vault + approval matrix + CAIO + idempotency) stays in force, and a new `WhatsAppLifecycleEvent` table makes the dispatch idempotent on `lifecycle:{action}:{type}:{id}:{event}`. New Claim Vault coverage audit (`apps.compliance.coverage` + `check_claim_vault_coverage` mgmt cmd + `GET /api/compliance/claim-coverage/` admin endpoint) reports per-product `ok / weak / missing` risk and exits 1 in CI when products lack approved coverage; usage_explanation lifecycle template fails closed when coverage is missing. Three new endpoints: `POST /api/whatsapp/conversations/{id}/handoff-to-call/`, `GET /api/whatsapp/conversations/{id}/handoffs/`, `GET /api/whatsapp/lifecycle-events/`. Eleven new audit kinds (`whatsapp.handoff.{call_requested,call_triggered,call_failed,call_skipped,call_skipped_duplicate}`, `whatsapp.lifecycle.{queued,sent,blocked,skipped_duplicate,failed}`, `whatsapp.ai.order_moved_to_confirmation`, `compliance.claim_coverage.checked`). Four new env vars (`WHATSAPP_CALL_HANDOFF_ENABLED`, `WHATSAPP_LIFECYCLE_AUTOMATION_ENABLED`, `WHATSAPP_LIVE_META_LIMITED_TEST_MODE`, `WHATSAPP_LIVE_META_ALLOWED_TEST_NUMBERS`) — all default safe (handoff/lifecycle OFF, limited-test-mode ON). Frontend adds a "Call customer" button to the AI panel + new TS types (`WhatsAppHandoffToCall`, `WhatsAppLifecycleEvent`, `ClaimVaultCoverageReport`) + four new api methods. Hard stops still in force: no medical-emergency calls, no freeform claims, no CAIO send, no shipment from chat, no campaigns, no refunds, no ad-budget execution. **498 backend tests + 13 frontend tests, all green.**

Earlier (kept verbatim): Phase 5C shipped the WhatsApp AI Chat Sales Agent. Phase 5C ships the **WhatsApp AI Chat Sales Agent**: new `apps.whatsapp.ai_orchestration` runs on every inbound (Celery `run_whatsapp_ai_agent_for_conversation`) with deterministic language detection (Hindi / Hinglish / English in `apps.whatsapp.language`), locked Hindi greeting via approved UTILITY template, OpenAI dispatch through `apps.integrations.ai.dispatch`, strict JSON schema validator (`apps.whatsapp.ai_schema`), Claim Vault grounding, blocked-phrase filter, discount discipline + 50% total cap (`apps.whatsapp.discount_policy`), order booking from chat via `apps.orders.services.create_order` (+ ₹499 advance link via `apps.payments.services.create_payment_link` — uses `FIXED_ADVANCE_AMOUNT_INR` from Phase 3E), and auto-send rate gates (env-driven `WHATSAPP_AI_AUTO_REPLY_ENABLED` / `WHATSAPP_AI_AUTO_REPLY_CONFIDENCE_THRESHOLD` / `WHATSAPP_AI_MAX_TURNS_PER_CONVERSATION_PER_HOUR` / `WHATSAPP_AI_MAX_MESSAGES_PER_CUSTOMER_PER_DAY`). Six endpoints — `GET /api/whatsapp/ai/status/`, `PATCH /api/whatsapp/conversations/{id}/ai-mode/`, `POST /api/whatsapp/conversations/{id}/{run-ai,handoff,resume-ai}/`, `GET /api/whatsapp/conversations/{id}/ai-runs/`. 18 audit kinds (`whatsapp.ai.run_started/completed/failed/reply_auto_sent/reply_blocked/suggestion_stored/greeting_sent/greeting_blocked/language_detected/category_detected/address_updated/order_draft_created/order_booked/payment_link_created/handoff_required/discount_objection_handled/discount_offered/discount_blocked`).

Earlier (kept verbatim): Phase 5B shipped the Inbound WhatsApp Inbox + Customer 360 Timeline. Phase 5B ships the **Inbound WhatsApp Inbox + Customer 360 Timeline**: new `WhatsAppInternalNote` model, six new endpoints (`GET /api/whatsapp/inbox/`, `PATCH /api/whatsapp/conversations/{id}/`, `POST /api/whatsapp/conversations/{id}/mark-read/`, `GET + POST /api/whatsapp/conversations/{id}/notes/`, `POST /api/whatsapp/conversations/{id}/send-template/` routing through Phase 5A's `queue_template_message`, `GET /api/whatsapp/customers/{customer_id}/timeline/`), six new audit kinds (`whatsapp.conversation.opened/updated/assigned/read`, `whatsapp.internal_note.created`, `whatsapp.template.manual_send_requested`), conversation list filters (`unread=true`, `assignedTo`, `q`), conversation serializer extended with `customerName / customerPhone / assignedToUsername` + message serializer with `templateName`. Frontend ships a three-pane `/whatsapp-inbox` page (filters / conversation list / thread + internal notes + manual template send modal + AI-suggestions-disabled placeholder) with live refresh via Phase 4A `connectAuditEvents` filtered on `whatsapp.*`, plus a Customer 360 WhatsApp tab. **Manual-only — AI auto-reply / chat-to-call handoff / rescue discount / order booking from chat are all deferred to Phase 5C–5F.** Operations users send only approved templates; backend gates (consent + approved template + Claim Vault + approval matrix + CAIO hard stop + idempotency) remain final. **434 backend tests + 13 frontend tests, all green.**

**Production is LIVE at <https://ai.nirogidhara.com>.** Operational reference: `nd.md` §17 + `docs/DEPLOYMENT_VPS.md`. VPS folder `/opt/nirogidhara-command`, host port `18020 → 80`, six namespaced containers, host Ubuntu Nginx + Certbot terminate TLS. The first VPS deploy surfaced four issues that were patched directly on the server and are now committed to `main` (Phase 5B-Deploy hotfix sync — see `nd.md` §17 closing block): repo-root build context for the backend image, explicit `deploy/backend/entrypoint.sh` copy + CRLF normalisation + `chmod +x` in the Dockerfile, no-args default in the entrypoint (`set -e` only, defaults to `daphne ...` on empty `$@`), and the duplicate-index `calls_calltranscriptline_call_id_*` recovery procedure documented in both `nd.md` §17 and `docs/DEPLOYMENT_VPS.md` §8.5. **Do not change container / volume / network names or the `18020` host port** — host Nginx + SSL cert paths assume that exact shape. **Never commit `.env.production`** (gitignored).

**Phase 5B-Deploy** added the production Docker scaffold for `ai.nirogidhara.com` on Hostinger VPS: `docker-compose.prod.yml` (six isolated containers — `nirogidhara-db` Postgres + `nirogidhara-redis` + `nirogidhara-backend` Daphne ASGI + `nirogidhara-worker` Celery + `nirogidhara-beat` Celery beat + `nirogidhara-nginx` serving the Vite SPA), `backend/Dockerfile` (slim Python 3.11 + tini + non-root user; entrypoint waits for Postgres/Redis, runs migrate + collectstatic, then exec's the supplied command), `frontend/Dockerfile` (multi-stage node 20 → nginx alpine; bakes `VITE_API_BASE_URL=/api` + `VITE_WS_BASE_URL=""` so production stays same-origin), `deploy/nginx/nirogidhara.conf` (proxies `/api/`, `/admin/`, `/ws/` with WebSocket upgrade headers; serves SPA with hashed-asset caching + `index.html` no-cache), `.env.production.example` covering every settings env var, `docs/DEPLOYMENT_VPS.md` runbook (DNS, TLS via Certbot, smoke tests, backups, security checklist, resource-safety notes for the shared VPS). Host port `18020 → 80` to avoid conflict with Postzyo / OpenClaw. CSRF_TRUSTED_ORIGINS is now env-driven. New runtime deps: `psycopg[binary]`, `requests`. **Mock-mode defaults stay locked** — all integrations (WhatsApp / Razorpay / Delhivery / Vapi / Meta / AI provider) ship with `*_MODE=mock` / `disabled` so the first deploy never sends a live message. Existing 434 backend + 13 frontend tests stay green.

Earlier phases unchanged: Phase 5A shipped the WhatsApp Live Sender Foundation (`apps.whatsapp` Django app with 8 models, three providers — `mock` default + `meta_cloud` Nirogidhara-built Graph client + `baileys_dev` dev-only stub, service layer gating consent + approved template + Claim Vault + approval matrix + CAIO + idempotency, Celery `send_whatsapp_message` task with autoretry/backoff/jitter/max_retries=5, signed webhook at `/api/webhooks/whatsapp/meta/`, 9 read + 4 write API endpoints under `/api/whatsapp/`, `sync_whatsapp_templates` management command, frontend Settings → WABA section + read-only `/whatsapp-templates` page). All earlier phases (CRM, write APIs, four gateway integrations, AgentRun + 7 per-agent runtimes, Celery beat at 09:00 + 18:00 IST, OpenAI → Anthropic fallback, model-wise USD cost tracking, sandbox + versioned prompts + per-agent USD budgets, Scheduler + Governance pages, Phase 3E business config — catalog + discount policy 10/20% bands + ₹499 fixed advance + reward/penalty scoring + approval matrix, Phase 4B reward/penalty engine + CEO AI net accountability, Phase 4C approval-matrix middleware, Phase 4D Approved Action Execution Layer, Phase 4A real-time AuditEvent WebSockets, Phase 4E expanded execution registry — discount.up_to_10 / discount.11_to_20 / ai.sandbox.disable) remain green. **Phase 5A-0 + 5A-1** are the doc-only WhatsApp design phases; the integration plan in `docs/WHATSAPP_INTEGRATION_PLAN.md` (sections A–R + S–GG) drove the Phase 5A + 5B implementations. Next: Phase 5C WhatsApp AI Chat Sales Agent.

GitHub: https://github.com/prarit0097/Nirogidhara-AI-Command-Center

**Phase 6E note:** Phase 6D org-aware write assignment is **FULL PASS**.
Phase 6E adds the safe SaaS admin/integration-settings foundation:
`OrganizationIntegrationSetting`, masked secret-ref readiness selectors,
admin SaaS overview/integration APIs, diagnostics commands, and read-only
`/saas-admin`. Runtime providers still use env/config rather than DB settings;
raw secrets are rejected and never exposed; per-org runtime provider routing is
deferred to **Phase 6F Per-Org Runtime Integration Routing Plan**. Global
tenant filtering is still not blanket-enabled and WhatsApp flags remain
untouched/off.

**Phase 6F note:** **Phase 6F Per-Org Runtime Integration Routing Plan
shipped.** New `apps.saas.integration_runtime` ships a read-only resolver
layer: `mask_secret_ref`, `validate_secret_ref_format`,
`get_secret_ref_status` / `resolve_secret_ref_preview` (ENV: refs check
`os.environ` presence only — never return the value; VAULT: refs return
`planned/not_configured`), per-provider preview composition, and a
combined `get_all_provider_runtime_previews` that derives a
`safeToStartPhase6G` boolean. **`runtimeSource` is always `env_config`**
and **`perOrgRuntimeEnabled` / `runtimeUsesPerOrgSettings` are always
`False`** in this phase — the live runtime is unchanged. New
`python manage.py inspect_runtime_integration_routing --json`
management command + `GET /api/v1/saas/runtime-routing-readiness/`
admin-only DRF endpoint return the per-provider preview; both consume
the same selector. New idempotent
`python manage.py seed_default_org_integration_refs --dry-run|--apply
--json` writes ENV: secret refs only (never raw values), defaults to
`--dry-run`, emits `saas.integration_refs.seeded` audit rows on apply,
and never activates runtime routing. SaaS Admin Panel (`/saas-admin`)
gains a new "Runtime Integration Routing Preview" section showing
runtime source = Env/config, per-org runtime disabled, six provider
rows (WhatsApp Meta / Razorpay / PayU / Delhivery / Vapi / OpenAI) with
setting status / secret-ref counts / resolvable preview / runtime
source columns, and a clear warning banner: "Per-org runtime routing
is not active. Runtime still uses env/config." **No activation /
enable / send / campaign buttons anywhere.** **24 new backend tests +
3 new frontend tests; 1015 backend + 32 frontend, all green.**

**Phase 6G note:** **Phase 6G Controlled Runtime Routing Dry Run +
AI Provider Routing shipped.** New `apps.saas.runtime_operations`
declares 14 operations (`whatsapp.{send_text,send_template}`,
`razorpay.{create_order,create_payment_link}`, `payu.create_payment`,
`delhivery.create_shipment`, `vapi.place_call`,
`openai.agent_completion`, `ai.{reports_summary,ceo_planning,
caio_compliance,customer_hinglish_chat,critical_fallback,smoke_test}`)
— each with `dryRunAllowed=True` + `liveAllowedInPhase6G=False`,
provider type, side-effect risk, expected env keys, secret refs,
config keys, and `next_phase_for_live_execution`. New
`apps.saas.ai_runtime_preview` ships the **NVIDIA-primary AI routing
table** (reports → minimaxai/minimax-m2.7; ceo_planning →
moonshotai/kimi-k2.6; caio_compliance / critical_fallback →
mistralai/mistral-medium-3.5-128b; hinglish_customer_chat /
smoke_test → google/gemma-4-31b-it) with OpenAI fallback +
Anthropic Claude secondary fallback. Task-wise `max_tokens` resolves
from env (`AI_MAX_TOKENS_REPORTS=3000`, `_CEO=2048`,
`_COMPLIANCE=1024`, `_CUSTOMER_CHAT=512`, `_SMOKE=32`) with default
fallback. New `apps.saas.runtime_dry_run` composes operation
taxonomy + Phase 6F provider preview + AI route preview into a
single decision; every output is asserted by
`validate_dry_run_has_no_side_effects` (`runtimeSource="env_config"`,
`perOrgRuntimeEnabled=False`, `dryRun=True`,
`liveExecutionAllowed=False`, `externalCallWillBeMade=False`).
Three read-only management commands
(`inspect_controlled_runtime_routing_dry_run`,
`inspect_ai_provider_routing`, `preview_runtime_operation`) +
one operator-only `smoke_test_ai_provider_routes` (the ONLY path
that may issue a tiny live NVIDIA / OpenAI request — default
prompt `"Reply only OK"`, never customer data, API keys never
logged or returned, audit rows for every run). Three new
admin-only DRF endpoints (`/api/v1/saas/runtime-dry-run/`,
`/api/v1/saas/ai-provider-routing/`,
`/api/v1/saas/controlled-runtime-readiness/`) — POST/PATCH/DELETE
return 405. Frontend `/saas-admin` gains "Controlled Runtime
Routing Dry Run" (14-row operation table) + "AI Provider Routing
Preview" (NVIDIA primary / OpenAI fallback summary, per-task
primary / fallback / max-tokens / safety wrappers / next action)
sections. **No "Run live" / "Execute" / "Go live" / "Send"
buttons anywhere on the UI.** PayU + Delhivery missing envs
surface as deferred-provider warnings, not blockers; Vapi
missing `phone_number_id` / `webhook_secret` surfaces as
warning. Raw NVIDIA / OpenAI / Anthropic / Razorpay / Meta /
Vapi keys NEVER appear in any output (asserted with fake env
values). **56 new backend tests + 2 new frontend tests; 1071
backend + 34 frontend, all green.** Phase 6H shipped the audit-only live
gate, and Phase 6I now ships the simulation-only rehearsal layer.

**Phase 6H note:** **Phase 6H Controlled Runtime Routing Live Audit
Gate shipped.** Phase 6G Controlled Runtime Routing Dry Run is **FULL
PASS**. Phase 6H adds the mandatory audit/approval/kill-switch gate for
future provider-side side effects, without enabling any live execution.
New `apps.saas.live_gate_policy` defines the protected operations
(`whatsapp.{send_text,send_template}`, `razorpay.{create_order,
create_payment_link}`, `payu.create_payment`, `delhivery.create_shipment`,
`vapi.place_call`, `ai.{customer_hinglish_chat,caio_compliance,
ceo_planning,reports_summary,critical_fallback,smoke_test}`), and every policy keeps
`liveAllowedByDefault=false`, `approvalRequired=true`, and
`allowedInPhase6H=false`. New `RuntimeLiveGatePolicySnapshot`,
`RuntimeLiveExecutionRequest`, and `RuntimeKillSwitch` models store
sanitized decisions, payload hashes, approval state, blockers/warnings,
and kill-switch state; the default global kill switch is enabled.
New `apps.saas.live_gate` evaluates/records decisions and always returns
`runtimeSource="env_config"`, `perOrgRuntimeEnabled=false`, `dryRun=true`,
`liveExecutionAllowed=false`, and `externalCallWillBeMade=false` in Phase
6H. Approval in Phase 6H is audit-only and does not execute external
calls. New commands and `/api/v1/saas/runtime-live-gate/...` APIs expose
readiness, policies, requests, preview, approve, and reject flows with no
raw payloads, raw secrets, or full phone numbers. Frontend `/saas-admin`
gains "Controlled Runtime Live Audit Gate" with global safety state,
operation table, approval queue, recent audit events, and warnings. No
real WhatsApp/payment/shipment/call/provider side effect happens in Phase
6H. **16 new backend tests + 2 new frontend tests; 1087 backend + 36
frontend, all green.**

**Phase 6I note:** **Phase 6I Single Internal Live Gate Simulation
shipped.** It adds `RuntimeLiveGateSimulation` plus
`apps.saas.live_gate_simulation` as a simulation-only layer on top of the
Phase 6H gate. Allowed operations are `razorpay.create_order` (default),
`whatsapp.send_text`, and `ai.smoke_test`. Commands:
`prepare_single_internal_live_gate_simulation`,
`request_single_internal_live_gate_approval`,
`approve_single_internal_live_gate_simulation`,
`reject_single_internal_live_gate_simulation`,
`run_single_internal_live_gate_simulation`,
`rollback_single_internal_live_gate_simulation`, and
`inspect_single_internal_live_gate_simulation`. APIs live under
`/api/v1/saas/runtime-live-gate/simulations/`. Frontend `/saas-admin`
shows "Single Internal Live Gate Simulation". Hard invariant: no
WhatsApp/Razorpay/PayU/Delhivery/Vapi/AI provider side-effect call is made;
`dryRun=true`, `liveExecutionAllowed=false`,
`externalCallWillBeMade=false`, `externalCallWasMade=false`, and
`providerCallAttempted=false` stay false in every simulation response. Global
kill switch remains active; runtime providers still use env/config.

**Phase 6J note:** **Phase 6J Single Internal Provider Test Plan
shipped.** New `apps.saas.provider_test_plan_policy` registers seven
plan policies (Razorpay create_order + create_payment_link, WhatsApp
send_text, AI smoke_test, Vapi place_call, Delhivery create_shipment,
PayU create_payment); Phase 6J's only **implementation target** is
`razorpay.create_order`. New `RuntimeProviderTestPlan` model
(migration `0005_add_runtime_provider_test_plan`) stores plan state
through `draft → prepared → validated → approved_for_future_execution
→ rejected → archived → blocked`. Every plan declares `dry_run=True`,
`provider_call_allowed=False`, `external_call_will_be_made=False`,
`external_call_was_made=False`, `provider_call_attempted=False`,
`real_money=False`, `real_customer_data_allowed=False` (asserted by
`assert_provider_test_plan_has_no_side_effects`). Razorpay synthetic
payload: `{amount: 100, currency: "INR", receipt:
"phase6j_internal_test_plan_<plan_id>"}` — never customer data.
Six new strictly-read-only / plan-only management commands
(`prepare_single_provider_test_plan`,
`validate_single_provider_test_plan`,
`approve_single_provider_test_plan`,
`reject_single_provider_test_plan`,
`archive_single_provider_test_plan`,
`inspect_single_provider_test_plan`) emit one
`runtime.provider_test_plan.{prepared,validated,approved,rejected,archived,blocked,invariant_violation_blocked}`
audit row each. Seven new admin-only DRF endpoints under
`/api/v1/saas/provider-test-plans/`. Frontend `/saas-admin` gains
"Single Internal Provider Test Plan" with safety-invariant
sub-card + Razorpay env-readiness sub-card. **No "Execute
Razorpay" / "Create Order" / "Create Payment Link" buttons exist
anywhere on the UI.** Approval ONLY unlocks a future Phase 6K
execution gate; it NEVER enables a provider call in Phase 6J.
Raw NVIDIA / OpenAI / Razorpay / Meta / Vapi keys NEVER appear
in any plan, audit, command output, or API response. Global kill
switch remains active; runtime providers still use env/config.
**31 new backend tests + 2 new frontend tests; 1131 backend + 40
frontend, all green.**

**Phase 6K note:** **Phase 6K Single Internal Razorpay Test-Mode
Execution Gate shipped (Phase 6K-A: code/gate/readiness only).** New
`apps.saas.provider_execution_policy` allows ONE Phase 6K target —
`razorpay.create_order` against a Razorpay TEST key — and locks
`amount_paise=100`, `currency="INR"`, `real_money=False`,
`real_customer_data_allowed=False`,
`api_execution_allowed=False` (CLI-only),
`frontend_execution_allowed=False`,
`max_executions_per_approved_plan=1`. New
`RuntimeProviderExecutionAttempt` model (migration
`0006_add_runtime_provider_execution_attempt`) tracks status
+ rollback_status + locked safety booleans (real_money / real_customer_data_allowed
/ business_mutation_was_made / payment_link_created /
payment_captured / customer_notification_sent — all False) +
masked env_readiness + safe_request_summary + safe_response_summary
(id/status/amount/currency/receipt only) + provider_object_id.
New `apps.saas.razorpay_test_execution` is a SEPARATE adapter
from the Phase 2B production payment-link client; owns one
endpoint only (`Orders API .create()` against a test key).
Five management commands:
`inspect_single_provider_execution_gate` (read-only readiness),
`prepare_single_provider_execution_attempt` (no provider call),
`execute_single_razorpay_test_order` (the ONE manual command
that may dispatch — refuses unless approved Phase 6J plan +
`PHASE6K_RAZORPAY_TEST_EXECUTION_ENABLED=true` env flag +
`--confirm-test-execution` + `RAZORPAY_KEY_ID` starts with
`rzp_test` + no prior successful execution + amount=100 paise
+ kill switch enabled),
`rollback_single_provider_execution_attempt`,
`archive_single_provider_execution_attempt`. Eight new audit
kinds (`runtime.provider_execution.*`). Five admin-only DRF
endpoints under `/api/v1/saas/provider-execution-attempts/`
(GET list, GET detail, POST prepare, POST rollback, POST
archive). **No POST execute endpoint** — execution is
exclusively CLI-driven in Phase 6K. Frontend `/saas-admin`
gains "Single Internal Razorpay Test-Mode Execution Gate"
section with readiness sub-card + invariants sub-card +
attempts table. **No "Execute Razorpay" / "Create Order" /
"Capture" / "Go Live" / "Send" buttons exist on the UI.**
Phase 6K never creates a payment link, never captures a
payment, never sends a customer notification, never mutates
business records. Raw secrets NEVER appear in any output.
**34 new backend tests + 2 new frontend tests; 1165 backend +
42 frontend, all green.**

**Phase 6M note:** **Phase 6M Razorpay Webhook Handler (test-mode)
shipped.** Implements the canonical receiver designed in the
Phase 6L plan. New `apps.payments.razorpay_webhooks.process_razorpay_webhook`
verifies `X-Razorpay-Signature` (HMAC-SHA256 over RAW body, constant-time
compare), parses JSON, classifies the event, runs the 300-second
replay window check, dedupes on `X-Razorpay-Event-Id`, and persists a
safe summary on `RazorpayWebhookEvent` (migration `payments.0004`).
Allowlist: 9 events (payment.* / order.paid / refund.* / payment_link.*).
Denylist: 9 events (subscription / dispute / transfer / payout /
virtual_account / qr_code / invoice). New `RazorpayTestWebhookView` at
`POST /api/webhooks/razorpay/test/` (the production
`RazorpayWebhookView` at `/api/webhooks/razorpay/` is untouched). Nine
new audit kinds (`razorpay.webhook.*`). Four management commands
(`inspect_razorpay_webhook_handler_readiness`,
`simulate_razorpay_webhook_event`, `inspect_razorpay_webhook_events`,
`purge_razorpay_webhook_test_events`). Four admin/auth-protected DRF
endpoints under `/api/v1/saas/razorpay/` (handler-readiness, events
list, event detail, simulator). Frontend `/saas-admin` gains
"Razorpay Webhook Handler (Test Mode)" section with handler-readiness
card + safety-invariants sub-card + counters sub-card + events table.
**No "Capture Payment" / "Mark Order Paid" / "Replay Event" / "Send
WhatsApp" / "Refund" buttons exist anywhere on the UI.** Phase 6M
NEVER calls Razorpay, NEVER mutates Order / Payment / Shipment /
DiscountOfferLog, NEVER sends a customer notification, NEVER returns
the raw webhook secret / raw signature / raw payload (asserted with
fake env values + planted card / email / phone payloads in tests).
Defaults LOCKED: `RAZORPAY_WEBHOOK_TEST_MODE_ENABLED=false`,
`RAZORPAY_WEBHOOK_BUSINESS_MUTATION_ENABLED=false`,
`RAZORPAY_WEBHOOK_NOTIFY_CUSTOMER_ENABLED=false`,
`RAZORPAY_WEBHOOK_STORE_RAW_PAYLOAD=false`,
`RAZORPAY_WEBHOOK_REPLAY_WINDOW_SECONDS=300`. **42 new backend tests +
2 new frontend tests; 1283 backend + 48 frontend, all green.**

**Phase 6M-0 note:** **Phase 6M-0 MCP Gateway Foundation +
AI Connector Readiness shipped.** New `apps.mcp_gateway` Django
app ships six models (`McpClientApp`, `McpAccessPolicy`,
`McpToolDefinition`, `McpResourceDefinition`,
`McpPromptDefinition`, `McpToolInvocationLog`), services layer
(masking / schemas / default tools+resources+prompts / registry /
audit / auth / tool_executor / tool_handlers / readiness), 5
read-only / planning-only management commands
(`ensure_mcp_defaults`, `inspect_mcp_gateway_readiness`,
`list_mcp_tools`, `simulate_mcp_tool_call`,
`inspect_mcp_security_posture`), 7 admin/auth-protected DRF
endpoints under `/api/v1/mcp/`, and a `/saas-admin` "MCP Gateway
Readiness" section. **Defaults LOCKED**: `MCP_ENABLED=false`,
`MCP_READ_ONLY_MODE=true`, `MCP_WRITE_TOOLS_ENABLED=false`,
`MCP_PROVIDER_TOOLS_ENABLED=false`, `MCP_REQUIRE_AUTH=true`,
`MCP_AUDIT_ENABLED=true`, `MCP_MASK_PII=true`. The executor
refuses any tool from the 13-name forbidden list (razorpay /
whatsapp / vapi / delhivery / shell / sql / http_fetch / etc) and
any tool whose `provider_call_allowed` or
`business_mutation_allowed` field is True while the matching
global env flag is False. No "Run Tool" / "Execute MCP" / "Send"
buttons exist on the UI. The 10 default tools are read-only
wrappers around existing safe selectors (Phase 6L razorpay
audit + webhook readiness, Phase 5F WhatsApp gate, Phase 6E
saas org context, masked audit search, etc) — none call external
providers, none mutate business records. **43 new backend tests +
2 new frontend tests; 1241 backend + 46 frontend, all green.**

**Phase 6L note:** **Phase 6L Razorpay Test Execution Audit
Review + Webhook Readiness Plan shipped.** Phase 6K-B execution
on the VPS is **FULL PASS** (`pex_8f309650e9644cfaae4418f9` →
`order_Sks3KPf0vntKhf`, amount 100 paise INR, no payment link, no
capture, no notification, no business mutation,
`rollback_status=completed`,
`PHASE6K_RAZORPAY_TEST_EXECUTION_ENABLED=false`). New
`apps.saas.razorpay_audit_review` ships three read-only / planning
functions:
`review_razorpay_test_execution_audit(execution_id)` (replays the
ten Phase 6K invariants + scans every linked AuditEvent for raw-key
leak; FAILs on flipped safety booleans / missing rollback / missing
provider object id / leaked secret),
`inspect_razorpay_webhook_readiness()` (env presence-only check —
key mode + masked id + webhook secret presence boolean — never
the raw value), `plan_razorpay_webhook_readiness()` (canonical
policy doc with `POST /api/webhooks/razorpay/test/` endpoint
design, HMAC-SHA256 signature on `X-Razorpay-Signature`,
constant-time compare, `x_razorpay_event_id` idempotency,
300-second replay window, 9-event allowlist + 9-event denylist,
8 future audit kinds, 13-key sensitive-payload scrub list,
`businessMutationPolicy` all-False). Three new strictly-read-only
management commands
(`inspect_razorpay_test_execution_audit`,
`inspect_razorpay_webhook_readiness`,
`plan_razorpay_webhook_readiness`). Three auth-required DRF
endpoints under `/api/v1/saas/razorpay/`
(`/execution-audit/?execution_id=<ID>`,
`/webhook-readiness/`, `/webhook-plan/`) — POST/PATCH/DELETE
return 405. Frontend `/saas-admin` gains
"Razorpay Test Execution Audit + Webhook Readiness" section
(audit invariants card / readiness card / webhook plan card with
allowlist + denylist tables). **No "Execute Razorpay" / "Register
Webhook" / "Capture" / "Send WhatsApp" / "Go Live" buttons exist
on the UI.** Phase 6L never calls Razorpay, never creates a
payment link, never captures, never sends a customer notification,
never mutates business records, never returns raw secrets,
never returns the raw provider response (whitelisted summary
only). **33 new backend tests + 2 new frontend tests; 1198
backend + 44 frontend, all green.**

**Phase 6R note:** **Phase 6R Payment → WhatsApp/Courier
Readiness shipped (audit-only readiness contract).** Phase 6Q
Payment → Order Workflow Safety Gate is **FULL PASS**. Phase 6R
adds the audit-only readiness review record that converts an
**approved Phase 6Q gate** into a future-Phase-6S dispatch
readiness contract — without sending a single WhatsApp message,
calling Meta Cloud, calling Delhivery, creating any shipment /
AWB, mutating any real `Order` / `Payment` / `Shipment` /
`DiscountOfferLog` / `Customer` / `Lead`, calling Razorpay, or
touching `.env.production`. New env flag
`RAZORPAY_PAYMENT_DISPATCH_READINESS_ENABLED` defaults `False`
(production posture — `.env.production` is NOT edited). New
`RazorpayPaymentDispatchReadinessGate` model (migration
`payments.0008`) chains FKs through Phase 6Q gate → Phase 6P
attempt → Phase 6P ledger → Phase 6O review → original
`RazorpayWebhookEvent`; declares 12 locked-False safety
booleans (`dispatch_readiness_allowed_in_phase6r`,
`real_order_mutation_was_made`, `real_payment_mutation_was_made`,
`shipment_mutation_was_made`, `shipment_created`,
`whatsapp_message_created`, `whatsapp_message_queued`,
`customer_notification_sent`, `meta_cloud_call_attempted`,
`delhivery_call_attempted`, `razorpay_call_attempted`,
`provider_call_attempted`); 6-state flow `draft → blocked /
pending_manual_review → approved_for_future_phase6s / rejected
/ archived`. New `apps.payments.razorpay_payment_dispatch_readiness`
service ships
`build_phase6r_payment_dispatch_readiness_contract` (9 events
mapped to `futureWhatsAppReadinessAction /
futureCourierReadinessAction / futureDispatchReadinessAction`),
`assert_phase6r_no_live_send_or_courier_mutation` (defensive
12-boolean guard that emits audit + raises ValueError),
`validate_phase6r_source_gate_eligibility` (full chain
validation), and the 7 lifecycle helpers (preview / prepare /
approve / reject / archive / summarize / inspect). Seven new
strictly-CLI management commands
(`inspect_razorpay_payment_dispatch_readiness`,
`preview_razorpay_payment_dispatch_readiness_gate`,
`prepare_razorpay_payment_dispatch_readiness_gate`,
`approve_razorpay_payment_dispatch_readiness_gate` —
`require_reason=True`,
`reject_razorpay_payment_dispatch_readiness_gate`,
`archive_razorpay_payment_dispatch_readiness_gate`,
`inspect_razorpay_payment_dispatch_readiness_gates`). Eight
new audit kinds (`razorpay.payment_dispatch_readiness.{
readiness_inspected,previewed,prepared,
approved_for_future_phase6s,rejected,archived,blocked,
invariant_violation_blocked}`); audit payloads NEVER carry
`raw_payload` / `raw_signature` / `raw_secret` / `phone` /
`email` / `address` / `card` / `vpa` / `upi` / `bank_account` /
`wallet`. Four read-only auth-protected GET DRF endpoints under
`/api/v1/saas/razorpay/`
(`payment-dispatch-readiness/`,
`payment-dispatch-readiness-gates/`,
`payment-dispatch-readiness-gates/<id>/`,
`payment-dispatch-readiness-preview/?gate_id=<ID>`) —
**no POST endpoint** (review state changes are CLI-only).
POST/PATCH/DELETE return 405 on every endpoint. Frontend
`/saas-admin` gains the "Razorpay Payment → WhatsApp / Courier
Dispatch Readiness" section: status / phase badges, readiness
contract table (9 events, 3 disabled-in-Phase-6R lock columns),
recent readiness gates table, three readiness checklists
(WhatsApp / courier / dispatch), forbidden actions panel,
"Readiness contract only" CLI-only banner. **No "Send WhatsApp" /
"Queue WhatsApp" / "Create Shipment" / "Create AWB" / "Book
Courier" / "Dispatch Order" / "Notify Customer" / "Mark Paid" /
"Capture Payment" / "Refund" / "Apply Mutation" / "Mutate Order"
/ "Create Payment Link" / "Execute Webhook" / "Replay Event" /
"Enable Mutation" / "Go Live" / "Run MCP Tool" / "Execute
Workflow" / "Apply Order Update" / "Confirm Paid Order" /
"Start Live Workflow" / "Approve Readiness" / "Reject Readiness"
buttons exist anywhere on the UI.** **32 new backend tests + 2
new frontend tests; 1315 backend + 50 frontend, all green.**
Defaults LOCKED:
`RAZORPAY_PAYMENT_DISPATCH_READINESS_ENABLED=false`,
`WHATSAPP_LIFECYCLE_AUTOMATION_ENABLED=false`,
`WHATSAPP_AI_AUTO_REPLY_ENABLED=false`,
`WHATSAPP_CALL_HANDOFF_ENABLED=false`,
`WHATSAPP_RESCUE_DISCOUNT_ENABLED=false`,
`DELHIVERY_MODE=mock` (or `test`),
`PHASE6K_RAZORPAY_TEST_EXECUTION_ENABLED=false`. **Phase 6S is now
FULL PASS (see below).**

**Phase 6S note:** **Phase 6S Limited Internal Dispatch Pilot
Plan shipped (planning-only).** Phase 6R Payment → WhatsApp /
Courier Dispatch Readiness is **FULL PASS**. Phase 6S adds the
planning-only pilot plan record that converts an **approved
Phase 6R readiness gate** into a future-Phase-6T pilot
candidacy contract — without executing a pilot, sending a
WhatsApp message, calling Meta Cloud, calling Delhivery,
calling Razorpay, creating any shipment / AWB, mutating any real
`Order` / `Payment` / `Shipment` / `DiscountOfferLog` /
`Customer` / `Lead` row, or touching `.env.production`. New env
flag `RAZORPAY_PAYMENT_DISPATCH_PILOT_PLAN_ENABLED` defaults
`False` (production posture — `.env.production` is NOT edited).
New `RazorpayPaymentDispatchPilotPlan` model (migration
`payments.0009`) chains FKs through Phase 6R gate → Phase 6Q
gate → Phase 6P attempt → Phase 6P ledger → Phase 6O review →
original `RazorpayWebhookEvent`; declares **16 locked-False
safety booleans** (`pilot_execution_allowed_in_phase6s`,
`live_send_allowed_in_phase6s`, `courier_booking_allowed_in_phase6s`,
`provider_call_allowed_in_phase6s`, `real_order_mutation_was_made`,
`real_payment_mutation_was_made`, `shipment_mutation_was_made`,
`shipment_created`, `awb_created`, `whatsapp_message_created`,
`whatsapp_message_queued`, `customer_notification_sent`,
`meta_cloud_call_attempted`, `delhivery_call_attempted`,
`razorpay_call_attempted`, `provider_call_attempted`); 6-state
flow `draft → blocked / pending_manual_review →
approved_for_future_phase6t / rejected / archived`. Internal cohort
metadata only (no full PII); `internal_only=True`,
`max_pilot_orders=1`, `max_amount_paise=100`. New
`apps.payments.razorpay_payment_dispatch_pilot_plan` service ships
`build_phase6s_payment_dispatch_pilot_contract` (9 events mapped
to `futurePilotEligibility / futureWhatsAppPilotAction /
futureCourierPilotAction / futureDispatchPilotAction`),
`assert_phase6s_no_live_execution_or_provider_call` (defensive
16-boolean guard that emits audit + raises `ValueError`),
`validate_phase6s_source_readiness_gate_eligibility` (full chain
validation), and 7 lifecycle helpers (preview / prepare / approve /
reject / archive / summarize / inspect). Seven new
strictly-CLI management commands
(`inspect_razorpay_payment_dispatch_pilot_plan_readiness`,
`preview_razorpay_payment_dispatch_pilot_plan`,
`prepare_razorpay_payment_dispatch_pilot_plan`,
`approve_razorpay_payment_dispatch_pilot_plan` —
`require_reason=True`,
`reject_razorpay_payment_dispatch_pilot_plan`,
`archive_razorpay_payment_dispatch_pilot_plan`,
`inspect_razorpay_payment_dispatch_pilot_plans`). Eight
new audit kinds (`razorpay.payment_dispatch_pilot_plan.{
readiness_inspected,previewed,prepared,
approved_for_future_phase6t,rejected,archived,blocked,
invariant_violation_blocked}`); audit payloads NEVER carry
`raw_payload` / `raw_signature` / `raw_secret` / `phone` /
`email` / `address` / `card` / `vpa` / `upi` / `bank_account` /
`wallet`. Four read-only auth-protected GET DRF endpoints under
`/api/v1/saas/razorpay/`
(`payment-dispatch-pilot-plan-readiness/`,
`payment-dispatch-pilot-plans/`,
`payment-dispatch-pilot-plans/<id>/`,
`payment-dispatch-pilot-plan-preview/?readiness_id=<ID>`) —
**no POST endpoint** (review state changes are CLI-only).
POST/PATCH/DELETE return 405 on every endpoint. Frontend
`/saas-admin` gains the "Razorpay Limited Internal Dispatch
Pilot Plan" section: status / phase badges, pilot contract
table (9 events, 3 disabled-in-Phase-6S lock columns),
recent pilot plans table, four readiness checklists
(internal staff cohort / WhatsApp / courier / dispatch),
abort criteria, verification checklist, forbidden actions
panel, "Pilot plan only" CLI-only banner. **No "Start Pilot" /
"Run Pilot" / "Execute Pilot" / "Send WhatsApp" / "Queue
WhatsApp" / "Notify Customer" / "Create Shipment" / "Create
AWB" / "Book Courier" / "Dispatch Order" / "Call Delhivery" /
"Call Meta" / "Mark Paid" / "Capture Payment" / "Refund" /
"Apply Mutation" / "Mutate Order" / "Create Payment Link" /
"Execute Webhook" / "Replay Event" / "Enable Mutation" / "Go
Live" / "Run MCP Tool" / "Execute Workflow" / "Apply Order
Update" / "Confirm Paid Order" / "Start Live Workflow" /
"Approve Pilot Plan" / "Reject Pilot Plan" buttons exist
anywhere on the UI.** **33 new backend tests + 2 new frontend
tests; 1474 backend + 60 frontend, all green at the Phase 6S milestone (current baseline after Phase 6T is 1495 backend + 62 frontend).** Defaults
LOCKED: `RAZORPAY_PAYMENT_DISPATCH_PILOT_PLAN_ENABLED=false`,
`RAZORPAY_PAYMENT_DISPATCH_READINESS_ENABLED=false`,
`RAZORPAY_PAYMENT_ORDER_WORKFLOW_GATE_ENABLED=false`,
`RAZORPAY_SANDBOX_PAID_STATUS_MUTATION_ENABLED=false`,
`WHATSAPP_LIFECYCLE_AUTOMATION_ENABLED=false`,
`WHATSAPP_AI_AUTO_REPLY_ENABLED=false`,
`WHATSAPP_CALL_HANDOFF_ENABLED=false`,
`WHATSAPP_RESCUE_DISCOUNT_ENABLED=false`,
`DELHIVERY_MODE=mock` (or `test`),
`PHASE6K_RAZORPAY_TEST_EXECUTION_ENABLED=false`,
`MCP_ENABLED=false`. **Next planned: Phase 6T — Final Phase 6
audit + lock / controlled pilot execution decision gate (not
started).**

---

## 1. Working agreement (binding rule)

**Every meaningful change to this project MUST be followed by:**

1. **Update `nd.md`** — adjust the relevant section (TL;DR §0, what's done so far §8, phase roadmap §11, or whichever is impacted) so the project handoff stays the source of truth.
2. **Update `AGENTS.md`** — if a convention, hard stop, or "where things live" pointer changed, reflect it here so other AI tools that auto-load `AGENTS.md` pick it up.
3. **Run the full verification suite** before committing:
   - `cd backend && python manage.py makemigrations --check --dry-run` — must report `No changes detected`. Fixed Phase 5E hotfix: hand-rolled migration index names produced auto-suffix drift; `makemigrations --check` now blocks the commit if any model field, index, or constraint diverges from the migration history.
   - `cd backend && python -m pytest -q && python manage.py check`
   - `cd frontend && npm run lint && npm test && npm run build`
4. **Commit with a Conventional Commit message** (`feat:`, `fix:`, `refactor:`, `docs:`, `test:`, `chore:`).
5. **Push to `origin/main` at GitHub** — `git push origin main` to https://github.com/prarit0097/Nirogidhara-AI-Command-Center.

This is non-negotiable. The repo on GitHub must mirror the local state at the end of every working session.

### What counts as a "meaningful change"?

- Any code edit (backend or frontend)
- Any new endpoint, model, migration, service, or page
- Any new env var, dependency, or configuration
- Any docs change that affects onboarding (RUNBOOK, BACKEND_API, README)
- Any rule or convention change

### What does NOT count?

- Reading-only exploration
- Failed/discarded edits that were reverted
- Local-only experiments outside the repo

If you're unsure, default to "yes, this is meaningful, update + push."

### Exceptions (still ask the user)

- **Force-push** to `main`: never without explicit user authorization.
- **Rewriting history** (`rebase -i`, `commit --amend` on already-pushed commits): never without explicit user authorization.
- **Pushing secrets**: never. The `.gitignore` covers `.env`, `db.sqlite3`, etc. — verify before staging.
- **Pushing during plan mode**: never. Plan mode means edits are gated.

---

## 2. Hard stops (compliance — never bypass)

Inherited from `AGENTS.md` and the Master Blueprint §26. Most important to keep in mind for AI agents:

1. **No free-style medical claims.** AI may only emit content from `apps.compliance.Claim`. Hard-coded medical strings are forbidden.
2. **CAIO Agent never executes business actions.** Monitor / audit / suggest only.
3. **CEO AI is the execution approval layer.** High-risk decisions go to Prarit.
4. **Every important state change writes an `AuditEvent`** (Master Event Ledger).
5. **Reward/penalty is based on delivered profitable orders**, not punching count.
6. **Frontend never holds business logic.**
7. **Razorpay/PayU/Delhivery/Vapi/Meta secrets stay server-side.** Frontend gets URLs and statuses only.
8. **WhatsApp** (Phase 5A-0 / 5A-1 / 5A): production target is **Meta Cloud API**; Baileys is **dev/demo only** (off by default in non-DEBUG environments). Every send is consent + approved-template + Claim-Vault gated server-side; failed sends never mutate Order / Payment / Shipment. CAIO never sends customer messages. Full plan: `docs/WHATSAPP_INTEGRATION_PLAN.md`.
9. **WhatsApp AI Chat Sales Agent** (Phase 5A-1 addendum, §S–§GG): the WhatsApp module must work as an inbound-first AI Chat Sales Agent mirroring the AI Calling Agent's business objective. Greeting rule locked (fixed Hindi UTILITY template on first reply). First-phase mode is `auto-reply` — meaning the AI replies without operator click, NOT that it bypasses the matrix / Claim Vault / approval engine / sandbox / budget. Address is collected in chat via a stateful `WhatsAppConversation.metadata.address_collection`. Category must be detected before any product-specific text. Chat-to-call handoff fires on explicit request, low confidence, address/payment failure, or any of the six existing handoff flags.
10. **Discount discipline (LOCKED, the most important business rule).** Any AI/Calling/Chat/Confirmation/RTO/Customer-Success agent **must NOT offer a discount upfront**. Lead with standard ₹3000/30-capsule price; do not mention discount unless the customer asks; on first ask, handle the underlying objection (value/trust/benefit/brand/doctor/ingredients/lifestyle); only after 2–3 customer pushes may the AI offer a discount within the Phase 3E `validate_discount` bands. **Refusal-based rescue is the only proactive offer path** — eligible at three stages: A) order-booking refusal, B) confirmation refusal, C) delivery / RTO refusal.
11. **50% total discount hard cap (LOCKED).** Across all stages combined, the total discount on a single order must NEVER exceed 50%. Examples: 20+20+10=50% allowed; 20+20+20=60% blocked. Scope: every AI workflow that can offer a discount. Phase 5E will add `validate_total_discount_cap(order, additional_pct)` that runs before `apply_order_discount`; over-cap requests convert to a director-only `discount.above_50_director_override` `ApprovalRequest`.

Full list in `AGENTS.md`.

---

## 3. Architecture rule (the contract)

```
React UI  ──/api/JSON──►  Django + DRF  ──ORM──►  SQLite (dev) / Postgres (prod)
   │                          │
   │                          │ post_save signals + service-layer writes
   │                          ▼
   │                   audit.AuditEvent  ← Master Event Ledger
   │                          ▲
   │                          │
   │                   Razorpay webhooks (HMAC-verified, idempotent)
   │
   └── mock fallback ── frontend/src/services/mockData.ts (offline-safe)
```

- **Frontend → backend** flows through `frontend/src/services/api.ts`.
- **Backend → frontend** uses camelCase JSON keys (Django serializers map snake_case columns via `source=`).
- **Pages** consume `api` only. **Never** import `mockData.ts` from a page.
- **No `{success, data}` envelope** — endpoints return raw arrays/objects matching `frontend/src/types/domain.ts`.
- **Workflow logic lives in `apps/<app>/services.py`** (services), not views. Views are parse → call service → respond.
- **Gateway integrations live in `apps/<app>/integrations/`** (e.g. `apps/payments/integrations/razorpay_client.py`) with a mock/test/live mode dispatch.

---

## 4. Common commands

```bash
# Backend
cd backend
python -m venv .venv && .\.venv\Scripts\Activate.ps1  # Windows
pip install -r requirements.txt
python manage.py migrate
python manage.py seed_demo_data --reset
python manage.py runserver 0.0.0.0:8000
python -m pytest -q                    # 2200 tests today

# Frontend
cd frontend
npm install
npm run dev                            # http://localhost:8080
npm test                               # 82 tests today
npm run lint                           # 0 errors expected
npm run build                          # production build

# After any change
git add -A
git commit -m "<type>: <message>"
git push origin main
```

---

## 5. Where things live

| Task | File |
| --- | --- |
| Frontend → backend contract | `frontend/src/services/api.ts` |
| Shared TS types | `frontend/src/types/domain.ts` |
| Backend service layer per app | `backend/apps/<app>/services.py` |
| Razorpay adapter (mock/test/live) | `backend/apps/payments/integrations/razorpay_client.py` |
| Razorpay webhook receiver | `backend/apps/payments/webhooks.py` |
| Delhivery adapter (mock/test/live) | `backend/apps/shipments/integrations/delhivery_client.py` |
| Delhivery tracking webhook | `backend/apps/shipments/webhooks.py` |
| Vapi adapter (mock/test/live) | `backend/apps/calls/integrations/vapi_client.py` |
| Vapi voice trigger + webhook | `backend/apps/calls/services.py` + `backend/apps/calls/webhooks.py` |
| Meta Lead Ads adapter (mock/test/live) | `backend/apps/crm/integrations/meta_client.py` |
| Meta Lead Ads webhook | `backend/apps/crm/webhooks.py` (`apps/crm/services.ingest_meta_lead`) |
| AI provider adapters (Phase 3A) | `backend/apps/integrations/ai/{base,openai_client,anthropic_client,grok_client,dispatch}.py` |
| AI prompt builder (Claim Vault enforced) | `backend/apps/ai_governance/prompting.py` |
| AgentRun services (CAIO hard stop) | `backend/apps/ai_governance/services/__init__.py` |
| Per-agent runtime modules (Phase 3B) | `backend/apps/ai_governance/services/agents/{ceo,caio,ads,rto,sales_growth,cfo,compliance}.py` |
| Daily AI briefing management command | `backend/apps/ai_governance/management/commands/run_daily_ai_briefing.py` |
| Celery app + beat schedule (Phase 3C) | `backend/config/celery.py` |
| Celery task wrapping CEO + CAIO sweeps | `backend/apps/ai_governance/tasks.py` |
| Model-wise USD pricing (OpenAI + Anthropic) | `backend/apps/integrations/ai/pricing.py` |
| Local Redis (dev only) | `docker-compose.dev.yml` (root) |
| Frontend Scheduler Status page | `frontend/src/pages/Scheduler.tsx` (`/ai-scheduler`) |
| Sandbox state singleton (Phase 3D) | `backend/apps/ai_governance/sandbox.py` |
| PromptVersion lifecycle | `backend/apps/ai_governance/prompt_versions.py` |
| Per-agent budget guard | `backend/apps/ai_governance/budgets.py` |
| Frontend AI Governance page | `frontend/src/pages/Governance.tsx` (`/ai-governance`) |
| Product Catalog (Phase 3E) | `backend/apps/catalog/{models,serializers,views,urls,admin}.py` |
| Discount policy (Phase 3E) | `backend/apps/orders/discounts.py` (`validate_discount`) |
| Advance payment policy (Phase 3E, ₹499) | `backend/apps/payments/policies.py` (`FIXED_ADVANCE_AMOUNT_INR`) |
| Reward/Penalty scoring (Phase 3E) | `backend/apps/rewards/scoring.py` (`calculate_order_reward_penalty`) |
| Approval Matrix policy (Phase 3E) | `backend/apps/ai_governance/approval_matrix.py` (read at `/api/ai/approval-matrix/`) |
| WhatsApp design scaffold (Phase 3E) | `backend/apps/crm/whatsapp_design.py` |
| WhatsApp app (Phase 5A) | `backend/apps/whatsapp/` |
| WhatsApp models | `backend/apps/whatsapp/models.py` (8 tables) |
| WhatsApp provider interface | `backend/apps/whatsapp/integrations/whatsapp/base.py` |
| WhatsApp mock provider | `backend/apps/whatsapp/integrations/whatsapp/mock.py` |
| WhatsApp Meta Cloud provider | `backend/apps/whatsapp/integrations/whatsapp/meta_cloud_client.py` |
| WhatsApp Baileys dev stub | `backend/apps/whatsapp/integrations/whatsapp/baileys_dev.py` |
| WhatsApp service layer | `backend/apps/whatsapp/services.py` (queue_template_message, send_queued_message, handle_inbound/status webhook events) |
| WhatsApp consent helpers | `backend/apps/whatsapp/consent.py` (granted / revoked / opted_out + STOP / UNSUBSCRIBE / BAND keywords) |
| WhatsApp template registry | `backend/apps/whatsapp/template_registry.py` (sync from Meta + Claim-Vault flag) |
| WhatsApp Celery task | `backend/apps/whatsapp/tasks.py` (autoretry_for=ProviderError, max_retries=5) |
| WhatsApp signed webhook | `backend/apps/whatsapp/webhooks.py` (`/api/webhooks/whatsapp/meta/`) |
| WhatsApp REST endpoints | `backend/apps/whatsapp/views.py` + `urls.py` (`/api/whatsapp/...`) |
| WhatsApp template-sync command | `backend/apps/whatsapp/management/commands/sync_whatsapp_templates.py` |
| Frontend WhatsApp Templates page | `frontend/src/pages/WhatsAppTemplates.tsx` (`/whatsapp-templates`) |
| Frontend WhatsApp api methods | `frontend/src/services/api.ts` (Phase 5A section) |
| Frontend WhatsApp types | `frontend/src/types/domain.ts` (`WhatsApp*` block) |
| Frontend Settings WABA section | `frontend/src/pages/Settings.tsx` (extended) |
| Phase 5B WhatsApp internal note model | `backend/apps/whatsapp/models.py` (`WhatsAppInternalNote`) |
| Phase 5B inbox / notes / mark-read / send-template / timeline views | `backend/apps/whatsapp/views.py` (`WhatsAppInboxView`, `WhatsAppConversationNotesView`, `WhatsAppConversationMarkReadView`, `WhatsAppConversationSendTemplateView`, `WhatsAppCustomerTimelineView`, `_patch_conversation`) |
| Phase 5B WhatsApp inbox page | `frontend/src/pages/WhatsAppInbox.tsx` (`/whatsapp-inbox`) |
| Phase 5B Customer 360 WhatsApp tab | `frontend/src/pages/Customers.tsx` (extended — `WhatsAppTab`) |
| Production Docker compose | `docker-compose.prod.yml` (project name `nirogidhara-command`, six isolated containers, host port 18020) |
| Production backend image | `backend/Dockerfile` + `deploy/backend/entrypoint.sh` (waits for Postgres/Redis, runs migrate + collectstatic) |
| Production frontend image | `frontend/Dockerfile` (multi-stage node → nginx alpine; bakes same-origin VITE_*) |
| Production Nginx config | `deploy/nginx/nirogidhara.conf` (SPA + `/api/` + `/admin/` + `/ws/` with WebSocket upgrade) |
| Production env example | `.env.production.example` (mock-mode defaults locked) |
| Production deploy runbook | `docs/DEPLOYMENT_VPS.md` (DNS, TLS, smoke tests, backups, shared-VPS resource notes) |
| Phase 5C AI orchestration | `backend/apps/whatsapp/ai_orchestration.py` (`run_whatsapp_ai_agent`) |
| Phase 5C language detection | `backend/apps/whatsapp/language.py` |
| Phase 5C JSON schema + blocked phrases | `backend/apps/whatsapp/ai_schema.py` |
| Phase 5C discount cap | `backend/apps/whatsapp/discount_policy.py` (`evaluate_whatsapp_discount`, `validate_total_discount_cap`) |
| Phase 5C order booking from chat | `backend/apps/whatsapp/order_booking.py` (`book_order_from_decision`) |
| Phase 5C Celery task | `backend/apps/whatsapp/tasks.py` (`run_whatsapp_ai_agent_for_conversation`) |
| Phase 5C AI endpoints | `backend/apps/whatsapp/views.py` (`WhatsAppAiStatusView`, `WhatsAppConversationAiModeView`, `WhatsAppConversationRunAiView`, `WhatsAppConversationAiRunsView`, `WhatsAppConversationHandoffView`, `WhatsAppConversationResumeAiView`) |
| Phase 5C frontend AI panel | `frontend/src/pages/WhatsAppInbox.tsx` (`AiAgentPanel`) + `frontend/src/services/api.ts` (Phase 5C section) |
| Phase 5D Claim Vault coverage | `backend/apps/compliance/coverage.py` + `backend/apps/compliance/management/commands/check_claim_vault_coverage.py` |
| Phase 5D Claim coverage endpoint | `backend/apps/compliance/views.py` (`ClaimVaultCoverageView`) at `/api/compliance/claim-coverage/` |
| Phase 5D handoff model | `backend/apps/whatsapp/models.py` (`WhatsAppHandoffToCall`) |
| Phase 5D Vapi handoff service | `backend/apps/whatsapp/call_handoff.py` (`trigger_vapi_call_from_whatsapp`) |
| Phase 5D operator handoff endpoint | `backend/apps/whatsapp/views.py` (`WhatsAppConversationHandoffToCallView`) at `/api/whatsapp/conversations/{id}/handoff-to-call/` |
| Phase 5D lifecycle automation | `backend/apps/whatsapp/lifecycle.py` (`queue_lifecycle_message`, `LIFECYCLE_TRIGGERS`) |
| Phase 5D lifecycle event model | `backend/apps/whatsapp/models.py` (`WhatsAppLifecycleEvent`) |
| Phase 5D signal receivers | `backend/apps/whatsapp/signals.py` (Order/Payment/Shipment post_save) |
| Phase 5D lifecycle Celery task | `backend/apps/whatsapp/tasks.py` (`send_whatsapp_lifecycle_message_task`) |
| Phase 5D lifecycle list endpoint | `backend/apps/whatsapp/views.py` (`WhatsAppLifecycleEventsListView`) at `/api/whatsapp/lifecycle-events/` |
| Phase 5D AI booked → confirmation | `backend/apps/whatsapp/order_booking.py` (calls `apps.orders.services.move_to_confirmation` post-create) |
| Phase 5E rescue discount engine | `backend/apps/orders/rescue_discount.py` (`calculate_rescue_discount_offer`, `create_rescue_discount_offer`, `accept_rescue_discount_offer`, `reject_rescue_discount_offer`, `validate_total_discount_cap`) |
| Phase 5E DiscountOfferLog model | `backend/apps/orders/models.py` (`DiscountOfferLog`) + migration `0003_phase5e_discount_offer_log` |
| Phase 5E discount endpoints | `backend/apps/orders/views.py` (`OrderViewSet.list_discount_offers / create_rescue_offer / accept_discount_offer / reject_discount_offer`) at `/api/orders/{id}/discount-offers/...` |
| Phase 5E lifecycle rescue + Day-20 actions | `backend/apps/whatsapp/lifecycle.py` (`LIFECYCLE_TRIGGERS` extended, `RESCUE_DISCOUNT_ACTIONS`, `REORDER_DAY20_ACTION`) |
| Phase 5E Day-20 reorder sweep | `backend/apps/whatsapp/reorder.py` (`run_day20_reorder_sweep`) + `python manage.py run_reorder_day20_sweep` + `apps.whatsapp.tasks.run_reorder_day20_sweep_task` |
| Phase 5E Day-20 admin endpoints | `backend/apps/whatsapp/views.py` (`WhatsAppReorderDay20StatusView`, `WhatsAppReorderDay20RunView`) at `/api/whatsapp/reorder/day20/{status,run}/` |
| Phase 5E default Claim Vault seed | `python manage.py seed_default_claims [--reset-demo] [--json]` |
| Phase 5E rescue / reorder template names | `backend/apps/whatsapp/template_registry.py` (`whatsapp.{confirmation,delivery,rto}_rescue_discount`, `whatsapp.reorder_day20_reminder`) |
| Phase 5E approval matrix rows | `backend/apps/ai_governance/approval_matrix.py` (`discount.rescue.ceo_review`, `discount.above_safe_auto_band`, four rescue / Day-20 templates) |
| Phase 5F-Gate one-number-test helpers | `backend/apps/whatsapp/meta_one_number_test.py` (`is_number_allowed_for_live_meta_test`, `verify_provider_and_credentials`, `resolve_test_template`, `webhook_url_summary`) |
| Phase 5F-Gate management command | `python manage.py run_meta_one_number_test [--to +91… --template <name> --verify-only|--send|--check-webhook-config --json]` |
| Phase 5F-Gate Hardening helpers | `backend/apps/whatsapp/meta_one_number_test.py` (`check_waba_subscription`, `find_existing_message_by_idempotency_key`, `WabaSubscriptionStatus`, duplicate-idempotency emitter) |
| Phase 5F-Gate Hardening inspector | `python manage.py inspect_whatsapp_live_test --phone +91XXXXXXXXXX --json` (read-only; never sends, never mutates DB) |
| Phase 5F-Gate final-send limited-mode guard | `backend/apps/whatsapp/services.py` (`_limited_test_mode_blocks_send`) — runs inside `send_freeform_text_message` + `queue_template_message` |
| Phase 5F-Gate controlled-test command | `python manage.py run_controlled_ai_auto_reply_test --phone +91XXXXXXXXXX --message "…" [--dry-run\|--send] [--json]` |
| Orchestrator scoped auto-reply override | `backend/apps/whatsapp/ai_orchestration.run_whatsapp_ai_agent(..., force_auto_reply=True)` — CLI-only; never flip the global env from code |
| Phase 5F-Gate category → Claim.product map | `backend/apps/whatsapp/claim_mapping.py` (`category_to_claim_product`, `CATEGORY_SLUG_TO_PRODUCT`, `ALIASES`) — used by `_claims_for_category` and the controlled-test diagnostics |
| Phase 5F-Gate prompt action discipline | `backend/apps/whatsapp/ai_orchestration._build_prompt` (`BUSINESS FACTS` + `ACTION SELECTION DECISION TREE` + `FINAL CHECK`) |
| Phase 5F-Gate grounding count helper | `backend/apps/whatsapp/ai_orchestration._claim_grounding_counts` (returns `claim_row_count` / `approved_claim_count` / `disallowed_phrase_count` for audit + diagnostics) |
| Phase 5F-Gate deterministic grounded reply builder | `backend/apps/whatsapp/grounded_reply_builder.py` (`is_normal_product_info_inquiry`, `can_build_grounded_product_reply`, `build_grounded_product_reply`, `validate_reply_uses_claim_vault`) — controlled-test fallback only |
| Phase 5F-Gate intent classifier | `backend/apps/whatsapp/grounded_reply_builder.classify_inbound_intent` — priority order `unsafe → human_request → discount_objection → product_info → unknown`; controlled-test command consumes this BEFORE invoking the orchestrator |
| Phase 5F-Gate objection-aware reply builder | `backend/apps/whatsapp/grounded_reply_builder.{can_build_objection_reply, build_objection_aware_reply, validate_objection_reply}` — controlled-test discount/price-objection fallback; never promises a discount, never mutates DiscountOfferLog/Order/Payment |
| Phase 5F-Gate human-request handoff | `backend/apps/whatsapp/management/commands/run_controlled_ai_auto_reply_test._handle_human_request_handoff` — short-circuits orchestrator with typed `human_advisor_requested` reason; Vapi gated by `WHATSAPP_CALL_HANDOFF_ENABLED` |
| Phase 5F-Gate cohort inspector | `python manage.py inspect_whatsapp_internal_cohort [--show-full-numbers] [--json]` — read-only readiness report for the entire allow-list |
| Phase 5F-Gate cohort registrar | `python manage.py prepare_whatsapp_internal_test_number --phone +91… --name "…" [--source …] --json` — refuses non-allow-list numbers; creates/reuses Customer + grants WhatsAppConsent; never sends; emits `whatsapp.internal_cohort.number_prepared` |
| Phase 5F-Gate cohort dry-run | `python manage.py run_whatsapp_internal_cohort_dry_run --json` — per-number scenario readiness, read-only |
| Phase 5F-Gate auto-reply gate inspector | `python manage.py inspect_whatsapp_auto_reply_gate [--json]` — `backend/apps/whatsapp/management/commands/inspect_whatsapp_auto_reply_gate.py`; reports every gate the real-inbound webhook auto-reply path depends on + `readyForLimitedAutoReply` |
| Phase 5F-Gate recent activity inspector | `python manage.py inspect_recent_whatsapp_auto_reply_activity --hours N [--json]` — `backend/apps/whatsapp/management/commands/inspect_recent_whatsapp_auto_reply_activity.py`; counts AI activity + business-state mutation deltas in the last N hours |
| Phase 5F-Gate auto-reply audit emits | `backend/apps/whatsapp/ai_orchestration._send_freeform_reply` — emits `whatsapp.ai.auto_reply_flag_path_used` only when real flag-driven path took the auto-send (NOT on `force_auto_reply=True` CLI runs); emits `whatsapp.ai.auto_reply_guard_blocked` when the final-send limited-mode allow-list guard refuses |
| Phase 5F-Gate real-inbound deterministic fallback | `backend/apps/whatsapp/ai_orchestration._attempt_deterministic_grounded_fallback` + `_dispatch_deterministic_fallback` — wired at every soft non-safety blocker site (`claim_vault_not_used`, `low_confidence`, `ai_handoff_requested`, `no_action`); covers both standard grounded reply and discount-objection-aware reply; emits `whatsapp.ai.deterministic_grounded_reply_used` / `whatsapp.ai.objection_reply_used` + flips `decision.safety["claimVaultUsed"]=True` after the validator confirms an approved phrase is embedded |
| Phase 5F-Gate latest-inbound safety isolation | `backend/apps/whatsapp/ai_orchestration.run_whatsapp_ai_agent` (the `safety_downgraded` audit emit) — payload now carries `latest_inbound_message_id`, `latest_inbound_safety_flags`, `history_safety_flags`, `history_safety_ignored_for_current_safe_query` so soak inspectors can see when older history biased the LLM but the corrector kept the decision based on the latest inbound text |
| Phase 5F-Gate dashboard selectors | `backend/apps/whatsapp/dashboard.py` — single source of truth for the auto-reply monitoring dashboard. Read-only `get_auto_reply_gate_summary` / `get_recent_auto_reply_activity` / `get_internal_cohort_summary` / `get_recent_whatsapp_audit_events` / `get_whatsapp_mutation_safety_summary` / `get_unexpected_outbound_summary` / `get_whatsapp_monitoring_dashboard`. Phones masked to last-4. Audit payloads scrubbed of tokens / verify token / app secret. Both inspector management commands now delegate here. |
| Phase 5F-Gate dashboard endpoints | `backend/apps/whatsapp/monitoring_views.py` — admin-only DRF views under `/api/whatsapp/monitoring/{overview,gate,activity,cohort,audit,mutation-safety,unexpected-outbound}/`. Read-only (POST/PATCH/DELETE → 405). |
| Phase 5F-Gate dashboard page | `frontend/src/pages/WhatsAppMonitoring.tsx` (`/whatsapp-monitoring`) — status badge, gate cards, automation flag pills, activity metrics, mutation safety, cohort table, recent audit timeline, backend `nextAction` panel. Auto-refresh every 30s. No send / enable / disable controls anywhere on the page. |
| Phase 5F-Gate dashboard api methods | `frontend/src/services/api.ts` (`getWhatsAppMonitoring{Overview,Gate,Activity,Cohort,Audit,MutationSafety,UnexpectedOutbound}`) + new TypeScript types in `frontend/src/types/domain.ts`. |
| Phase 5F-Gate customer pilot model | `backend/apps/whatsapp/models.py` (`WhatsAppPilotCohortMember`) - references `crm.Customer`, stores masked phone/suffix only, tracks pending/approved/paused/removed, consent verification, source, approver, daily cap, notes, metadata. |
| Phase 5F-Gate customer pilot selectors | `backend/apps/whatsapp/pilot.py` - read-only readiness summary, consent checks, masked phone helpers, SaaS gap audit, and safe prepare/pause service helpers. No send path and no Order/Payment/Shipment/Discount mutation. |
| Phase 5F-Gate customer pilot commands | `inspect_whatsapp_customer_pilot --json`, `prepare_whatsapp_customer_pilot_member --phone +91... --name "..." --source approved_customer_pilot --json`, `pause_whatsapp_customer_pilot_member --phone +91... --reason "..." --json`. Prepare/pause write audit rows only; inspect is read-only. |
| Phase 5F-Gate customer pilot API | `backend/apps/whatsapp/v1_urls.py` + `monitoring_views.py` expose read-only `/api/v1/whatsapp/monitoring/pilot/` and include `pilot` in `/api/v1/whatsapp/monitoring/overview/`. Phones masked; admin/director/superuser monitoring permission only. |
| Phase 5F-Gate customer pilot dashboard | `frontend/src/pages/WhatsAppMonitoring.tsx` adds the read-only "Approved Customer Pilot Readiness" section: counts, blockers, nextAction, masked member table, daily caps. No send / approve / pause / enable controls. |
| Phase 6A SaaS Foundation models | `backend/apps/saas/models.py` (`Organization`, `Branch`, `OrganizationMembership`, `OrganizationFeatureFlag`, `OrganizationSetting`) |
| Phase 6A SaaS selectors | `backend/apps/saas/selectors.py` — read-only tenant lookups; `get_default_organization` / `get_user_organizations` / `is_feature_enabled` / `get_non_sensitive_settings` (sensitive rows omitted) |
| Phase 6A SaaS API endpoints | `backend/apps/saas/views.py` + `backend/apps/saas/urls.py` mounted at `/api/v1/saas/{current-organization,my-organizations,feature-flags}/` (read-only; admin/director/viewer auth all allowed; POST/PATCH/DELETE → 405) |
| Phase 6A default-org seeder | `python manage.py ensure_default_organization [--json] [--skip-memberships]` — idempotent; emits `saas.default_organization.ensured`; never mutates business data |
| Phase 6A frontend org badge | `frontend/src/components/layout/OrgBadge.tsx` (read-only topbar pill) + `frontend/src/services/api.ts` (`getSaasCurrentOrganization` / `getSaasMyOrganizations` / `getSaasFeatureFlags`) + new types in `frontend/src/types/domain.ts` |
| Phase 6B nullable org/branch FKs | Per-model — added `organization` (and `branch` where applicable) to `crm.Lead`, `crm.Customer`, `orders.Order`, `orders.DiscountOfferLog`, `payments.Payment`, `shipments.Shipment`, `calls.Call`, `whatsapp.WhatsAppConsent`, `whatsapp.WhatsAppConversation`, `whatsapp.WhatsAppMessage`, `whatsapp.WhatsAppLifecycleEvent`, `whatsapp.WhatsAppHandoffToCall`, `whatsapp.WhatsAppPilotCohortMember`, `audit.AuditEvent`. All `null=True, blank=True, on_delete=SET_NULL`. |
| Phase 6B backfill command | `python manage.py backfill_default_organization_data [--dry-run\|--apply] [--json]` — defaults to dry-run; emits `saas.default_org_backfill.{started,completed,failed}`; never overwrites existing assignments. |
| Phase 6B coverage inspector | `python manage.py inspect_default_organization_coverage [--json]` — read-only per-model coverage report with typed `nextAction`. |
| Phase 6B coverage selector | `backend/apps/saas/coverage.compute_default_organization_coverage` — single source of truth used by the CLI inspector and the API endpoint. |
| Phase 6B data-coverage API | `GET /api/v1/saas/data-coverage/` (auth required, read-only, no secrets). |
| Phase 6B frontend coverage card | `frontend/src/components/SaasCoverageCard.tsx` rendered on `frontend/src/pages/Index.tsx` — read-only org/branch coverage % + nextAction. |
| Phase 6C SaaS context module | `backend/apps/saas/context.py` — resolvers (`get_default_organization`, `get_user_active_organization`, `resolve_request_organization`, `user_has_org_access`) + queryset helpers (`scoped_queryset_for_user`, `scoped_queryset_for_request`, `filter_queryset_by_organization`). Models without `organization` field pass through; superusers see across tenants. |
| Phase 6C audit auto-org context | `backend/apps/audit/signals.write_event` — accepts `organization=` / `request=` / `user=` and auto-resolves the active org (falls back to default). Backwards-compatible. |
| Phase 6C readiness command | `python manage.py inspect_org_scoped_api_readiness [--json]` + `backend/apps/saas/readiness.compute_org_scoped_api_readiness`. Returns scoped/unscoped model + API map, audit-auto-org status, `safeToStartPhase6D`. |
| Phase 6C readiness API | `GET /api/v1/saas/org-scope-readiness/` (auth required, read-only). |
| Phase 6C frontend readiness card | `frontend/src/components/OrgScopeReadinessCard.tsx` rendered on `frontend/src/pages/Index.tsx` — read-only audit-auto-org / global-filter / scoped/unscoped counts / nextAction. No mutation controls. |
| Phase 6D write context helpers | `backend/apps/saas/write_context.py` — `resolve_write_organization`, `resolve_write_branch`, `apply_org_branch`, `get_parent_org_branch`, `assign_org_branch_from_parent`, `assign_org_branch_from_first_parent`. Pure helpers; never persist. |
| Phase 6D auto-assign signal | `backend/apps/saas/signals.py` — `auto_assign_org_branch` connected via `apps.SaasConfig.ready` to 13 business models. Fires only on create + when FK is NULL. Inherits from parent chain, falls back to default org / branch. Silent on errors. |
| Phase 6D/6E readiness command | `python manage.py inspect_org_write_path_readiness [--json]` + `backend/apps/saas/write_readiness.compute_org_write_path_readiness`. Returns covered/deferred create paths, system/global exceptions, recent unscoped writes, `enforcementMode`, `safeToStartPhase6E`, and `safeToStartPhase6F`. |
| Phase 6D readiness API | `GET /api/v1/saas/write-path-readiness/` (auth required, read-only). |
| Phase 6D frontend readiness card | `frontend/src/components/WritePathReadinessCard.tsx` rendered on `frontend/src/pages/Index.tsx` — read-only auto-assign / covered-paths / missing-row counts / nextAction. No mutation controls. |
| Phase 6E integration setting model | `backend/apps/saas/models.py` (`OrganizationIntegrationSetting`) — per-org provider readiness only; non-sensitive config plus `ENV:` / `VAULT:` secret refs. Runtime providers still use env/config. |
| Phase 6E integration selectors | `backend/apps/saas/integration_settings.py` — provider readiness and secret-ref masking. APIs return masked refs/booleans only. |
| Phase 6E SaaS admin selectors/API | `backend/apps/saas/admin_readiness.py`, `backend/apps/saas/views.py`, `backend/apps/saas/urls.py` — admin overview, organizations, integration settings, integration readiness. |
| Phase 6E SaaS Admin Panel | `frontend/src/pages/SaasAdmin.tsx` at `/saas-admin` — read-only org scope, write readiness, integration readiness, safety locks, SaaS audit events; no send/enable/provider-activation controls. |
| Phase 6H live gate policy | `backend/apps/saas/live_gate_policy.py` - source of truth for protected live operations; all Phase 6H policies keep `allowedInPhase6H=false`. |
| Phase 6H live gate service | `backend/apps/saas/live_gate.py` - evaluates/records sanitized audit-only gate decisions; never calls providers and always keeps `externalCallWillBeMade=false` in Phase 6H. |
| Phase 6H live gate models | `backend/apps/saas/models.py` (`RuntimeLiveGatePolicySnapshot`, `RuntimeLiveExecutionRequest`, `RuntimeKillSwitch`) |
| Phase 6H live gate commands/API/UI | `inspect_runtime_live_audit_gate`, `preview_live_gate_decision`, `request/approve/reject_live_execution_*`, `enable/disable_runtime_kill_switch`; `/api/v1/saas/runtime-live-gate/...`; `frontend/src/pages/SaasAdmin.tsx` Controlled Runtime Live Audit Gate section. |
| Phase 6I live gate simulation | `backend/apps/saas/live_gate_simulation.py`, `RuntimeLiveGateSimulation`, `*single_internal_live_gate_simulation` commands, `/api/v1/saas/runtime-live-gate/simulations/...`, and `/saas-admin` Single Internal Live Gate Simulation section; simulation-only, no provider calls. |
| Phase 6R dispatch readiness service | `backend/apps/payments/razorpay_payment_dispatch_readiness.py` (`build_phase6r_payment_dispatch_readiness_contract`, `validate_phase6r_source_gate_eligibility`, `assert_phase6r_no_live_send_or_courier_mutation`, `preview/prepare/approve/reject/archive_phase6r_payment_dispatch_readiness_gate`, `summarize/inspect_phase6r_payment_dispatch_readiness`) — audit-only readiness contract; never sends WhatsApp; never calls Meta Cloud / Delhivery; never creates a shipment / AWB; never mutates real business tables; never calls Razorpay |
| Phase 6S dispatch pilot plan service | `backend/apps/payments/razorpay_payment_dispatch_pilot_plan.py` (`build_phase6s_payment_dispatch_pilot_contract`, `validate_phase6s_source_readiness_gate_eligibility`, `assert_phase6s_no_live_execution_or_provider_call`, `preview/prepare/approve/reject/archive_phase6s_payment_dispatch_pilot_plan`, `summarize/inspect_phase6s_payment_dispatch_pilot_plan_readiness`) — planning-only Limited Internal Dispatch Pilot Plan; never executes a pilot; never sends WhatsApp; never calls Meta Cloud / Delhivery / Razorpay; never creates a shipment / AWB; never mutates real business tables |
| Phase 6S pilot plan model | `backend/apps/payments/models.py` (`RazorpayPaymentDispatchPilotPlan`) + migration `payments.0009_phase6s_payment_dispatch_pilot_plan` |
| Phase 6S commands | `python manage.py inspect_razorpay_payment_dispatch_pilot_plan_readiness [--no-audit] [--json]`, `preview_razorpay_payment_dispatch_pilot_plan --readiness-id <PHASE6R_READINESS_ID> [--json]`, `prepare_razorpay_payment_dispatch_pilot_plan --readiness-id <PHASE6R_READINESS_ID> [--json]`, `approve_razorpay_payment_dispatch_pilot_plan --plan-id <ID> --reason "…" [--json]` (require_reason=True), `reject_razorpay_payment_dispatch_pilot_plan --plan-id <ID> --reason "…" [--json]`, `archive_razorpay_payment_dispatch_pilot_plan --plan-id <ID> --reason "…" [--json]`, `inspect_razorpay_payment_dispatch_pilot_plans [--limit N] [--json]` |
| Phase 6S read-only API | `apps.saas.views.RazorpayPaymentDispatchPilotPlanReadinessView` / `RazorpayPaymentDispatchPilotPlansListView` / `RazorpayPaymentDispatchPilotPlanDetailView` / `RazorpayPaymentDispatchPilotPlanPreviewView` mounted under `/api/v1/saas/razorpay/payment-dispatch-pilot-plan{,-plans,-plans/<id>,-preview}/`; auth + admin only; POST/PATCH/DELETE → 405 |
| Phase 6S frontend section | `frontend/src/pages/SaasAdmin.tsx` ("Razorpay Limited Internal Dispatch Pilot Plan" section, `data-testid="razorpay-payment-dispatch-pilot-plan-section"`) + new TS types in `frontend/src/types/domain.ts` + `getSaasRazorpayPaymentDispatchPilotPlanReadiness` / `getSaasRazorpayPaymentDispatchPilotPlans` in `frontend/src/services/api.ts` + mock fixtures in `frontend/src/services/mockData.ts` |
| Phase 6R readiness model | `backend/apps/payments/models.py` (`RazorpayPaymentDispatchReadinessGate`) + migration `payments.0008_phase6r_payment_dispatch_readiness` |
| Phase 6R commands | `python manage.py inspect_razorpay_payment_dispatch_readiness [--no-audit] [--json]`, `preview_razorpay_payment_dispatch_readiness_gate --gate-id <PHASE6Q_GATE_ID> [--json]`, `prepare_razorpay_payment_dispatch_readiness_gate --gate-id <PHASE6Q_GATE_ID> [--json]`, `approve_razorpay_payment_dispatch_readiness_gate --readiness-id <ID> --reason "…" [--json]` (require_reason=True), `reject_razorpay_payment_dispatch_readiness_gate --readiness-id <ID> --reason "…" [--json]`, `archive_razorpay_payment_dispatch_readiness_gate --readiness-id <ID> --reason "…" [--json]`, `inspect_razorpay_payment_dispatch_readiness_gates [--limit N] [--json]` |
| Phase 6R read-only API | `apps.saas.views.RazorpayPaymentDispatchReadinessView` / `RazorpayPaymentDispatchReadinessGatesListView` / `RazorpayPaymentDispatchReadinessGateDetailView` / `RazorpayPaymentDispatchReadinessPreviewView` mounted under `/api/v1/saas/razorpay/payment-dispatch-readiness{,-gates,-gates/<id>,-preview}/`; auth + admin only; POST/PATCH/DELETE → 405 |
| Phase 6R frontend section | `frontend/src/pages/SaasAdmin.tsx` ("Razorpay Payment → WhatsApp / Courier Dispatch Readiness" section, `data-testid="razorpay-payment-dispatch-readiness-section"`) + new TS types in `frontend/src/types/domain.ts` + `getSaasRazorpayPaymentDispatchReadiness` / `getSaasRazorpayPaymentDispatchReadinessGates` in `frontend/src/services/api.ts` + mock fixtures in `frontend/src/services/mockData.ts` |
| Phase 7D execution service | `backend/apps/payments/razorpay_controlled_pilot_execution.py` (`build_phase7d_controlled_pilot_execution_contract`, `validate_phase7d_source_gate_eligibility`, `assert_phase7d_no_business_mutation`, `preview/prepare/approve/execute/rollback/archive/recover_phase7d_razorpay_test_execution_*`, `summarize/inspect_phase7d_razorpay_test_execution_readiness`) — one-shot internal Razorpay TEST execution capability; CLI-only execution path; never sends WhatsApp; never calls Meta Cloud / Delhivery / Vapi; never creates a shipment / AWB; never creates a payment link; never captures; never refunds; never mutates real business tables; never edits any `.env*` file |
| Phase 7D models | `backend/apps/payments/models.py` (`RazorpayControlledPilotExecutionAttempt`, `RazorpayControlledPilotExecutionRollback`) + migration `payments.0012_phase7d_controlled_pilot_execution` |
| Phase 7D commands | `python manage.py inspect_razorpay_controlled_pilot_execution_readiness [--no-audit] [--json]`, `preview_razorpay_controlled_pilot_execution_attempt --gate-id <ID> [--json]`, `prepare_razorpay_controlled_pilot_execution_attempt --gate-id <ID> [--json]`, `approve_razorpay_controlled_pilot_execution_attempt --attempt-id <ID> --reason "…" [--json]` (require_reason=True), `execute_razorpay_controlled_pilot_test_order --attempt-id <ID> --confirm-one-shot-test-execution --director-signoff "…" [--json]` (CLI-only one-shot Razorpay TEST execution; refuses on missing flags / non-`rzp_test_` key / kill-switch off / not-approved attempt / second-run idempotency lock), `rollback_razorpay_controlled_pilot_execution_attempt --attempt-id <ID> --reason "…" [--json]`, `archive_razorpay_controlled_pilot_execution_attempt --attempt-id <ID> --reason "…" [--json]`, `inspect_razorpay_controlled_pilot_execution_attempts [--limit N] [--json]`, `recover_razorpay_controlled_pilot_execution_attempt --idempotency-key <KEY> --provider-object-id <ORDER_ID> [--json]` |
| Phase 7D read-only API | `apps.saas.views.RazorpayControlledPilotExecutionReadinessView` / `RazorpayControlledPilotExecutionAttemptsListView` / `RazorpayControlledPilotExecutionAttemptDetailView` / `RazorpayControlledPilotExecutionPreviewView` / `RazorpayControlledPilotExecutionRollbacksView` mounted under `/api/v1/saas/razorpay/controlled-pilot-execution-{readiness,attempts,attempts/<id>,preview,rollbacks/<attempt_id>}/`; auth + admin only; POST/PATCH/DELETE → 405; **no POST execute / approve / reject / archive endpoint** |
| Phase 7D frontend section | `frontend/src/pages/SaasAdmin.tsx` ("Razorpay Controlled Pilot Execution (One-shot TEST)" section, `data-testid="razorpay-controlled-pilot-execution-section"`) + new TS types in `frontend/src/types/domain.ts` + `getSaasRazorpayControlledPilotExecutionReadiness` / `getSaasRazorpayControlledPilotExecutionAttempts` in `frontend/src/services/api.ts` + mock fixtures in `frontend/src/services/mockData.ts` |
| Phase 7E shared UTC window utility | `backend/apps/saas/utc_window.py` — pure `parse_director_signoff_window` + `validate_review_window` (24h window, Phase 7E approve) + `validate_within_director_window` / `validate_execution_window` alias (15-min window, Phase 7D-Hotfix-1 execute). Module is pure (no DB, no settings reads, no env reads, no logging, no Django imports). |
| Phase 7D-Hotfix-1 execute window guard | `backend/apps/payments/management/commands/execute_razorpay_controlled_pilot_test_order.py` + `backend/apps/saas/management/commands/execute_single_razorpay_test_order.py` — both refuse to dispatch unless `apps.saas.utc_window.parse_director_signoff_window` returns a `ParsedWindow` AND `validate_within_director_window` returns `valid=True`. Service-layer wiring lives in `apps.payments.razorpay_controlled_pilot_execution.execute_phase7d_razorpay_test_order` and `apps.saas.provider_execution.execute_single_razorpay_test_order` (both persist parsed `recorded_signoff_window_*` fields on the attempt row). Migrations: `payments.0014_phase7d_hotfix_director_signoff_window` + `saas.0007_phase7d_hotfix_director_signoff_window` (schema-only; no `RunPython`). Historical Phase 7D attempt id 1 (`order_SmThqpK6sc6Dhs`) stays `NULL` on the new fields and remains the canonical legacy free-text row — Phase 7E continues to handle it via the `--acknowledge-source-phase7d-window-violation` path. |
| Phase 7E gate service | `backend/apps/payments/razorpay_whatsapp_internal_notification.py` (`build_phase7e_whatsapp_internal_notification_contract`, `validate_phase7e_source_eligibility`, `assert_phase7e_no_send_or_business_mutation`, `preview/prepare/dry_run/rollback_dry_run/approve/reject/archive_phase7e_gate`, `summarize_phase7e_gates`, `inspect_phase7e_readiness`) — gate-only readiness contract for an executed-and-rolled-back Phase 7D attempt; never sends WhatsApp; never calls Meta Cloud / Delhivery / Vapi; never creates a shipment / AWB / payment link; never captures; never refunds; never sends a customer notification; never mutates real business rows; never imports `apps.whatsapp.services.send_*` / `meta_cloud_client` / `dotenv` (asserted with static-file scan); never edits any `.env*` file |
| Phase 7E models | `backend/apps/payments/models.py` (`RazorpayWhatsAppInternalNotificationGate`, `RazorpayWhatsAppInternalNotificationDryRunRecord`) + migration `payments.0013_phase7e_whatsapp_internal_notification_gate` |
| Phase 7E commands | `python manage.py inspect_razorpay_whatsapp_internal_notification_readiness [--no-audit] [--json]`, `preview_razorpay_whatsapp_internal_notification_gate --attempt-id <ID> [--json]`, `prepare_razorpay_whatsapp_internal_notification_gate --attempt-id <ID> [--json]`, `dry_run_razorpay_whatsapp_internal_notification_gate --gate-id <ID> [--json]`, `rollback_dry_run_razorpay_whatsapp_internal_notification_gate --gate-id <ID> --reason "…" [--json]` (require_reason=True), `approve_razorpay_whatsapp_internal_notification_gate --gate-id <ID> --reason "…" --director-signoff "…" [--acknowledge-source-phase7d-window-violation] [--json]` (require_reason=True; structured `BEGIN_UTC=...`/`END_UTC=...` markers required + literal `phase7d_attempt_id_<ID>` reference + window length ≤ 24h; legacy free-text source signoff requires `--acknowledge-source-phase7d-window-violation` + reason contains `acknowledged_phase7d_window_violation_ref_attempt_<ID>`), `reject_razorpay_whatsapp_internal_notification_gate --gate-id <ID> --reason "…" [--json]` (require_reason=True; refuses unless gate is `draft`/`pending_manual_review`), `inspect_razorpay_whatsapp_internal_notification_gates [--limit N] [--json]` |
| Phase 7E read-only API | `apps.saas.views.RazorpayWhatsAppInternalNotificationReadinessView` / `RazorpayWhatsAppInternalNotificationGatesListView` / `RazorpayWhatsAppInternalNotificationGateDetailView` / `RazorpayWhatsAppInternalNotificationPreviewView` / `RazorpayWhatsAppInternalNotificationDryRunsView` mounted under `/api/v1/saas/razorpay/whatsapp-internal-notification-{readiness,gates,gates/<pk>,preview,dry-runs/<gate_id>}/`; auth + admin only; POST/PATCH/DELETE → 405; **no POST endpoint dispatches state changes** |
| Phase 7E frontend section | `frontend/src/pages/SaasAdmin.tsx` ("Razorpay → WhatsApp Internal Notification Readiness" section, `data-testid="razorpay-whatsapp-internal-notification-section"`) + new TS types in `frontend/src/types/domain.ts` + `getSaasRazorpayWhatsAppInternalNotificationReadiness` / `getSaasRazorpayWhatsAppInternalNotificationGates` in `frontend/src/services/api.ts` + mock fixtures in `frontend/src/services/mockData.ts` |
| Phase 7E plan + Hotfix-1 plan | `docs/PHASE_7E_PLAN.md` (corrected Phase 7E plan), `docs/PHASE_7D_HOTFIX_1_PLAN.md` (mandatory follow-up: structured UTC window guard for execute commands; reuses `apps.saas.utc_window`) |
| Phase 7F gate service | `backend/apps/payments/razorpay_courier_readiness.py` (`build_phase7f_courier_readiness_contract`, `validate_phase7f_source_eligibility`, `assert_phase7f_no_courier_or_business_mutation`, `preview/prepare/dry_run/rollback_dry_run/approve/reject_phase7f_gate`, `summarize_phase7f_gates`, `inspect_phase7f_readiness`) — gate-only courier readiness contract; never calls Delhivery; never creates a `Shipment` / `WorkflowStep` / `RescueAttempt` / AWB / pickup / label; never sends WhatsApp; never calls Meta Cloud / Razorpay / Vapi; never mutates real business rows; never imports the Delhivery client / shipments service / WhatsApp send helpers / `meta_cloud_client` / `razorpay_client` / `dotenv` (asserted with static-file scan); never edits any `.env*` file |
| Phase 7F models | `backend/apps/payments/models.py` (`RazorpayCourierReadinessGate`, `RazorpayCourierReadinessDryRunRecord`) + migration `payments.0015_phase7f_courier_readiness_gate` |
| Phase 7F commands | `python manage.py inspect_delhivery_courier_readiness [--no-audit] [--json]`, `preview_delhivery_courier_readiness_gate --phase7e-gate-id <ID> [--json]`, `prepare_delhivery_courier_readiness_gate --phase7e-gate-id <ID> [--json]`, `dry_run_delhivery_courier_readiness_gate --gate-id <ID> [--json]`, `rollback_dry_run_delhivery_courier_readiness_gate --gate-id <ID> --reason "…" [--json]` (require_reason=True), `approve_delhivery_courier_readiness_gate --gate-id <ID> --reason "…" [--json]` (require_reason=True; **NO --director-signoff argument**; refuses unless dry-run + rollback-dry-run + Hotfix-1 all pass), `reject_delhivery_courier_readiness_gate --gate-id <ID> --reason "…" [--json]` (require_reason=True; refuses unless gate is draft/pending_manual_review), `inspect_delhivery_courier_readiness_gates [--limit N] [--json]` |
| Phase 7F read-only API | `apps.saas.views.RazorpayCourierReadinessReadinessView` / `RazorpayCourierReadinessGatesListView` / `RazorpayCourierReadinessGateDetailView` / `RazorpayCourierReadinessPreviewView` / `RazorpayCourierReadinessDryRunsView` mounted under `/api/v1/saas/delhivery/courier-readiness{,-gates,-gates/<pk>,-preview,-dry-runs/<gate_id>}/`; auth + admin only; POST/PATCH/DELETE → 405; **no POST endpoint dispatches state changes** |
| Phase 7F frontend section | `frontend/src/pages/SaasAdmin.tsx` ("Delhivery / Courier Readiness" section, `data-testid="delhivery-courier-readiness-section"`) + new TS types in `frontend/src/types/domain.ts` + `getSaasRazorpayCourierReadiness` / `getSaasRazorpayCourierReadinessGates` in `frontend/src/services/api.ts` + mock fixtures in `frontend/src/services/mockData.ts` |
| Phase 7F plan | `docs/PHASE_7F_PLAN.md` — final plan; chain naming (`RazorpayCourierReadinessGate`); archive deferred; approve takes only `--reason`; 31 forbidden actions |
| Phase 7G execution service | `backend/apps/payments/razorpay_courier_execution.py` (`build_phase7g_courier_execution_contract`, `validate_phase7g_source_chain`, `assert_phase7g_no_unauthorised_mutation`, `preview/prepare/approve/reject_phase7g_courier_execution_attempt`, `execute_phase7g_courier_one_shot` — the **only** Delhivery-touching function with **lazy local-scope** import of `apps.shipments.integrations.delhivery_client.create_awb` inside `_create_awb_via_dedicated_wrapper`, `rollback_phase7g_courier_execution_attempt` (record-only — never calls Delhivery cancel), `summarize_phase7g_attempts`, `inspect_phase7g_courier_execution_readiness`) — one-shot internal Delhivery TEST/MOCK courier execution capability; CLI-only execution path; **Phase 7G writes NO `Shipment` row** — provider/AWB summary lives on the attempt row only; never books a courier pickup separately; never generates / prints a courier label; never sends or queues WhatsApp; never calls Meta Cloud / Razorpay / Vapi; never sends a customer notification; never mutates real `Order` / `Payment` / `Customer` / `Lead` / `DiscountOfferLog` rows; never edits any `.env*` file |
| Phase 7G models | `backend/apps/payments/models.py` (`RazorpayCourierExecutionAttempt`, `RazorpayCourierExecutionRollback`) + migration `payments.0016_phase7g_courier_execution_attempt` |
| Phase 7G commands | `python manage.py inspect_delhivery_courier_execution_readiness [--no-audit] [--json]`, `preview_delhivery_courier_execution_attempt --gate-id <PHASE7F_GATE_ID> [--json]`, `prepare_delhivery_courier_execution_attempt --gate-id <PHASE7F_GATE_ID> [--json]`, `approve_delhivery_courier_execution_attempt --attempt-id <ID> --reason "…" [--json]` (require_reason=True), `reject_delhivery_courier_execution_attempt --attempt-id <ID> --reason "…" [--json]` (require_reason=True), `execute_delhivery_courier_one_shot --attempt-id <ID> --confirm-one-shot-courier-execution --director-signoff "…" --operator-name "…" --mode-acknowledgement "mock\|test" --rollback-record-only-acknowledged [--json]` (CLI-only one-shot Delhivery TEST/MOCK execution; refuses on missing flags / mode-ack mismatch / kill-switch off / not-approved attempt / second-run idempotency lock), `rollback_delhivery_courier_execution_attempt --attempt-id <ID> --reason "…" [--json]` (record-only — never calls Delhivery cancel), `inspect_delhivery_courier_execution_attempts [--limit N] [--json]`. **Archive + recover commands intentionally deferred.** |
| Phase 7G read-only API | `apps.saas.views.RazorpayCourierExecutionReadinessView` / `RazorpayCourierExecutionAttemptsListView` / `RazorpayCourierExecutionAttemptDetailView` / `RazorpayCourierExecutionPreviewView` / `RazorpayCourierExecutionRollbacksView` mounted under `/api/v1/saas/delhivery/courier-execution-{readiness,attempts,attempts/<pk>,preview,rollbacks/<attempt_id>}/`; auth + admin only; POST/PATCH/DELETE → 405; **no POST execute / approve / reject / archive endpoint** |
| Phase 7G frontend section | `frontend/src/pages/SaasAdmin.tsx` ("Razorpay Delhivery Courier One-shot TEST/MOCK Execution" section, `data-testid="razorpay-courier-execution-section"`) + new TS types in `frontend/src/types/domain.ts` + `getSaasRazorpayCourierExecutionReadiness` / `getSaasRazorpayCourierExecutionAttempts` in `frontend/src/services/api.ts` + mock fixtures in `frontend/src/services/mockData.ts` |
| Phase 7G plan | `docs/PHASE_7G_PLAN.md` — final plan; chain naming (`RazorpayCourierExecutionAttempt`); archive + recover deferred; synthetic payload only (`"Phase 7G TEST"` / phone last-4 `"0000"` / `"[redacted]"` / pin prefix `"11000"`); 33 forbidden actions; 22 forbidden payload keys; allowed `DELHIVERY_MODE` ∈ {`mock`, `test`}; **Phase 7G-Live (real customer courier execution) remains NOT approved** |
| Phase 8A sandbox service | `backend/apps/payments/phase8a_payment_order_mutation_sandbox.py` (`build/_build_evidence_json`, `assert_phase8a_no_business_mutation`, `_validate_phase7i_lock`, `_validate_synthetic_reference`, `preview/prepare/dry_run/rollback_dry_run/approve/reject/archive_phase8a_payment_order_mutation_sandbox`, `summarize_phase8a_gates`, `inspect_phase8a_payment_order_mutation_sandbox_readiness`) — sandbox / dry-run only against a *locked* Phase 7I final audit lock; **never calls Razorpay / Meta Cloud / Delhivery / Vapi; never sends or queues WhatsApp; never creates a `Shipment` / `WorkflowStep` / `RescueAttempt` / AWB / payment link; never captures; never refunds; never sends a customer notification; never mutates real `Order` / `Payment` / `Shipment` / `DiscountOfferLog` / `Customer` / `Lead` / `WhatsAppMessage` / `WhatsAppLifecycleEvent` / `WhatsAppHandoffToCall` rows; never edits any `.env*` file** |
| Phase 8A models | `backend/apps/payments/models.py` (`RazorpayPaymentOrderMutationSandboxGate`, `RazorpayPaymentOrderMutationDryRun`) + migration `payments.0019_phase8a_payment_order_mutation_sandbox` |
| Phase 8A commands | `python manage.py inspect_phase8a_payment_order_mutation_sandbox [--no-audit] [--json]`, `preview_phase8a_payment_order_mutation_sandbox --phase7i-lock-id <ID> [--json]`, `prepare_phase8a_payment_order_mutation_sandbox --phase7i-lock-id <ID> [--json]`, `dry_run_phase8a_payment_order_mutation_sandbox --gate-id <ID> --synthetic-order-reference "phase8a::sandbox::…" [--json]` (refuses unless the reference begins with `phase8a::sandbox::` / `phase8a-sandbox-` / `sandbox::`), `rollback_dry_run_phase8a_payment_order_mutation_sandbox --dry-run-id <ID> --reason "…" [--json]` (require_reason=True), `approve_phase8a_payment_order_mutation_sandbox --gate-id <ID> --reason "…" [--json]` (require_reason=True; refuses unless `gate.status=dry_run_passed` AND at least one passed dry-run exists), `reject_phase8a_payment_order_mutation_sandbox --gate-id <ID> --reason "…" [--json]` (require_reason=True), `archive_phase8a_payment_order_mutation_sandbox --gate-id <ID> --reason "…" [--json]` (require_reason=True) |
| Phase 8A read-only API | `apps.saas.views.Phase8APaymentOrderMutationSandboxReadinessView` / `Phase8APaymentOrderMutationSandboxGatesListView` / `Phase8APaymentOrderMutationSandboxGateDetailView` / `Phase8APaymentOrderMutationSandboxPreviewView` / `Phase8APaymentOrderMutationSandboxDryRunsView` mounted under `/api/v1/saas/phase8/payment-order-mutation-sandbox-{readiness,gates,gates/<pk>,preview,dry-runs/<gate_id>}/`; auth + admin only; POST/PATCH/DELETE → 405; **no POST endpoint dispatches state changes** (review is CLI-only) |
| Phase 8A frontend section | `frontend/src/pages/SaasAdmin.tsx` ("Phase 8A Payment → Order Mutation Sandbox Gate" section, `data-testid="phase8a-payment-order-mutation-sandbox-section"`) + new TS types in `frontend/src/types/domain.ts` (`SaasPhase8APaymentOrderMutationSandboxReadiness`, `SaasPhase8APaymentOrderMutationSandboxGatesResponse`, `SaasPhase8APaymentOrderMutationSandboxGateDto`, `SaasPhase8APaymentOrderMutationDryRunDto`) + `getSaasPhase8APaymentOrderMutationSandboxReadiness` / `getSaasPhase8APaymentOrderMutationSandboxGates` in `frontend/src/services/api.ts` + mock fixtures in `frontend/src/services/mockData.ts` |
| Phase 8B review service | `backend/apps/payments/phase8b_payment_order_mutation_review.py` (`build/_build_evidence_json`, `assert_phase8b_no_business_mutation`, `_validate_phase8a_gate`, `_validate_phase7i_lock`, `_validate_phase7d_attempt`, `_validate_review_reference`, `preview/prepare/dry_run/rollback_dry_run/approve/reject/archive_phase8b_payment_order_mutation_review_gate`, `summarize_phase8b_gates`, `inspect_phase8b_payment_order_mutation_review_readiness`) — review / dry-run only against an *approved* Phase 8A sandbox gate; **never calls Razorpay / Meta Cloud / Delhivery / Vapi; never sends or queues WhatsApp; never creates a `Shipment` / `WorkflowStep` / `RescueAttempt` / AWB / payment link; never captures; never refunds; never sends a customer notification; never mutates real `Order` / `Payment` / `Shipment` / `DiscountOfferLog` / `Customer` / `Lead` / `WhatsAppMessage` / `WhatsAppLifecycleEvent` / `WhatsAppHandoffToCall` rows; never approves Phase 8C; never edits any `.env*` file** |
| Phase 8B models | `backend/apps/payments/models.py` (`RazorpayPaymentOrderMutationReviewGate`, `RazorpayPaymentOrderMutationReviewDryRun`) + migration `payments.0020_phase8b_payment_order_mutation_review_gate` |
| Phase 8B commands | `python manage.py inspect_phase8b_payment_order_mutation_review_gate [--no-audit] [--json]`, `preview_phase8b_payment_order_mutation_review_gate --phase8a-gate-id <ID> [--json]`, `prepare_phase8b_payment_order_mutation_review_gate --phase8a-gate-id <ID> [--json]`, `dry_run_phase8b_payment_order_mutation_review_gate --gate-id <ID> --target-order-reference "phase8b::review::order::…" [--json]` (refuses unless the reference begins with `phase8b::review::order::` / `phase8b-review-` / `review::phase8b::`), `rollback_dry_run_phase8b_payment_order_mutation_review_gate --dry-run-id <ID> --reason "…" [--json]` (require_reason=True), `approve_phase8b_payment_order_mutation_review_gate --gate-id <ID> --reason "…" [--json]` (require_reason=True; refuses unless `gate.status=dry_run_passed` AND at least one passed dry-run AND a recorded rollback dry-run), `reject_phase8b_payment_order_mutation_review_gate --gate-id <ID> --reason "…" [--json]` (require_reason=True), `archive_phase8b_payment_order_mutation_review_gate --gate-id <ID> --reason "…" [--json]` (require_reason=True) |
| Phase 8B read-only API | `apps.saas.views.Phase8BPaymentOrderMutationReviewReadinessView` / `Phase8BPaymentOrderMutationReviewGatesListView` / `Phase8BPaymentOrderMutationReviewGateDetailView` / `Phase8BPaymentOrderMutationReviewPreviewView` / `Phase8BPaymentOrderMutationReviewDryRunsView` mounted under `/api/v1/saas/phase8/payment-order-mutation-review-{readiness,gates,gates/<pk>,preview,dry-runs/<gate_id>}/`; auth + admin only; POST/PATCH/DELETE → 405; **no POST endpoint dispatches state changes** (review is CLI-only) |
| Phase 8B frontend section | `frontend/src/pages/SaasAdmin.tsx` ("Phase 8B Payment → Order Mutation Review Gate" section, `data-testid="phase8b-payment-order-mutation-review-section"`) + new TS types in `frontend/src/types/domain.ts` (`SaasPhase8BPaymentOrderMutationReviewReadiness`, `SaasPhase8BPaymentOrderMutationReviewGatesResponse`, `SaasPhase8BPaymentOrderMutationReviewGateDto`, `SaasPhase8BPaymentOrderMutationReviewDryRunDto`) + `getSaasPhase8BPaymentOrderMutationReviewReadiness` / `getSaasPhase8BPaymentOrderMutationReviewGates` in `frontend/src/services/api.ts` + mock fixtures in `frontend/src/services/mockData.ts` |
| Phase 8C controlled mutation service | `backend/apps/payments/phase8c_payment_order_controlled_mutation.py` (`build/_build_evidence_json`, `assert_phase8c_no_unauthorized_side_effect`, `assert_phase8c_no_provider_send_or_courier`, `_validate_phase8b_gate`, `_validate_phase7i_lock`, `_validate_target_references`, `_validate_target_safety`, `_order_is_internal_sandbox`, `_payment_is_internal_sandbox`, `preview/prepare/dry_run/approve/execute/rollback/reject/archive_phase8c_payment_order_controlled_mutation`, `summarize_phase8c_gates`, `inspect_phase8c_payment_order_controlled_mutation_readiness`) — CLI-only one-shot controlled mutation; execute mutates ONLY `Order.payment_status` and `Payment.status` to `"Paid"` on a proven internal/sandbox/test target pair; **never calls Razorpay / Meta Cloud / Delhivery / Vapi; never sends or queues WhatsApp; never creates a `Shipment` / `WorkflowStep` / `RescueAttempt` / AWB / payment link; never captures; never refunds; never sends a customer notification; never mutates real `Customer` / `Lead` / `Shipment` / `DiscountOfferLog` / `WhatsAppMessage` / `WhatsAppLifecycleEvent` / `WhatsAppHandoffToCall` rows; never edits any `.env*` file** |
| Phase 8C models | `backend/apps/payments/models.py` (`RazorpayPaymentOrderControlledMutationGate`, `RazorpayPaymentOrderControlledMutationAttempt`, `RazorpayPaymentOrderControlledMutationRollback`) + migration `payments.0021_phase8c_controlled_payment_order_mutation` |
| Phase 8C commands | `python manage.py inspect_phase8c_payment_order_controlled_mutation [--no-audit] [--json]`, `preview_phase8c_payment_order_controlled_mutation --phase8b-gate-id <ID> [--json]`, `prepare_phase8c_payment_order_controlled_mutation --phase8b-gate-id <ID> [--json]`, `dry_run_phase8c_payment_order_controlled_mutation --gate-id <ID> --target-order-id … --target-payment-id … --target-order-reference "phase8c::controlled::order::…" --target-payment-reference "phase8c::controlled::payment::…" [--json]`, `approve_phase8c_payment_order_controlled_mutation --gate-id <ID> --reason "…" [--json]` (require_reason=True; refuses unless `gate.status=dry_run_passed` AND a `pending_director_signoff` attempt exists), `execute_phase8c_payment_order_controlled_mutation --attempt-id <ID> --confirm-one-shot-mutation --director-signoff "phase8c_attempt_id_<ID> phase8b_gate_id_<ID> BEGIN_UTC=… END_UTC=…" --operator-name "…" [--json]` (CLI-only one-shot execute; refuses on missing flags / non-structured signoff / stale or out-of-window / unsafe target / second-run); `rollback_phase8c_payment_order_controlled_mutation --attempt-id <ID> --reason "…" [--json]` (record-only restore), `reject_phase8c_payment_order_controlled_mutation --gate-id <ID> --reason "…" [--json]`, `archive_phase8c_payment_order_controlled_mutation --gate-id <ID> --reason "…" [--json]` |
| Phase 8C read-only API | `apps.saas.views.Phase8CPaymentOrderControlledMutationReadinessView` / `Phase8CPaymentOrderControlledMutationGatesListView` / `Phase8CPaymentOrderControlledMutationGateDetailView` / `Phase8CPaymentOrderControlledMutationPreviewView` / `Phase8CPaymentOrderControlledMutationAttemptsView` / `Phase8CPaymentOrderControlledMutationRollbacksView` mounted under `/api/v1/saas/phase8/payment-order-controlled-mutation-{readiness,gates,gates/<pk>,preview,attempts/<gate_id>,rollbacks/<attempt_id>}/`; auth + admin only; POST/PATCH/DELETE → 405; **no POST endpoint dispatches state changes or execute** (review + execute are CLI-only) |
| Phase 8C frontend section | `frontend/src/pages/SaasAdmin.tsx` ("Phase 8C Controlled Payment → Order Mutation" section, `data-testid="phase8c-payment-order-controlled-mutation-section"`) + new TS types in `frontend/src/types/domain.ts` (`SaasPhase8CPaymentOrderControlledMutationReadiness`, `SaasPhase8CPaymentOrderControlledMutationGatesResponse`, `SaasPhase8CPaymentOrderControlledMutationGateDto`, `SaasPhase8CPaymentOrderControlledMutationAttemptDto`, `SaasPhase8CPaymentOrderControlledMutationRollbackDto`) + `getSaasPhase8CPaymentOrderControlledMutationReadiness` / `getSaasPhase8CPaymentOrderControlledMutationGates` in `frontend/src/services/api.ts` + mock fixtures in `frontend/src/services/mockData.ts` |
| Phase 8C-Hotfix-1 fixture seed | `backend/apps/payments/management/commands/seed_phase8c_internal_controlled_mutation_fixture.py` — CLI-only idempotent `python manage.py seed_phase8c_internal_controlled_mutation_fixture [--apply] [--json]`; creates exactly ONE safe sandbox `Order.id="phase8c-controlled-order-001"` + `Payment.id="phase8c-controlled-payment-001"` with `raw_response.phase8c_sandbox=True`; **never calls a provider; never sends WhatsApp; never sends a customer notification; never creates a `Shipment` / AWB / `DiscountOfferLog` / `WorkflowStep` / `RescueAttempt`; never mutates `Customer` / `Lead`; never edits any `.env*` file**; 3 new audit kinds (`phase8c.fixture.{seeded,dry_run,blocked}`, each ≤ 64 chars); inline row-count guard runs inside `transaction.atomic()` and rolls back on unexpected drift |
| Phase 8D evidence lock service | `backend/apps/payments/phase8d_controlled_mutation_evidence_lock.py` (`build/_build_evidence_json`, `assert_phase8d_no_provider_or_business_mutation`, `_validate_phase8c_gate`, `_validate_phase8c_attempt`, `_validate_phase8c_rollback`, `_validate_final_target_state`, `preview/prepare/lock/reject/archive_phase8d_controlled_mutation_evidence_lock`, `summarize_phase8d_locks`, `inspect_phase8d_controlled_mutation_evidence_lock_readiness`) — lock-only meta-audit over the completed Phase 8C executed+rolled_back chain; **never executes / rolls back Phase 8C again; never calls Razorpay / Meta Cloud / Delhivery / Vapi; never sends or queues WhatsApp; never creates a `Shipment` / AWB / payment link; never captures / refunds; never sends a customer notification; never mutates real `Order` / `Payment` / `Customer` / `Lead` / `Shipment` / `DiscountOfferLog` / `WhatsAppMessage` rows; never edits any `.env*` file** |
| Phase 8D model | `backend/apps/payments/models.py` (`RazorpayPaymentOrderControlledMutationEvidenceLock`) + migration `payments.0022_phase8d_controlled_mutation_evidence_lock` |
| Phase 8D commands | `python manage.py inspect_phase8d_controlled_mutation_evidence_lock [--no-audit] [--json]`, `preview_phase8d_controlled_mutation_evidence_lock --phase8c-gate-id <ID> [--json]`, `prepare_phase8d_controlled_mutation_evidence_lock --phase8c-gate-id <ID> [--json]`, `lock_phase8d_controlled_mutation_evidence_lock --lock-id <ID> --reason "…" [--json]` (require_reason=True; revalidates eligibility at lock time), `reject_phase8d_controlled_mutation_evidence_lock --lock-id <ID> --reason "…" [--json]` (require_reason=True), `archive_phase8d_controlled_mutation_evidence_lock --lock-id <ID> --reason "…" [--json]` (require_reason=True) |
| Phase 8D read-only API | `apps.saas.views.Phase8DControlledMutationEvidenceLockReadinessView` / `Phase8DControlledMutationEvidenceLocksListView` / `Phase8DControlledMutationEvidenceLockDetailView` / `Phase8DControlledMutationEvidenceLockPreviewView` mounted under `/api/v1/saas/phase8/controlled-mutation-evidence-{lock-readiness,locks,locks/<pk>,lock-preview}/`; auth + admin only; POST/PATCH/DELETE → 405; **no POST lock / reject / archive endpoint exists** |
| Phase 8D frontend section | `frontend/src/pages/SaasAdmin.tsx` ("Phase 8D Controlled Mutation Evidence Lock" section, `data-testid="phase8d-controlled-mutation-evidence-lock-section"`) + new TS types in `frontend/src/types/domain.ts` (`SaasPhase8DControlledMutationEvidenceLockReadiness`, `SaasPhase8DControlledMutationEvidenceLocksResponse`, `SaasPhase8DControlledMutationEvidenceLockDto`) + `getSaasPhase8DControlledMutationEvidenceLockReadiness` / `getSaasPhase8DControlledMutationEvidenceLocks` in `frontend/src/services/api.ts` + mock fixtures in `frontend/src/services/mockData.ts` |
| Phase 8E pilot service | `backend/apps/payments/phase8e_real_customer_payment_order_pilot.py` (`build/_build_evidence_json`, `assert_phase8e_no_business_mutation`, `_validate_phase8d_lock`, `_validate_candidate_pair`, `_looks_like_phase8c_sandbox`, `_mask_phone_last4`, `_mask_customer_name`, `preview/prepare/select/dry_run/approve/reject/archive_phase8e_real_customer_payment_order_pilot`, `summarize_phase8e_gates`, `inspect_phase8e_real_customer_payment_order_pilot_readiness`) — review / dry-run only against ONE real-customer Order + Payment candidate; **never mutates real `Order` / `Payment` / `Customer` / `Lead` / `Shipment` / `DiscountOfferLog` / `WhatsAppMessage` rows; never calls Razorpay / Meta Cloud / Delhivery / Vapi; never sends or queues WhatsApp; never sends a customer notification; never creates a `Shipment` / AWB / payment link; never captures / refunds; never edits any `.env*` file; never exposes raw phone / email / address / provider payload / full `gateway_reference_id`** |
| Phase 8E models | `backend/apps/payments/models.py` (`RazorpayRealCustomerPaymentOrderMutationPilotGate`, `RazorpayRealCustomerPaymentOrderMutationCandidate`, `RazorpayRealCustomerPaymentOrderMutationPilotDryRun`) + migration `payments.0023_phase8e_real_customer_payment_order_pilot` |
| Phase 8E commands | `python manage.py inspect_phase8e_real_customer_payment_order_pilot [--no-audit] [--json]`, `preview_phase8e_real_customer_payment_order_pilot --phase8d-lock-id <ID> [--json]`, `prepare_phase8e_real_customer_payment_order_pilot --phase8d-lock-id <ID> [--json]`, `select_phase8e_real_customer_candidate --gate-id <ID> --order-id <ID> --payment-id <ID> [--json]` (rejects Phase 8C sandbox markers; persists only masked phone-last-4 / masked customer name; never persists raw_response), `dry_run_phase8e_real_customer_payment_order_pilot --gate-id <ID> --candidate-id <ID> [--json]`, `approve_phase8e_real_customer_payment_order_pilot --gate-id <ID> --reason "…" [--json]` (require_reason=True; refuses unless `gate.status=dry_run_passed` AND at least one passed dry-run AND at least one validated candidate; flips status to `approved_for_future_phase8f_real_customer_controlled_mutation` only), `reject_phase8e_real_customer_payment_order_pilot --gate-id <ID> --reason "…" [--json]` (require_reason=True), `archive_phase8e_real_customer_payment_order_pilot --gate-id <ID> --reason "…" [--json]` (require_reason=True). **No execute command exists.** |
| Phase 8E read-only API | `apps.saas.views.Phase8ERealCustomerPaymentOrderPilotReadinessView` / `Phase8ERealCustomerPaymentOrderPilotGatesListView` / `Phase8ERealCustomerPaymentOrderPilotGateDetailView` / `Phase8ERealCustomerPaymentOrderPilotPreviewView` / `Phase8ERealCustomerPaymentOrderPilotCandidatesView` / `Phase8ERealCustomerPaymentOrderPilotDryRunsView` mounted under `/api/v1/saas/phase8/real-customer-payment-order-pilot-{readiness,gates,gates/<pk>,preview,candidates/<gate_id>,dry-runs/<gate_id>}/`; auth + admin only; POST/PATCH/DELETE → 405; **no POST endpoint dispatches state changes** (review is CLI-only) |
| Phase 8E frontend section | `frontend/src/pages/SaasAdmin.tsx` ("Phase 8E Real Customer Payment → Order Pilot" section, `data-testid="phase8e-real-customer-payment-order-pilot-section"`) + new TS types in `frontend/src/types/domain.ts` (`SaasPhase8ERealCustomerPaymentOrderPilotReadiness`, `SaasPhase8ERealCustomerPaymentOrderPilotGatesResponse`, `SaasPhase8ERealCustomerPaymentOrderPilotGateDto`, `SaasPhase8ERealCustomerPaymentOrderPilotCandidateDto`, `SaasPhase8ERealCustomerPaymentOrderPilotDryRunDto`) + `getSaasPhase8ERealCustomerPaymentOrderPilotReadiness` / `getSaasPhase8ERealCustomerPaymentOrderPilotGates` in `frontend/src/services/api.ts` + mock fixtures in `frontend/src/services/mockData.ts` |
| Phase 8F-Hotfix-3 recovery command | `backend/apps/payments/management/commands/recover_phase8f_attempt_to_approved.py` - governance-only blocked-attempt recovery (`blocked` -> `approved_for_one_shot_real_mutation` + `phase8fHotfix3Recovery_recovered_from_blocked` audit marker); no Order/Payment/provider/WhatsApp/env touch; live execute remains NOT approved |
| Phase 8F-Hotfix-3 tests | `backend/tests/test_phase8f_hotfix3_attempt_recovery.py` - attempt not found, wrong status, gate not approved, executed-attempt guard, missing signoff markers, confirm/operator/flag/kill-switch refusals, success recovery, and proof the recovered attempt can pass execute preconditions in the test DB |
| Reward/Penalty engine (Phase 4B) | `backend/apps/rewards/engine.py` |
| RewardPenaltyEvent model | `backend/apps/rewards/models.py` |
| Reward/Penalty Celery task | `backend/apps/rewards/tasks.py` |
| `calculate_reward_penalties` command | `backend/apps/rewards/management/commands/calculate_reward_penalties.py` |
| Reward sweep / events / summary endpoints | `backend/apps/rewards/views.py` (`/api/rewards/{events,summary,sweep}/`) |
| Approval Matrix middleware (Phase 4C) | `backend/apps/ai_governance/approval_engine.py` |
| ApprovalRequest + ApprovalDecisionLog models | `backend/apps/ai_governance/models.py` |
| Approval endpoints | `backend/apps/ai_governance/views.py` (`/api/ai/approvals/{,id/,id/approve/,id/reject/,id/execute/,evaluate/}` + `/api/ai/agent-runs/{id}/request-approval/`) |
| Approved Action Execution (Phase 4D) | `backend/apps/ai_governance/approval_execution.py` |
| ApprovalExecutionLog model | `backend/apps/ai_governance/models.py` |
| Realtime AuditEvent serializer + publisher (Phase 4A) | `backend/apps/audit/realtime.py` |
| Audit WebSocket consumer (Phase 4A) | `backend/apps/audit/consumers.py` |
| Audit WebSocket route (Phase 4A) | `backend/apps/audit/routing.py` (`/ws/audit/events/`) |
| Top-level WebSocket router (Phase 4A) | `backend/config/routing.py` |
| Frontend realtime client | `frontend/src/services/realtime.ts` |
| Master Event Ledger receivers | `backend/apps/audit/signals.py` |
| Permissions (role-based) | `backend/apps/accounts/permissions.py` |
| Order state machine | `backend/apps/orders/services.py` (`ALLOWED_TRANSITIONS`) |
| Seed deterministic data | `backend/apps/dashboards/management/commands/seed_demo_data.py` |
| Env config | `backend/config/settings.py` + `backend/.env.example` |
| Endpoint reference | `docs/BACKEND_API.md` |
| How to run | `docs/RUNBOOK.md` |
| Phased roadmap | `docs/FUTURE_BACKEND_PLAN.md` |
| WhatsApp integration plan (Phase 5A-0) | `docs/WHATSAPP_INTEGRATION_PLAN.md` |
| Project handoff | `nd.md` |

---

## 6. When in doubt

1. Read `nd.md` (the full handoff). Section index is at the top.
2. Read the relevant section of [`docs/MASTER_BLUEPRINT_V2.md`](docs/MASTER_BLUEPRINT_V2.md) — strategic mirror of `nd.md`. The v1.0 PDF is historical only.
3. Run the health check: `pytest -q`, `npm test`, `npm run lint`, `npm run build`.
4. **Ask Prarit before doing anything that touches**: ad budgets, real medical claims, payment flows in live mode, customer messaging, kill-switch states, or production data.

The blueprint wins. Every decision in this codebase traces back to a section there.
